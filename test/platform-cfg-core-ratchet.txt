# Files in crates/thegn-core/src (outside termcaps.rs / sandbox*) that carry a
# platform #[cfg]. thegn-core is substrate-agnostic; per-OS behaviour should be
# data handed in by the host or a platform table. Debt to burn down.
#
# SHRINK-ONLY. Enforced by `platform_cfgs_are_pinned` in
# crates/thegn-core/src/platform_ratchet_tests.rs; `just ratchet-update`.

activity.rs
bundle.rs
config_write.rs
fsperm.rs
merge_guard.rs
profile.rs
ssh_creds.rs
startup.rs
util.rs
# config_repo.rs: two symlink-only tests (#[cfg(unix)] on std::os::unix::fs::symlink); no runtime cfg.
config_repo.rs
# toolchain_activation.rs: one symlink-only test (#[cfg(unix)]); detection is
# runtime-platform-neutral and rejects symlinks through file metadata.
toolchain_activation.rs
