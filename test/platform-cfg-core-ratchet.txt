# Files in crates/thegn-core/src (outside termcaps.rs / sandbox*) that carry a
# platform #[cfg]. thegn-core is substrate-agnostic; per-OS behaviour should be
# data handed in by the host or a platform table. Debt to burn down.
#
# SHRINK-ONLY. Enforced by `platform_cfgs_are_pinned` in
# crates/thegn-core/src/platform_ratchet_tests.rs; `just ratchet-update`.

# TEST-ONLY cfg, not per-OS production code (THE-515): the overlay-identity
# regression creates a real symlinked second spelling of one checkout to prove
# both slugs resolve to the same repository. std::os::unix::fs::symlink has no
# portable equivalent, and the production path is platform-free
# (util::file_identity owns the per-OS split).
account.rs
activity.rs
bundle.rs
config_write.rs
fsperm.rs
merge_guard.rs
profile.rs
ssh_creds.rs
startup.rs
util.rs
# config_repo.rs: two symlink-only tests (#[cfg(unix)] on std::os::unix::fs::symlink); no runtime cfg.
config_repo.rs
# toolchain_activation.rs: one symlink-only test (#[cfg(unix)]); detection is
# runtime-platform-neutral and rejects symlinks through file metadata.
toolchain_activation.rs
# TEST-ONLY cfg: migration-policy alias regressions create real symlink and
# hardlink aliases around the SQLite state file. The production migration
# authority and compatibility paths remain platform-neutral; hiding these
# fixtures behind production substrate code would weaken the path-identity
# assertion they exist to make.
db_tests.rs
# TEST-ONLY cfg (THE-516): symlink-alias and raw non-UTF-8 path fixtures for
# exact Git worktree/repository identity need std::os::unix APIs; the
# production identity code routes platform bytes through util.rs.
repo.rs
worktree.rs
