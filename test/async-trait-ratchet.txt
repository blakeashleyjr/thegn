# Files carrying `#[allow(async_fn_in_trait)]` — a provider trait (or its impl)
# declared with `async fn`, which is not object-safe, so its router is a
# hand-written delegation enum instead of `Vec<Box<dyn T>>`.
#
# The provider-seams spec's rule: async seam methods return `BoxFuture`
# (like `ControlApi`). Each entry is retired when that seam converts.
#
# SHRINK-ONLY. Enforced by `test/ratchet.sh async-trait` in `just lint`;
# regenerate with `just ratchet-update`.

crates/thegn-media/src/lib.rs
crates/thegn-svc/src/calendar/mod.rs
crates/thegn-svc/src/ci.rs
crates/thegn-svc/src/issue/github.rs
crates/thegn-svc/src/issue/jira.rs
crates/thegn-svc/src/issue/kaneo.rs
crates/thegn-svc/src/issue/linear.rs
crates/thegn-svc/src/issue/mod.rs
crates/thegn-svc/src/provider.rs
crates/thegn-svc/src/ssh.rs
