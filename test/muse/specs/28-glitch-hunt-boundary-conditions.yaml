name: glitch_hunt_boundary_conditions
# Boundary conditions: all chrome at once at a small size, a long sidebar
# filter, a no-match palette query (the palette must say so, not render
# garbage), scrollback clamping, and the 2s refresh-timer race.
case_tmp_env: XDG_STATE_HOME
matrix:
  profiles: [xterm]
  sizes: ["100x30"]
spawn:
  - sh
  - -c
  - |
    # A fixture repo with pinned dates so its hashes, ages and branch names are
    # the same on every machine (the panel renders them). thegn runs in it.
    set -e
    repo="$XDG_STATE_HOME/repo"
    mkdir -p "$repo"; cd "$repo"
    git init -q -b main
    git config user.email e2e@example.invalid; git config user.name e2e
    export GIT_AUTHOR_DATE=2020-01-01T00:00:00Z GIT_COMMITTER_DATE=2020-01-01T00:00:00Z
    printf 'hello\n' > README.md
    git add -A; git commit -q -m init
    exec thegn
env:
  XDG_STATE_HOME: "{case_tmp}"
  THEGN_LOG: warn
  MUSE_READY: "1"
  TERM: xterm-256color
sync:
  quiet_window_ms: 1500
  max_settle_ms: 6000
steps:
  # Startup: the statusbar mode and the pane's (fixed, `$ `) prompt.
  - expect_visible: {text: "NORMAL", timeout_ms: 20000}
  - expect_visible: {regex: "│\\$ ", timeout_ms: 20000}
  - resize: "60x16"
  - sleep_ms: 800
  - key: {key: "n", mods: [alt]}
  - sleep_ms: 600
  - expect_visible: {text: "NORMAL", timeout_ms: 5000}
  - resize: "100x30"
  - sleep_ms: 800
  - key: {key: "space", mods: [ctrl]}
  - expect_visible: {text: "jump", timeout_ms: 5000}
  - write: "zzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzz-no-such-command"
  - sleep_ms: 600
  - expect_not_visible: {text: "Cycle theme"}
  - expect_visible: {text: "jump"}
  - key: {key: "escape"}
  - sleep_ms: 120
  - expect_not_visible: {text: "jump", timeout_ms: 5000}
  - write: "seq 1 500\n"
  - expect_visible: {regex: "│500 ", timeout_ms: 8000}
  - key: {key: "pageup", mods: [shift]}
  - key: {key: "pageup", mods: [shift]}
  - key: {key: "pagedown", mods: [shift]}
  - sleep_ms: 400
  # Refresh-timer race: sit across a 2s tick boundary with a modal open.
  - key: {key: "space", mods: [ctrl]}
  - sleep_ms: 2100
  - expect_visible: {text: "jump"}
  - key: {key: "escape"}
  - sleep_ms: 120
  - expect_visible: {text: "NORMAL", timeout_ms: 5000}
  - check_file:
      path: "{case_tmp}/thegn/logs/thegn.log"
      reject_re: "ERROR|thread '.*' panicked|WARN.*OOM|WARN.*corrupt|subtract with overflow|index out of bounds"
