name: input_modes
# Input modes via the statusbar indicator: key-lock (⌁ LOCKED), Emacs,
# Vim-normal, back to Normal. While locked, a host chord must reach the pane
# instead of the host.
case_tmp_env: XDG_STATE_HOME
matrix:
  profiles: [xterm]
  sizes: ["100x30"]
spawn:
  - sh
  - -c
  - |
    # A fixture repo with pinned dates so its hashes, ages and branch names are
    # the same on every machine (the panel renders them). thegn runs in it.
    set -e
    repo="$XDG_STATE_HOME/repo"
    mkdir -p "$repo"; cd "$repo"
    git init -q -b main
    git config user.email e2e@example.invalid; git config user.name e2e
    export GIT_AUTHOR_DATE=2020-01-01T00:00:00Z GIT_COMMITTER_DATE=2020-01-01T00:00:00Z
    printf 'hello\n' > README.md
    git add -A; git commit -q -m init
    exec thegn
env:
  XDG_STATE_HOME: "{case_tmp}"
  THEGN_LOG: warn
  MUSE_READY: "1"
  TERM: xterm-256color
sync:
  quiet_window_ms: 2000
  max_settle_ms: 10000
steps:
  # Startup: the statusbar mode and the pane's (fixed, `$ `) prompt.
  - expect_visible: {text: "NORMAL", timeout_ms: 20000}
  - expect_visible: {regex: "│\\$ ", timeout_ms: 20000}
  - key: {key: "g", mods: [ctrl]}
  - sleep_ms: 30
  - expect_visible: {text: "LOCKED", timeout_ms: 5000}
  # Locked: Ctrl-Alt-s is passed through, so the sidebar stays.
  - key: {key: "s", mods: [ctrl, alt]}
  - sleep_ms: 30
  - sleep_ms: 400
  - expect_visible: {text: "WORKSPACES"}
  - key: {key: "g", mods: [ctrl]}
  - sleep_ms: 30
  - expect_not_visible: {text: "LOCKED", timeout_ms: 5000}
  - key: {key: "e", mods: [ctrl, alt]}
  - sleep_ms: 30
  - expect_visible: {text: "EMACS", timeout_ms: 5000}
  - key: {key: "v", mods: [ctrl, alt]}
  - sleep_ms: 30
  - expect_visible: {text: "VIM NORMAL", timeout_ms: 5000}
  - key: {key: "n", mods: [ctrl, alt]}
  - sleep_ms: 30
  - expect_not_visible: {text: "VIM NORMAL", timeout_ms: 5000}
  - expect_visible: {text: "NORMAL"}
  - check_file:
      path: "{case_tmp}/thegn/logs/thegn.log"
      reject_re: "ERROR|thread '.*' panicked|WARN.*OOM|WARN.*corrupt|subtract with overflow|index out of bounds"
