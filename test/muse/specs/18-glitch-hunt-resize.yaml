name: glitch_hunt_resize
# Resize storm through every breakpoint, down to 40x10 and back: no panic, and
# the final layout at 100x30 matches the recorded baseline (so a resize path
# that leaves a stale region behind is caught, not just a crash).
case_tmp_env: XDG_STATE_HOME
matrix:
  profiles: [xterm]
  sizes: ["100x30"]
spawn:
  - sh
  - -c
  - |
    # A fixture repo with pinned dates so its hashes, ages and branch names are
    # the same on every machine (the panel renders them). thegn runs in it.
    set -e
    repo="$XDG_STATE_HOME/repo"
    mkdir -p "$repo"; cd "$repo"
    git init -q -b main
    git config user.email e2e@example.invalid; git config user.name e2e
    export GIT_AUTHOR_DATE=2020-01-01T00:00:00Z GIT_COMMITTER_DATE=2020-01-01T00:00:00Z
    printf 'hello\n' > README.md
    git add -A; git commit -q -m init
    exec thegn
env:
  XDG_STATE_HOME: "{case_tmp}"
  THEGN_LOG: warn
  MUSE_READY: "1"
  TERM: xterm-256color
sync:
  quiet_window_ms: 1500
  max_settle_ms: 6000
snapshot_defaults:
  kind: text
  masks:
    # The statusbar after the mode word: transient status messages have a
    # TTL (and push the keyhints around), so only the mode is compared.
    - {content: "(?m)^  (NORMAL|EMACS|VIM NORMAL) +\\?.*$"}
  normalize:
    # relative ages in the git panel ("1s", "6y") drift with the wall clock
    - {re: "\\b[0-9]+(s|m|h|d|w|mo|y)\\b", replace: "<age>"}
steps:
  # Startup: the statusbar mode and the pane's (fixed, `$ `) prompt.
  - expect_visible: {text: "NORMAL", timeout_ms: 20000}
  - expect_visible: {regex: "│\\$ ", timeout_ms: 20000}
  - expect_visible: {text: "working tree clean", timeout_ms: 15000}
  - resize: "200x50"
  - resize: "160x40"
  - resize: "120x36"
  - resize: "100x30"
  - resize: "80x24"
  - resize: "60x20"
  - resize: "40x12"
  - resize: "40x10"
  - resize: "80x24"
  - resize: "200x50"
  - resize: "40x10"
  - resize: "100x30"
  - sleep_ms: 1500
  - expect_visible: {text: "NORMAL", timeout_ms: 8000}
  # The 40-column steps hide the panel; whether widening reveals it again is
  # a separate (load-sensitive) question — tasks.md 748. Pin the panel state
  # explicitly so the snapshot asks only "are there stale cells?".
  - key: {key: "right", mods: [ctrl]}
  - sleep_ms: 30
  - expect_visible: {text: "2 commits", timeout_ms: 8000}
  - key: {key: "left", mods: [ctrl]}
  - sleep_ms: 30
  - expect_visible: {text: "Ctrl g lock", timeout_ms: 8000}
  - expect_visible: {regex: "│\\$ "}
  - snapshot: {name: after_storm}
  - check_file:
      path: "{case_tmp}/thegn/logs/thegn.log"
      reject_re: "ERROR|thread '.*' panicked|WARN.*OOM|WARN.*corrupt|subtract with overflow|index out of bounds"
