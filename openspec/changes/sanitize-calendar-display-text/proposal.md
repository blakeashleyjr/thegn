# Sanitize calendar display values

THE-471 identifies provider titles/calendar labels and configured world-clock names that can move the terminal cursor outside popup clipping. Add one bounded display policy at every reachable calendar string projection while preserving raw semantic/provider values.

Impact: calendar roadmap and calendar capability. The integrating reviewer owns reciprocal delivery registration. Structural ICS correctness and provider negotiation remain separate issues.
