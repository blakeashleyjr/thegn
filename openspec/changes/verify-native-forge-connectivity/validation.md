# THE-622 current source checkpoint

The typed SDK repair is already on local main. Its existing tests inspected the
private native circuit; the residual test-only follow-up asserts the actual
process-global connectivity holder and exact real-Ladder counts. One owned
self-reexec isolates those globals even under ordinary parallel `cargo test`.
The helper checks eleven in-memory SDK cases, with explicit Auto/threshold-one
and preexisting Offline/Online premises. It never calls a provider, credential
helper, shell or real network endpoint.

THE-611's exact-child custody is extracted to shared test-only source; no Cargo
feature, production networking function, global reset API or existing custody
behavior changes. The parent bounds execution/output, retains custody on failed
observation/cleanup and requires strict normal temporary-directory removal.
The helper is ignored during ordinary discovery and refuses a missing marker;
its receipt is emitted only after all assertions. It is not an independently
counted second acceptance test.

Source review and native/counterfactual/strict-lint gates remain pending. Root
owns execution and reviewed landing; no Done claim follows from this checkpoint.
The separate active change is owned only by THE-622; the earlier delivered
diagnostics/forge change and its historical validation remain unchanged.

## Final evidence checkpoint — 2026-09-15

The historical pending statement above is superseded for the test and review
gates by the durable audit at
`docs/audits/THE-622-forge-connectivity-2026-09-15.md` and its raw evidence
manifest. Primary and independent source review, the parallel positive native
receipt (20 passed, 1 ignored), and the scoped counterfactual (build exit 0,
intended native exit 101 at the first reachable errors-only global-success
assertion) are recorded there. The counterfactual's later matrix and strict
normal-success `TempDir::close` path remain unreachable by design after that
first failure.

The focused maintenance06 proof is a separate 121/121 native result covering
the combined 603/602/622 scope and existing controls. The corrected maintenance06
gate receipt's 16 native passes are the corrected THE-603 Linux capture result,
not a THE-622 test count. The original maintenance06 Clippy failure with two
style checks is retained alongside the corrected Clippy pass. Source, native,
counterfactual, strict-lint, specification, and delivery review gates are
complete; only reviewed local-main landing remains pending.
