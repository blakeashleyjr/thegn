# Tasks

- [x] Core: `NamedCommand.model/env/permissions` (+ `harness` alias), `PipelineStage` overrides, `Harness::model_flag`, `pi` harness, `effective_agent`, validation.
- [x] Host: daemon door + TUI resolution via `effective_agent`; env overlay at spawn; `agent_permissions` seeding; `session open --stage`; per-request config source; `doctor`.
- [x] Docs: config.toml.example, CHANGELOG, pipeline skill.
- [ ] Pipeline board shows the stage's effective model (THE-74 lane owns the board).
