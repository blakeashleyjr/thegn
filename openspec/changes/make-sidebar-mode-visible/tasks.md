# Tasks

- [ ] 1. Add flat/grouped mode to the loop-owned render model.
- [ ] 2. Render `FLAT` persistently in the full sidebar beside sort/hold state;
     grouped mode and the slim rail omit the mode word.
- [ ] 3. Reuse the existing `g` toggle/persistence path and do not introduce a
     second state owner or false mouse hit target.
- [ ] 4. Add flat on/off, constrained-width, filter-header, rail, supported-theme,
     same-frame toggle, and persisted-reload tests.
- [ ] 5. Keep sidebar help/contextual hints explicit about `g` and run focused
     rendering/OpenSpec gates.
