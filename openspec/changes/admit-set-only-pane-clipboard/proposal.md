# Admit bounded set-only pane clipboard commands

THE-343 identifies OSC 52 query passthrough across pane/remote/sandbox trust boundaries. Parse complete bounded set syntax before writer admission, preserve split input and scoped retry, and clear unadmitted state at session barriers.

Impact: roadmap A.2 terminal integration and terminal-compat. The integrating reviewer owns reciprocal delivery registration. Physical-write delivery uncertainty (THE-357), general OOB bounds and graphics transactions remain separate.
