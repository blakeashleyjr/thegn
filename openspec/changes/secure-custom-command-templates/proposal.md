# Secure custom Git command templates

THE-377 repairs implicit repository-text injection into the existing custom command shell path. Add explicit migration admission, inert argument templates, typed compiled programs and conservative shell contexts. Related roadmap: existing Git custom commands; maintenance batch THE-629.

Impact: core custom_cmd/config validation, host command dispatch, service execution, configuration example. Root registers delivery metadata.
