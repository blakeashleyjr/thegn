extends: default
ignore: |
  apps/switchboard/**
  target/**
rules:
  line-length:
    max: 120
    level: warning
  document-start: disable
  truthy:
    check-keys: false
  comments:
    min-spaces-from-content: 1
