# THE-484 independent review

Initial checkpoint: `d81cc770` in `/tmp/thegn-audit-time-policy-20260913`. Review is independent source inspection; author and integration test results remain separate evidence. No provider network request or destructive action was performed by this reviewer.

The duration conversion, checked relative deadlines, fractional-delay rounding, epoch subtraction, TTL rollback behavior, and explicit zero-policy handling inspected are consistent with the intended repair. Extreme unsigned durations no longer wrap negative in the reviewed budget, breaker, lease, cache, backoff and provider-delay paths. Changes preserve configuration deserialization and do not replace the whole configuration on a policy error.

The following admission blockers were sent to the author and primary reviewer:

| Finding                                                                                                                                                                                                                                                                     | Required revision                                                                                                                                                                                       |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| VPS deduplicates by provider kind and API base without credential identity, then applies the first environment's lifetime to every listed resource. Fly explicitly uses the first configured environment. Neither ledger proves which environment policy owns the resource. | Quarantine ambiguous policy/account choices before provider operations and ledger cleanup unless exact ownership resolves them. Include conflicting lifetimes 60/0 and different credential references. |
| A VPS ledger row with an empty persisted instance ID passes the new identity check; later destruction resolves an ID independently by name.                                                                                                                                 | Require a nonempty matching persisted ID for age-derived destruction of a recorded resource. Retaining staged intent is acceptable; do not claim complete resource-generation fencing.                  |
| Primary reviewer found Fly custody read returns `Result<Option<Record>>`, and the new admission discarded the `Option`, accepting `Ok(None)`.                                                                                                                               | Require an actual custody record or equivalent proof before inventory access and destructive admission. Test missing custody with zero network/action calls.                                            |

Provider time parser fixtures correctly reject absent, malformed, nonpositive and future creation dates through the shared decision policy. Fly inventory reconciliation requires one machine, matching persisted ID, machine name and ownership metadata. Inventory-read/delete races and the broader provider generation-authority contract remain explicitly outstanding.

The subsequent source revision addresses these blockers. `reaper_policy::with_unambiguous` encloses the entire reconciliation callback, and both Fly and VPS call it before provider construction or ledger retirement. It refuses differing endpoint, credential reference or lifetime configuration; exact duplicates reconcile once. Recorded VPS resources require a nonempty matching instance ID. Fly custody requires an actual record matching provider, account, instance, key path and fingerprint before invoking the injected inventory reader. Fixtures include conflicting lifetimes 60/0, differing credentials/endpoints, all three zero-action counters, exact duplicates, each custody mismatch, and retry after transient inventory failure.

Status: independent source review approves revision `2dbe30aa` atop `d81cc770`, including the simplified singleton VPS reconciliation and same-kind ledger filter. The author reports 118 plus 26 focused core tests passing; service, proxy, media and combined host evidence remain pending. The initial `d81cc770` checkpoint alone is not approved for landing.
