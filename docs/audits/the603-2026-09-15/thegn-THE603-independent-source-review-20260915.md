THE603 independent adversarial source review — approved with scope limits

Source: 3c26e9879b5da8451afccf4abca17f9a18c5b11e in /tmp/thegn-maintenance-04-state-capture-20260915-v51v547m/source, base bb97ec24e6d679400b4e97615e4e27d2c4a97039. Read the implementation plan, dated audit, all new production/fixture modules and registration/delivery changes. Independently verified clean HEAD, the exact sixteen-file inventory and every checkpoint hash, eight protected files byte-identical to base, and four source-gate log hashes. No Cargo, native test, live database or candidate mutation was performed.

No scoped source blocker found. Missing observations retain owned ancestor descriptors, exact first missing name and final-leaf classification. Both Present and Missing traverse the same final orchestration rendezvous. The absence path rechecks ancestor identities/types/ownership/modes, inspects all three adjacent orphan names with O_PATH, then repeats the exact missing-component lookup. Any existing orphan object refuses without SQL; only an independently observed missing lookup permits Absent. Missing intermediate-directory creation also refuses. Present observations retain pre/post-read checks and reject lost/replaced prior sidecars; newly appearing sidecars must pass the same metadata policy. SQL data is discarded if post-read verification fails.

The core helper uses the explicit Unix VFS, read-only/no-follow/no-mutex/private-cache flags, read-only verification, a 250ms lock timeout and the existing THE602 reader. No new schema decoder, SQL mutation, migration, policy installation or general connection escape is introduced. Existing strict snapshot/definition/composition and legacy DB files are byte-identical. Errors carry static categories and the existing snapshot Debug remains redacted. Namespace storage/work is bounded by the path/component limits; existing reader bounds remain in force. These do not bound total filesystem/SQLite execution time. Early open/setup errors drop the owned Connection normally; the completed read path explicitly closes before releasing its copied result.

Fixture evidence is meaningful by source. The creation and ancestor-change tests execute actual observation/finish_capture with a read counter and require the rendezvous plus zero SQL. They then establish a fresh positive Present or Absent result. Each orphan suffix is tested before and after initial observation, with byte/inventory retention; FIFO and symlink orphans are inspected without opening them for data I/O. The post-read replacement test obtains actual strict SQL data before changing the path and verifies the data cannot escape. Permission denial uses ordinary File descriptors acquired before chmod, restores through those descriptors before checking results, and proves a fresh successful read plus unchanged bytes. The guard handles assertion unwind; explicit restoration failures are visible, and a restoration failure during an existing unwind is reported without double-panic. The permission test requires a non-root runner rather than silently passing.

The five core fixtures cover live-writer uncheckpointed WAL data, base/WAL/sentinel/schema preservation, malformed/corrupt/missing state, schema incompatibility and actual exclusive-lock Busy recovery. SHM bookkeeping is explicitly permitted. The sixteen Linux fixtures require a real supported local filesystem and include a separate production refusal under shared /tmp. The trusted-root test seam does not demonstrate successful production traversal from literal /. The unsupported-VFS branch asserts refusal and cannot count as positive Unix capture; the non-Linux host fixture similarly covers only typed Unsupported. Root must retain these platform/premise distinctions in native receipts.

The additive helper remains unwired, with narrowly documented dead-code annotations on its two module registrations. It grants copied data, not launch authority. Retained O_PATH descriptors do not pin the inode consumed by SQLite’s later lexical open; transient/ABA/same-UID/root/mount-admin replacement and a raced special file remain outside the stated stable-namespace guarantee. It is not an atomic absence lease or a total I/O/cancellation guarantee. THE592/THE607 startup Hold, authority and worker integration remain open. Other delivery entries and owners are preserved; active THE603 tasks correctly leave current native/lint/landing unchecked.

Recommended root gate: exact host_db_capture::tests:: (five), platform::state_db_capture::linux::tests:: (sixteen on supported unprivileged Linux), existing THE602 snapshot/definition/checked-composition compatibility, affected strict lint/source/spec/delivery checks, then scoped local landing. Keep finite outer test-runner deadlines. Separately approved old missing-return/orphan-bypass counterfactuals should fail the owned semantic assertions, without allowing a mutant to enter SQLite on a FIFO. No native, counterfactual, platform-positive or Done claim follows from this source approval.
