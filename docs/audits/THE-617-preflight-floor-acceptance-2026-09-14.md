# THE-617 final floor after exec-preflight failure

The final host admission repair is already on main. Earlier actual preparation tests covered automatic resolution with no candidate, explicit host selection, deliberate degradation and bare SSH. The dated session review left one specific regression pending: a stronger candidate passes its initial floor check and ensure, then fails exec preflight.

The new Linux fixture calls actual `agent::prepare_sandbox_env` with a private executable named `podman`. It accepts only four complete argv shapes: `version`, existence of one fixed inert image, inspection of the exact fixture-derived container name with the production format, and exec of that same name with `/bin/sh -lc true`. It never executes the inner command. Image/inspection responses establish a controlled existing running container with no mounts, matching `FileAccess::None`; ensure succeeds without create/pull/remove. Exec returns 37 with a unique diagnostic.

Every case requires the exact journal `version`, `image-present`, `ensure-running`, `preflight-failed`. A candidate rejected earlier, skipped preflight, unexpected command, or accidental runtime creation cannot satisfy that assertion. Automatic fail policy must reject the final host-process floor; degrade must report both preflight failure and missed floor; off permits host fallback while retaining the preflight diagnostic. An explicit runtime retains its preflight failure without host fallback.

All filesystem paths are owned by a TempDir. PATH contains only the inert adapter; config, state, cache, runtime and shell environment files are private. HOME is unchanged. The existing environment guard protects changes; probe-cache RAII clears before and after while that guard remains held, including unwind. Explicit TempDir close makes normal cleanup errors visible. The fixture prepares launch data only: no OCI process, provider, mount or interactive pane is started.

Primary and independent source review approved the command allowlist, meaningful journal, policy matrix and owned cleanup. The final fixture bytes are `6d341918f5677c598058a2e020aaef3acdf8a9b6759b6925005fd42af1ddaf7e`; the floor implementation, sandbox helpers and preparation function at the relevant seam match the tested source, with the only `prepare_sandbox_env` difference being the separate THE-639 devcontainer branch. This fixture sets `DevcontainerMode::Off` and passes no devcontainer input, so that branch is unreachable in this test. The combined05 full receipt for source `33c0db71` reports 8465/8465 passed with 26 skips and includes this exact selector; its log and receipt remain the sole native evidence for the floor cases. Source comparison, native evidence and independent review are complete for this extracted test; standalone landing completed on local main at `046483b6` on September 15. The earlier 8354/8354 pass and commit-time pending wording are retained as historical context. This proves preparation control flow, not real container isolation, native macOS/Windows behavior or new runtime support.

The standalone candidate passed source ratchets, strict validation of the owning OpenSpec change and formatting on September 15. Durable raw reviews, the explicit source bridge, the combined05 receipt, exact passing log excerpt and scoped gate logs are indexed in [the evidence manifest](the617-2026-09-15/manifest.json). The combined05 full log remains hash-pinned externally; no new full-suite execution is claimed for this extraction.
