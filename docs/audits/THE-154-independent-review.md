# THE-154 independent lifecycle review

Review is in progress against the implementation lane at `/tmp/thegn-audit-maintenance-protocols-20260913`. The files inspected so far are drafts, not a stable landing candidate. Native Windows execution and full descendant containment are outstanding evidence; neither is implied by a direct child exit or successful cancellation request.

The accepted scope permits a resident I/O and ownership repair with truthful leader/pipe completion and an explicit unproven tree guarantee. It does not require adding a new cgroup or namespace feature, disabling Windows, or marking the complete THE-154 issue resolved without containment evidence.

| Finding                                                                                                                                             | Required revision or evidence                                                                                                                                                                                          | Status                                        |
| --------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- |
| Windows startup failure can detach previously created idle workers: `Worker::Drop` tests `is_finished` immediately after disconnecting the channel. | Distinguish pre-child idle workers and join them off the compositor, or transfer them to tracked custody. Inject second/third worker admission and process-spawn failures.                                             | Author acknowledged; revision pending review. |
| Unix `waitid` errors can leave `Some(Child)` available for a later group signal even after `ECHILD` indicates lost custody.                         | Latch identity loss, prohibit subsequent PID/group signaling, and retain an explicit unresolved outcome. Inject lost-child observation and simultaneous exit/kill cases.                                               | Author acknowledged; revision pending review. |
| Keeping `Process` outside only the running loop's panic catcher does not survive cleanup panic or cancellation of the entire Tokio task.            | Construct a custody guard before handing the future to `runtime.spawn`; dropping an unpolled, cancelled or unwinding owner must transfer the process into retained registry custody.                                   | Sent to author.                               |
| Outcome publication and `JoinHandle::is_finished` are used without awaiting owner handles.                                                          | A settled shutdown receipt must also observe owner task completion and join errors under the same application deadline; finished handles must not be silently discarded.                                               | Sent to author.                               |
| Natural process exit can win the biased select before readable final stdout; cleanup then closes pending RPCs before delivering those bytes.        | Preserve a complete buffered final reply before natural-close notification, while requested cancellation remains immediate and no callback follows the terminal callback. Add a deterministic reply-then-exit fixture. | Sent to author.                               |
| Cleanup awaits leader exit before starting pipe settlement despite its concurrency comment.                                                         | Start pipe cancellation before or concurrently with leader waiting, against the same absolute deadline. Test a held leader with pipes that can settle.                                                                 | Sent to author.                               |

The Windows draft correctly treats `ERROR_NOT_FOUND` from `CancelSynchronousIo` as inconclusive and retries against the retained dedicated thread. Microsoft documents both the race between synchronous operations and the need to observe completion after requesting cancellation. A deterministic barrier immediately before the syscall is needed to test this path, and native Windows results remain distinct from source review. Sources: [Microsoft cancellation guidance](https://learn.microsoft.com/en-us/windows/win32/fileio/canceling-pending-i-o-operations), [CancelSynchronousIo contract](https://learn.microsoft.com/en-us/windows/win32/api/ioapiset/nf-ioapiset-cancelsynchronousio).

The later draft adds the idle-worker distinction, pre-spawn custody guard, Unix identity-loss latch, deferred bounded natural-response drain, explicit-close escalation, and pipe cancellation before leader wait. Those revisions address the first round by source inspection; final fixtures remain pending.

| Additional finding                                                                                                                                                      | Required revision or evidence                                                                                                               | Status                                                                         |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| Taking task handles into shutdown waiter futures detaches them when the waiter is cancelled and races concurrent shutdown calls.                                        | Keep task handles in registry-owned join state; preserve join errors and test cancelled/concurrent shutdown waiters.                        | Revised to an async mutex holding the handle in place; source review positive. |
| Natural-close hooks remained deferred until all pipe/leader cleanup finished; escalation to explicit shutdown did not drain them immediately.                           | Finish deferred hooks after the short stdout drain, and immediately when explicit cancellation supersedes natural close.                    | Revised; source review positive.                                               |
| An old provider response not resolved locally fell through to host lookup by plugin name, allowing an old response to resolve a replacement bridge's reused request ID. | Remove current-by-name fallback or carry exact generation identity; test replacement bridges with the same request ID.                      | Host fallback removed; source review positive.                                 |
| Spawn publishes an entry before registering its owner handle, then uses `try_lock().expect` against the same task mutex shutdown can hold.                              | Deterministic registration synchronization and a reservation/shutdown barrier fixture; ordinary shutdown must not cause a post-spawn panic. | Sent to author; pending.                                                       |
| `Child::wait` can fail with `ECHILD` after a successful non-consuming observation, leaving the identity-loss latch false.                                               | Latch loss on consuming wait failure before any Held retry; verify retry cannot signal the old PID.                                         | Sent to author; pending.                                                       |

Primary review additionally found final supervisor release only logs a retained Held process, although dropping Unix `Child` does not terminate it. The requested revision is a bounded final termination attempt through the exact still-owned process object, with no post-reap PID fallback and with unresolved reporting preserved. An aborted-before-first-poll owner followed by final supervisor release needs a fixture. This revision remains pending.

The latest draft resolves these remaining source paths: registration owns its task-state mutex before publishing the entry; consuming wait errors latch identity loss; platform `Process::Drop` attempts bounded final termination/cancellation through the retained object, including a custody guard released after its supervisor. Deterministic fixtures now hold owner joins while cancelling a shutdown waiter, compare four held joins against one deadline, and pause spawn between child creation and owner registration. The join fixture's notification release needs a retained signal or readiness acknowledgement to avoid scheduler-dependent loss. Native Windows has a pre-syscall cancellation barrier fixture, but it has not run here. Source review is positive pending a stable checkpoint and final tests.

No production plugin was launched or signaled during this review. No shared Cargo build was started by the reviewer. The next review checkpoint must cover the completed owner, platform seam, provider pending-table closure, host shutdown coordinator and fixture results together.
