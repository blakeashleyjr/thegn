# THE-639: bounded devcontainer startup and retained custody

The repair reserves bounded startup capacity before provider preparation and keeps the exact child, stderr reader, snapshot and session resources owned through timeout, cancellation, observation failure and publication unwind. Startup continues to discard stdout, matching the existing provider contract; stderr is drained concurrently with a 2 MiB bound. The caller has one deadline and reports held/unknown after uncertain creation, preventing automatic OCI fallback or a duplicate startup within that controller's lifetime. No resource is deleted by a mutable name.

The standalone Rust source at `8a83b1ec` is extracted onto local main `3f365bfe`. All four changed Rust files, including the entire agent and provider files, match actual tested source `33c0db71` exactly. The existing full05 run passed all 8,465 tests with 26 skips, including the 23 exact startup and migrated provider selectors. Existing strict workspace/all-target Clippy passed for that source. No new build or native run is claimed for this extraction.

Three isolated counterfactuals each failed their intended assertion. Premature spawn claim produced one spawn instead of zero after fixture settlement. Moving displaced-session custody into a local value lost the retained cell on caught publication unwind; settlement and the HELD/poison assertions completed first. Gating the stderr reader until the consuming child-exit hook let the zero-stderr case succeed, then caused the 2 MiB case to reach its three-second deadline; settlement completed before the held outcome failed the original success assertion. These are nonshipping mutations. Their receipts identify the later assertions that were not reached. The raw packaging review's phrase "injects child exit" is corrected by this description and the original mutant3 review.

The [evidence manifest](the639-2026-09-15/manifest.json) retains exact source hashes, positive selectors, original native and lint receipts, all three counterfactual patches and build provenance, and independent reviews. Original review Markdown is stored with a `.md.log` suffix to preserve its bytes. Root verified current source/specification/formatting gates and final independent standalone review. The metadata-only findings are resolved in the manifest; original reports remain unchanged. All four source hashes and all 23 positive selector lines were rechecked without compilation.

The guarantee is scoped to the owning controller and retained operation. It does not establish process-tree settlement, escaped-descendant cleanup, cross-controller/restart durability, a durable resource ledger, or automatic reconciliation/deletion. Linux fixtures do not establish Windows or Darwin runtime behavior. No real container, live database mutation, live restart or authenticated provider operation was required. A future PR still requires the repository pre-PR `just ci` gate; no full CI result is claimed.
