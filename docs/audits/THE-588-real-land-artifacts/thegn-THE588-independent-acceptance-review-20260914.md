# THE588 independent adversarial acceptance review — 2026-09-14

**HOLD closure for one concrete literal acceptance gap:** no retained fixture yet connects a real successful target land to configured physical cleanup. No new production defect was found in the reviewed conservative cleanup scope. The existing source/tests materially repair the original destructive scope and false collected-report defect, but separately passing fold and seeded-cleanup tests do not satisfy that final positive requirement.

Fresh Linear THE588 remains In Progress and explicitly requires private shared-repository/identity/dirtiness/error/concurrent/symlink cases plus configured cleanup after a real successful land. THE593/594/600 are its tracked dependencies; root has closed their reviewed narrower scopes. THE596 branch-ref retention remains explicit. This is a read-only review: no cleanup, Cargo, source/tracker write or live Git operation was performed.

## Independently verified source and receipts

`/tmp/thegn-THE588-independent-acceptance-evidence-20260914.json` lists **32 exact host test names** individually present in BOTH retained successful executions, ten exact source-file hashes, and canonical ancestry checks. These are32 registered tests, not subcase/helper counts; the list is relevant coverage, not an assertion every test independently exercises the whole contract.

- `/tmp/thegn-batch03-final-native-20260914.receipt.json`, SHA256 `b73ad1031d8e44b14fedba6c709e7c34cbb92ba10d3c8da06541045d292df809`: source41ae7c04e52a1ca159bdf08ea1b6967a24cc9fa8,8405/8405. Independently rehashed the actual log and matched `3bfc1ae5f5b7f6ff31c8b242afc7c106f4fba1f248d21638641704ee3e652f14`.
- `/tmp/thegn-rolling-full-workspace3-results-20260914.json`, SHA256 `94595fda1fb547dd8e81e6163b7d35f9c072c60ef956b3fce3e357831e50059a`: recorded source5b698dc4,8354/8354. Actual log hash independently matched `7166604b1d90122adf27a546457e289f17efec27cc82f7d80d40ba8d8ed78cfb`.
- Historical focused `/tmp/thegn-rolling-cleanup-20260914-results.json` reports50/50; file SHA256 `0102546ad9a7c5aa5a57144e83d6005ef98b18e841359bcda39f09f603df8a93`. The later exact-name/full-log verification above is the stronger receipt linkage used here.

Ten reviewed cleanup/integrate/canonical-history/transaction files are byte-identical between canonical7014a496 and41ae7c04. Canonical ancestry independently confirms ea807922,407e7d77,db2dbb55 and7ad070c6. Do not relabel41ae execution as a test of a future new fixture or production revision.

## Repaired boundaries and literal coverage

`merge_sweep` filters landed target rows and excludes proven foreign common-Git identities before applying the requesting repo's clock/report. Unknown paths remain conservative candidates but fail final admission; queue strings alone never authorize physical removal. Only CleanupOutcome::Removed appends collected; physical absence and Git unregistration must both verify. Bookkeeping failures remain distinct; held rows survive retry/cache pruning and are not repeatedly reported as newly collected.

`remove_landed_with_config` requires the exact selected landed row and landed OID, takes lexical/cooperative physical destroy claims, and verifies canonical linked-worktree/root/common/metadata identity, direct source/target refs, current branch tip and recorded land ancestry. It pins same-file handles and revalidates queue/cache/resource state before lifecycle stages and removal. Main/foreign/alias/redirected/unregistered/mismatched and changed identities fail closed. Dirty status uses ignored/all-untracked NUL output; config/index/status failures, hidden index flags, submodules and configured executable filters refuse rather than read as clean. Removal is exactly Git worktree remove without force and no recursive fallback. Actual runtime teardown remains guarded against re-resolution of remote/unowned resources.

Literal fixture linkage includes:

- Shared two-repository DB, forged main root and actual reporting: shared_database_collects_only_verified_repository_and_reports_actual_removal; dirty_conflicted_claim_and_wrong_target_rows_keep_retry_metadata.
- Identity/probe matrix: rejects_foreign_main_target_mismatched_and_unregistered_paths; branch_advanced_after_landing_or_during_cleanup_is_preserved; final_validation_preserves_new_ignored_files_and_replaced_directory; dirty_ignored_untracked_and_unknown_status_never_mean_clean; hidden_index_modifications_and_filters_refuse_without_running_driver.
- Removal refusal/locks/aliases: locked_worktree_busy_repository_and_symlink_alias_are_refused; symlinked_mutation_lock_and_revoked_final_guard_preserve_files; landed_remove_does_not_overlap_an_existing_destroy; landed_remove_keeps_git_unregistered_directory. These exercise concrete refusal modes, not every possible OS failure. Native Linux runs take the supported symlink branch; do not infer native Windows.
- Actual physical removal and retained refs: actual_no_force_removal_preserves_source_and_target_refs; landed_remove_keeps_branch_and_explicit_queue_hold; landed_detach_removes_worktree_but_keeps_branch; held_cleanup_survives_refresh_without_repeating_missing_worktree_errors_or_touching_refs.
- Exact eligibility/resource revalidation: selected_landed_row_cannot_authorize_requeued_or_revoked_cleanup; refinalized_result_oid_revokes_selected_cleanup_before_hooks; malformed/unknown resource metadata and both post-admission mutations; active_projection_and_sync_refuse_before_hooks_or_explicit_runtime_teardown.

CLI integrate no longer launches historical expiry cleanup after either successful or failed folds; the source explicitly describes cleanup as a separate sweep action. Existing failed_union_cannot_persist_a_land_or_run_landed_lifecycle arms Remove with actual failed fold/persistence and proves preservation. The32-name JSON contains exact fully qualified selectors rather than relying on prose abbreviations.

## Missing positive is real, not a historical environment failure

`merge_lifecycle::tests::record_private_land` only reads existing main HEAD and seeds a landed DB row. Its linked branch is initially at that same base. Likewise merge_sweep::fixture and cleanup::Fixture create a branch at base, not a new actual land. Their successful real Git removals are valuable guards but cannot prove ordering from an actual CAS/land.

`integrate::tests::green_gate_advances_red_gate_holds_back` runs actual folds, but does not call final persistence/configured cleanup. Existing real-Git merge_driver fixtures explicitly disable organize_folders. The106-command retained integrate fixture has no configured cleanup/sweep phase. A source comment mentioning an older smoke drain is not an execution receipt. Root independently found no stronger retained positive. Therefore THE588 should remain open until the missing focused actual path passes and is landed.

## Corrected minimal positive-fixture plan, awaiting primary greenlight

Do not nest Repo::new and TestIsolation::new: both retain the same non-reentrant core EnvGuard mutex. Do not construct two Repo fixtures under it. Use **one existing TestIsolation** and manual private Git setup only, beside the actual integrate/persistence failure fixtures.

1. Under that single guard create a canonical owned TempDir with two Git repositories and linked worktrees. Use isolation.git for fixed setup, explicit main branches, local identity/signing-off, and owned hooks/template/config. Extend only TestIsolation's existing environment list with its private GIT_CONFIG_GLOBAL/NOSYSTEM/count/parameters/template settings if run_fold's non-test-specialized commands need those settings. No second guard, ambient HOME change or production Git wrapper change. Existing sandbox=false/none and TLS Git context are needed because remove_landed reloads configuration; LocalResources' test search remains owned and cannot discover/execute ambient OCI.
2. Open a **fresh private Db::open under the guard**, including the implicit GitLoc registry requirement; use that DB for selected queued and foreign landed rows. Queue seeding is setup, never the alleged land. Register one real clean linked b1 branch with a commit distinct from main, and capture selected observation BEFORE folding. Preserve full foreign row, refs, registrations and file bytes.
3. Arm organize_folders=true, OnLanded::Remove, explicit LandStrategy::Merge (so source-tip ancestry remains provable), main target, signing off and a finite builtin green gate. No snapshot-dirty, agent/provider launch, gate toolchain, or broad sweep. Observe the main OID and candidate path/bytes before run_fold.
4. Invoke actual run_fold with that branch. Require Passed, advanced=true, one actual landed result, target OID changed to the report's full commit, original source tip is ancestor of target, and candidate checkout still exists immediately before persistence. This demonstrates actual Git CAS precedes any physical cleanup; no fabricated FoldReport or callback result.
5. Invoke actual persistence::persist with the pre-fold observations and unchanged report/config. This existing production path writes final landed state then calls apply_landed. Require checkout absent AND Git worktree list no longer registers it; target remains the verified landed commit and source branch remains its original tip. Require the exact landed queue result plus CleanupHold::BranchRetained, preserved queue metadata/attempts, and registry removal. THE596 is retained, not silently declared complete. Foreign refs, registrations, bytes and complete DB row remain unchanged.
6. Keep existing armed-Remove failed fold/persistence cases as the negative half and execute the new positive plus these exact selectors under root's bounded private native runner. Strictly close handles and owned TempDirs after use. No real CLI/live cleanup is necessary for this bounded missing acceptance slice. Another agent reviews the concrete test source before root compiles it.

No implementation of this proposal occurred in this review.

## Explicit limits that remain separate

The accepted design itself records that cooperative locks cannot exclude arbitrary external writers: an ignored file created AFTER the final check can race Git's own removal. Tests add ignored content before final revalidation, not inside that final OS race. THE370 owns stronger preservation/fault/native-platform criteria; this review never claims atomic filesystem isolation. THE237 owns authoritative stored repository-scoped selection rather than the present global enumeration with verified final identity filtering. THE593 does not solve identical-value ABA; THE596 keeps branch deletion withheld; Windows runtime proof is not inferred from Linux/cross-source evidence. These existing boundaries do not supply the missing configured real-land positive and must remain explicit in any later scoped closure.

## Actual-CAS positive fixture follow-up: source approved, execution pending

Reviewed root's two-file working candidate in /tmp/thegn-maintenance-04-cleanup-20260914. Exact new selector: `integrate::tests::configured_cleanup_runs_only_after_real_target_advance_and_committed_outcome`. This closes the missing source-level fixture design; the earlier acceptance HOLD remains until the actual compiled fixture passes and lands.

One TestIsolation owns the entire chain; no nested Repo/EnvGuard is constructed. It creates two distinct manual private repositories and linked worktrees, explicitly initializes main and local unsigned author configuration, and uses the existing TLS-aware Git command seam. The same fresh Db::open state is shared by implicit GitLoc registry reads and explicit queue persistence. Only the unrelated foreign row is seeded landed; selected candidate remains queued with nonzero attempts until real folding.

A distinct source commit, actual production fold engine with real update_ref_cas, passing finite built-in printf gate, changed main OID and source ancestry prove real land. Before persistence, selected checkout still exists and its entire queue row equals the captured original. The test calls real persist with pre-fold observations and actual report; source ordering commits final Landed before apply_landed. It then requires physical path absence and no worktree registration, unchanged target result and retained candidate branch ref, exact BranchRetained marker/result/attempts/queued_at, and unchanged full foreign row, refs, worktree registrations and payload. A refusal or seeded-only pseudo-land cannot pass these assertions. Existing failure fixtures remain the complementary negative coverage.

TestIsolation's test-only environment extension uses a single mixed set/unset guard, private Git config and no system config, clears inherited Git repository/index/object overrides, and retains/restores the existing TLS config slot. All environment changes restore on unwind. Its state owner outlives the private repository TempDir and explicit DB close; successful repository teardown calls strict TempDir::close. No production algorithm or cleanup policy changed. The new test is Unix-qualified, not Windows/native-matrix evidence. No compiled execution or Cargo by reviewer.
