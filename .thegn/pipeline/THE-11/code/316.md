# THE-11 row 316 — fold-gate fix complete

Verdict: PASS.

Implemented the two missing configuration-coverage updates without changing
runtime behavior:

- Documented `agents.drawer_scope` and `agents.drawer_cwd` in
  `config/config.toml.example`, including their unset defaults and scope-root
  behavior.
- Pinned both keys in `test/env-overlay-ratchet.txt` with the concrete reason:
  `agents` is a list, so an unindexed `THEGN_AGENTS_*` variable cannot select a
  named entry. This matches the existing treatment of the other per-agent
  fields and `tools.drawer_*`.
- Left `crates/thegn-core/src/config.rs` unchanged because adding global
  overlays for per-entry metadata would introduce ambiguous new behavior,
  contrary to the work order's no-behavior-change constraint.

Verification:

- `cargo nextest run -p thegn-core -E 'test(/every_shallow_key_has_an_env_knob|example_config_documents_every_section/)'`
  — 2 passed, 3,651 skipped.
- `just quick thegn-core` — passed.
- `THEGN_ALLOW_HEAVY=1 just test` — 7,126 passed, 22 skipped.
- `git diff --check` — passed.

Could not verify: nothing.

Next hint: retry the fold/land gate; the two failures named in `gate-fix.md`
are now covered and the full workspace test gate is green.
