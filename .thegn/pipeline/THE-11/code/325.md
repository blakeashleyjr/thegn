# Row 325 — merge main and re-gate

## Verdict

PASS. The THE-11 branch contains current `main` at
`8bf860280af023639e874f9606f67382928eae77`, and the required full gate is
green on that merge.

## Changes

- Merged `main` at `bb72f7ca3ce11860022477a9c5fe4909bb48fcb8` in
  `28d590ce7d5a016469776aef60d1252a95fd5379`.
- Resolved the conflict in `crates/thegn-host/src/platform/mod.rs` by retaining
  both merged semantics: THE-11's symlink-safe drawer-state helpers and main's
  FIFO test helpers.
- Main advanced during the gate, so merged its new tip
  `8bf860280af023639e874f9606f67382928eae77` in
  `1706f93facc535f0d4be853d64f9f8e3370c39f3` and ran the full gate again.
- Preserved the drawer feature and the row-316 `drawer_scope`/`drawer_cwd`
  example configuration and environment-overlay ratchet entries. No redesign
  or unrelated source change was made.

## Verification

- `env XDG_RUNTIME_DIR=/tmp RUSTC_WRAPPER= just quick thegn-host` — PASS.
- `env RUSTC_WRAPPER= THEGN_ALLOW_HEAVY=1 just test` against the final merge —
  PASS: 7,190 passed, 22 skipped.

The first sandboxed full-gate attempt exposed these failing test names before
the unrestricted rerun:

- `thegn-core dns_filter::tests::forward_query_round_trips_against_fake_resolver`
- `thegn-core dns_filter::tests::forward_query_times_out_when_no_resolver`

Both failed because the restricted sandbox denied UDP socket binding with
`PermissionDenied` / `Operation not permitted (os error 1)`. They passed in
the unrestricted full gate; no source fix was required. Before that attempt,
the default `sccache` wrapper also exited before compilation with
`sccache: error: Operation not permitted (os error 1)`, so the documented
`RUSTC_WRAPPER=` retry was used.

No merged-code test failure remained, and the stale base did not hide a
functional regression. Per the dev-loop policy and this work order, lint,
coverage, CI, and separate end-to-end suites were not run.
