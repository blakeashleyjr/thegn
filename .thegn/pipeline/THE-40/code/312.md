# Dispatch 312 completion — THE-40 code stage

Verdict: complete.

## Delivered

- Published the dated native GUI decision record. THE-40 builds no GUI;
  candidate 2, a separate observer-first GPU cell client of the daemon/control
  API, is the preferred future lane.
- Corrected the active draft's substrate assumptions: attach is cataloged and
  versioned, multiple observer/interactive subscribers exist, pairing and
  exact-origin CORS exist, the binary frame compatibility schema remains
  incomplete, and no server-side layout/chrome view model exists.
- Preserved all three candidate shapes and their trade-offs, the one-catalog,
  0%-idle, shell-independence, substrate, and security invariants, THE-34
  filter/lag coordination, and the THE-40-F1 observer cell-client follow-up.
- Synchronized and archived `define-gui-frontend-lane` under the dated archive
  path, with no active duplicate.
- Made no Rust, dependency, config, catalog, route, API schema, database,
  migration, roadmap, architecture source, test, or ratchet changes.

## Verification

- `nix develop --command just openspec-validate` — passed, 169/169.
- `git diff --check` — passed.
- `just quick thegn-core` — passed.
- `nix develop --command cargo nextest run -p thegn-core capability` — passed,
  18 tests selected.
- `just quick thegn-host` — passed.
- `nix develop --command cargo nextest run -p thegn-host help` — passed, 75
  tests selected.

The direct ambient OpenSpec invocation could not start because `openspec` was
not on `PATH`; the required validation was rerun successfully in the
repository's Nix dev shell. Full-workspace tests, CI, e2e, and a live daemon
were intentionally not run per the chunk's dev-loop policy.

## Commit

`docs(the-40): publish native GUI decision record`
