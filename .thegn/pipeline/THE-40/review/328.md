# THE-40 security/test/bug review — dispatch 328

## Verdict

PASS — ready for the merge queue.

## Findings

No blocking security, correctness, test, race, permission, or ratchet defect was
found in the branch diff.

- Confirmed the branch is documentation-only: no Rust, dependency, config,
  capability, route/schema, database, runtime, test, or ratchet file changes.
  The repository roadmap explicitly classifies THE-40 as a GUI-lane record, so
  replacing the stale draft's implementation tasks with a not-now decision is
  consistent with the authoritative scope.
- Audited the security-sensitive attach claims against the live client, HTTP
  adapter, daemon session actor, capability/scope table, codec, CORS, pairing,
  and token code. The decision record accurately identifies that observer
  attach is write-scoped and accepts upstream mutation, attach identifiers are
  interpolated into a URI, same-id reconnect can suffer stale-detach races, and
  post-handshake decode/transport errors collapse into channel closure.
- Confirmed those existing hazards are not presented as shipped safety. The
  record gates a future frontend on URI-safe identifiers, connection-owned
  detach semantics, required-first-`Hello`, typed decode/transport failures,
  observer mutation/scope fixtures, reconnect/sequence/lag/version behavior,
  and server-enforced rejection for any future read-only stream.
- Confirmed the one-catalog, shell-independence, 0%-idle, substrate-boundary,
  plaintext-TCP, scoped-token, and no-direct-DB/PTY constraints are consistent
  across the dated decision and archived OpenSpec record. No small fix was
  needed.

## Verification

Passed:

- `nix develop --command just openspec-validate` — 169/169 live specs and
  changes passed.
- `git diff --check main...HEAD` and final working-tree diff check.
- `nix develop --command just quick thegn-core`.
- `nix develop --command cargo nextest run -p thegn-core capability` — 18/18.
- `nix develop --command just quick thegn-host`.
- `cargo nextest run -p thegn-host help` — 75/75.
- Mandatory queue gate: `RUSTC_WRAPPER= THEGN_ALLOW_HEAVY=1 just test` with the
  managed socket sandbox lifted — 7,065/7,065 passed, 22 skipped.

The first mandatory-gate invocation died before tests because `sccache` was
denied by the managed sandbox, exactly matching the environmental exception.
The wrapper-disabled retry then proved two UDP loopback tests were also denied
socket creation by that sandbox. Re-running the same wrapper-disabled gate with
the required sandbox permission produced the green result above; both DNS
tests passed there.

## Unverified scope

- The understand-anything knowledge graph is absent, so no graph overlay could
  be generated; direct Git, roadmap, and source/call-site inspection was used.
- No CI, e2e suite, live daemon, GUI prototype, or network-facing deployment was
  run. THE-40 adds none of those surfaces, and the mandatory workspace test gate
  is green.
