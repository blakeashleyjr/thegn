# THE-429 adversarial source review — dispatch 504

verdict: source-review-clear

## Review basis

Reviewed the complete candidate from base `0be575ce` through `f1e613bd`
against `CLAUDE.md`, `docs/ARCHITECTURE.md`, `.thegn/maintenance/issue.json`,
and `.thegn/maintenance/primary-final-review.md`. Traced the Nix shell-entry
ordering, generated pre-commit/pre-push setup, immutable legacy detector,
ordinary Git checkout/worktree paths, and the native `thegn wt new` fixture.
No production code was changed.

## Findings

No remaining exploitable source defects found. In particular:

- `flake.nix:452-473` no longer installs or invokes a checkout hook, and the
  only shell-entry helper is immutable Nix-store detector code. The tracked
  branch-controlled hook payload is absent; explicit pre-commit/pre-merge/
  pre-push gates remain configured.
- `nix/detect-legacy-post-checkout.py:118-205` bounds Git output and runtime,
  scrubs repository/index/object/namespace/config redirection variables, uses
  an isolated POSIX process group, kills that group on timeout/overflow/capture
  failure, closes both pipes, and finitely reaps the leader. The normal EOF /
  waitpid race is handled without killing a successful query.
- `nix/detect-legacy-post-checkout.py:256-417` verifies the canonical main
  checkout and ordinary `.git/hooks` identity, refuses linked/shared/custom
  hook authorities and ambiguous path types, and reads the candidate through
  rooted no-follow descriptors without mutation. Refusals are reported rather
  than silently changing a hook or config.
- `test/the429_checkout_hooks.py:313-380` preserves the former config target
  matrix (regular, directory, valid/dangling symlink, FIFO, hardlink) around
  actual shell setup and checkout. This review added the concurrent shell-entry
  plus checkout regression at `test/the429_checkout_hooks.py:362-380`, proving
  the foreign config's bytes/inode/metadata remain unchanged while multiple
  immutable setup processes overlap real branch switches.
- The native worktree creation path and the ordinary Git path both showed no
  branch payload execution or automatic config seeding. Automatic install,
  update, uninstall, interrupted-update, and rollback criteria are not active
  paths in this removal design; the documented manual legacy cleanup remains
  primary-owned and is intentionally not performed by this review.

The pre-existing live legacy hook remains unsafe until the primary explicitly
retires it, as required by the primary instructions. That operational cleanup
was not performed and is not counted as source validation.

## Validation performed

Passed:

- `python3 -B -m py_compile nix/detect-legacy-post-checkout.py test/the429_checkout_hooks.py`
- `sh -n test/git-hooks/heal-worktree.sh`
- `python3 -B test/the429_checkout_hooks.py` — focused fixture passed; native
  fixture intentionally absent in this invocation.
- `THEGN_TEST_BINARY=/home/blake/code/thegn/target/release/thegn python3 -B test/the429_checkout_hooks.py` — focused and native `wt new` fixtures passed.
- `git diff --check 0be575ce..HEAD`
- `git diff --check`

Not run by instruction: Cargo, builds, nextest, Clippy, full Rust tests,
Nix evaluation, or any full gate. No live hook/config/DB mutation, merge,
push, tracker closure, child-agent dispatch, or row-status change was done.

## Exact primary-owned commands still needing execution

```text
nix develop --command just lint
nix develop --command just test
nix develop --command just smoke
nix develop --command just flake-check
nix develop --command just ci
```

This is a source-review-clear result only; it is not a full-gate pass.
