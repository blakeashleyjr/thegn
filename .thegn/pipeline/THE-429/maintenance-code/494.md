# THE-429 maintenance-code dispatch 494

verdict: implementation-ready

## Scope implemented

- Hardened `nix/detect-legacy-post-checkout.py` with a single bounded Git
  query helper: non-interactive stdin/environment, a two-second timeout,
  64 KiB combined stdout/stderr cap, typed refusal, and lossless filesystem
  decoding for Git path output.
- Refused inherited command-scope `core.hooksPath` before repository or hook
  inspection, while preserving the existing repository-local/global/system/
  worktree collision policy.
- Required `O_NOFOLLOW`, `O_DIRECTORY`, `O_NONBLOCK`, and `O_CLOEXEC`; opened
  the candidate nonblocking and compared its descriptor identity to the
  `lstat` candidate before reading. No deletion or repair was added.
- Added the exact reviewed legacy hook as a non-executable fixture and pinned
  its SHA256 to
  `90b85945a3c30c3a9aa806f87fe9585e9ce660e450672e3e5e24b40dca140365`.
- Expanded the hermetic regression to invoke the same immutable detector used
  by `flake.nix` before hostile checkout/worktree creation; added an isolated
  historical probe proving the old hook reaches branch payload bytes; added
  bounded child tests for FIFO, symlink, and different-regular replacement,
  oversized/hung/invalid-byte Git output, and inherited command-scope config.
- Registered the regression in the routine `test-the429` recipe and made
  `test` depend on it. Existing local-gate and manual migration behavior is
  unchanged; live legacy copies remain active until explicit user cleanup.

## Focused validation actually run

- `python3 -B test/the429_checkout_hooks.py` — passed:
  `THE-429 focused checkout-hook and detector fixtures: ok`
- `just test-the429` — passed; recipe output:
  `python3 -B test/the429_checkout_hooks.py` followed by
  `THE-429 focused checkout-hook and detector fixtures: ok`
- `python3 -B -m py_compile nix/detect-legacy-post-checkout.py test/the429_checkout_hooks.py` — passed.
- `git diff --check` — passed.
- Copied source tree with `.git` omitted:
  `scratch=$(mktemp -d); mkdir "$scratch/tree"; cp -a test nix flake.nix justfile "$scratch/tree/"; python3 -B "$scratch/tree/test/the429_checkout_hooks.py"`
  — passed:
  `THE-429 focused checkout-hook and detector fixtures: ok`.
- Fixture digest check — passed with the pinned SHA256 above; fixture mode is
  non-executable.

## Unverified / pending central validation

- No Cargo, build, nextest, Clippy, full test, Nix evaluation, or full gate was
  run, per primary instructions and CPU limits. Primary centrally schedules
  those checks across the batch.
- No existing compiled `target/debug/thegn` or `target/release/thegn` was
  present, so the native thegn worktree-creation path could not be exercised
  without a prohibited build. The Git worktree path, isolated XDG/Git config,
  malicious branch, and multiple-worktree cases were exercised.
- No live hooks, dotfiles, shared Git config, merge queue, tracker state, or
  issue status were changed.

Exact commands still needing primary-scheduled execution:

- `nix develop --command just test`
- `nix develop --command just lint`
- `nix develop --command just flake-check`
- `nix develop --command just ci`

next: run the primary-scheduled Nix/native/Rust validation; review the commit
and retain the explicit manual migration policy.
