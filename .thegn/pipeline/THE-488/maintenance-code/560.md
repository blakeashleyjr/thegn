# THE-488 maintenance code — native dispatch 560

verdict: implementation-ready

## Scope implemented

- Repaired the four failing regression cases in the shared ratchet helper tests.
- Kept the ordinary-string regression syntactically valid while exercising an
  escaped quote followed by `//`; the stripper must preserve that text and
  continue stripping only the later block comment.
- Chose `# header`, a blank separator, then `# RATCHET-EMPTY` as the canonical
  empty-marker output. The update test now runs twice and asserts idempotence.
- Fixed the symlinked-source-root fixture to create the manifest directory
  before installing the source-root symlink.
- Kept the injected traversal, directory-entry, metadata, source-read, and
  allowlist-read failures fail-closed. The out-of-root normalization case now
  asserts the intended path-refusal error without incorrectly requiring the
  injected-I/O marker.
- Applied every test change identically to the core, media, and metrics helper
  copies. The byte-identity regression remains in place.

The underlying approved implementation remains in scope: nested `cfg` and
`cfg_attr` predicate walking, string-aware comment stripping, fail-closed
source/allowlist I/O, symlink refusal, explicit empty markers, and the
dependency-neutral duplicated helper design.

## Newly exposed source reconciliation

The primary task data identifies exactly one newly exposed file across the
five ratchets:

- `crates/thegn-host/src/profile.rs` — retained outside `src/platform/` and
  pinned in `test/platform-cfg-host-ratchet.txt` with a written reason. Its
  `unix` predicate gates the profiling feature's SIGUSR2/pprof implementation,
  which is a small profiling seam rather than a general platform abstraction.

No other newly exposed file was identified in the approved scan result, and no
additional allowlist entries were added.

## Files changed in this dispatch

- `crates/thegn-core/src/test_support/ratchet.rs`
- `crates/thegn-media/src/ratchet.rs`
- `crates/thegn-metrics/src/ratchet.rs`
- `.thegn/pipeline/THE-488/maintenance-code/560.md`

## Checks actually run

- `rustfmt --edition 2021 --check crates/thegn-core/src/test_support/ratchet.rs crates/thegn-media/src/ratchet.rs crates/thegn-metrics/src/ratchet.rs`
- `git diff --check`
- `cmp` confirmed all three helper copies are byte-identical.
- SHA-256 confirmed all three helper copies have the same digest:
  `8cf8fd01c05c739d5ec4f5fecc3bcd05b4efa8482114eb032e0289acfc265dd6`.

## Unverified validation

Per the dispatch CPU restriction, no Cargo, build, nextest, Clippy, or full
suite command was run. The primary must execute these focused checks:

    nix develop --command cargo check -p thegn-core --all-targets
    nix develop --command cargo check -p thegn-svc --all-targets
    nix develop --command cargo check -p thegn-host --all-targets
    nix develop --command cargo check -p thegn-media --all-targets
    nix develop --command cargo check -p thegn-metrics --all-targets
    nix develop --command cargo test -p thegn-core --lib ratchet
    nix develop --command cargo test -p thegn-media --lib ratchet
    nix develop --command cargo test -p thegn-metrics --lib ratchet

The batch gate remains unverified here; no full-gate result is claimed. The
requested legacy `.thegn/maintenance/issue.json` path is absent; task data was
read from `.thegn/maintenance/THE-488/issue.json` and the primary revision
brief.

No merge, push, tracker closure, row-status change, child-agent action, or
main-branch edit was performed.
