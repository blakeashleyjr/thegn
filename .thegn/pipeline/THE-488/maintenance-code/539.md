# THE-488 maintenance code — native dispatch 539

verdict: implementation-ready

## Scope implemented

- Replaced the three byte-identical ratchet helpers with a dependency-neutral,
  hand-rolled lexer and balanced predicate walker.
- The walker inspects every sibling and nested expression in cfg and cfg_attr,
  including platform terms after feature terms and under not/any/all.
- Comment stripping now handles nested block comments, line comments, escaped
  ordinary strings, byte strings, and raw strings with arbitrary hash counts.
  Attribute discovery ignores comments and string contents.
- Added a private generic RatchetIo seam. Production ratchets use the real
  filesystem implementation; tests inject deterministic traversal, directory
  entry, metadata, path-normalization, source-read, and allowlist-read failures.
  Every failure preserves the operation, exact path, and underlying error.
  Update header reads and writes are also fail-closed.
- Added direct adversarial scanner tests, fail-closed filesystem tests,
  missing-allowlist coverage, header/update/stale-entry round-trip coverage,
  and a byte-identity test for core/media/metrics.
- Added the approved reasoned profile.rs entry to the host platform ratchet.

## Newly exposed source reconciliation

The primary investigation's sound-scan result identifies exactly one newly
exposed file:

- crates/thegn-host/src/profile.rs — pinned in
  test/platform-cfg-host-ratchet.txt. Its unix predicate gates the profiling
  feature's SIGUSR2/pprof implementation and no-op stubs, not a platform
  abstraction seam; the entry records this as explicit shrink-only debt.

The repaired scanner's full ratchet execution remains unverified in this
dispatch because the primary centrally schedules Rust validation.

## Files changed

- crates/thegn-core/src/test_support/ratchet.rs
- crates/thegn-media/src/ratchet.rs
- crates/thegn-metrics/src/ratchet.rs
- test/platform-cfg-host-ratchet.txt
- .thegn/pipeline/THE-488/maintenance-code/539.md

## Static checks performed

- rustfmt --edition 2021 on the canonical helper, then mirrored to media and
  metrics.
- rustfmt --edition 2021 --check on all three helpers.
- git diff --check.
- cmp and SHA-256 comparison confirmed all three helper copies are byte-identical.
- Source inspection confirmed the four profile.rs conditional sites and the
  reasoned profile.rs allowlist entry.

No Cargo command, build, nextest, Clippy run, or full test suite was executed.

## Exact validation commands still required

These are intentionally pending primary scheduling:

    nix develop --command just lint
    nix develop --command just test

Those gates must exercise the core/service/host/media/metrics ratchet callers,
the direct helper regressions, and the new profile.rs reconciliation. No full
gate result is claimed here.

No concrete design blocker was found within the approved scope. No merge, push,
tracker closure, row-status change, child-agent action, or main-branch edit was
performed.
