# THE-488 maintenance code — native dispatch 550

verdict: implementation-ready

## Scope implemented

- Kept the hand-rolled, dependency-neutral predicate walker and its complete
  nested `cfg`/`cfg_attr` coverage from the approved implementation.
- Made empty and comment-only allowlists fail closed with the exact allowlist
  path. Added the explicit `# RATCHET-EMPTY` marker for the two intentional
  zero-pin ratchets (`hostkey-host` and `hostkey-svc`), and made empty updates
  emit the marker rather than silently recreating an unpinned list.
- Switched source metadata to `symlink_metadata` and refuse every symlinked
  source entry before recursion, preventing loops and external-tree traversal.
- Documented that scans are not atomic with concurrent working-tree edits and
  may reflect either version when racing an edit, as adjudicated by the
  primary.
- Added direct regressions for comment-only empties, explicit empty markers,
  empty current hit sets, and Unix symlinked source entries. The prior direct
  predicate, comments/ordinary-and-raw-strings, injected I/O, and byte-identity
  regressions remain in place.
- Preserved byte-identical helper copies in core, media, and metrics.

## Newly exposed source reconciliation

The repaired scanner's approved reconciliation identifies exactly one newly
exposed source file:

- `crates/thegn-host/src/profile.rs` — already pinned in
  `test/platform-cfg-host-ratchet.txt` with the written reason that its
  SIGUSR2/pprof conditional is a small profiling seam rather than production
  platform abstraction code. No new file was exposed by this revision's
  fail-closed changes.

## Files changed

- `crates/thegn-core/src/test_support/ratchet.rs`
- `crates/thegn-media/src/ratchet.rs`
- `crates/thegn-metrics/src/ratchet.rs`
- `test/hostkey-host-ratchet.txt`
- `test/hostkey-svc-ratchet.txt`
- `.thegn/pipeline/THE-488/maintenance-code/550.md`

## Source-only checks performed

- `rustfmt --edition 2021` on the canonical helper, then mirrored to media and
  metrics.
- `rustfmt --edition 2021 --check` on all three helper copies.
- `cmp` and SHA-256 comparison confirmed all three helper copies are
  byte-identical.
- `git diff --check` passed.
- Static inspection confirmed exactly one explicit empty marker in each
  intentional empty host-key allowlist and the expected changed-file scope.

No Cargo command, build, nextest, Clippy run, lint gate, focused Rust test, or
full test suite was executed, per the CPU and primary-scheduling constraints.

## Exact validation commands still required

The primary must execute the batch gate:

    nix develop --command just lint
    nix develop --command just test

These must exercise the core/service/host/media/metrics ratchet callers,
predicate and fail-closed regressions, symlink handling on supported platforms,
the explicit empty-marker ratchets, and the existing `profile.rs`
reconciliation. No full gate result is claimed here.

No concrete design blocker was found within the approved scope. No merge, push,
tracker closure, row-status change, child-agent action, or main-branch edit was
performed.
