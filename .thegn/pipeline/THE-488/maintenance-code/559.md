# THE-488 maintenance code — native dispatch 559

verdict: implementation-ready

## Scope implemented

- Fixed the compiling regression in `allowlist()`: it still returns the
  established `BTreeSet<String>` contract, but now explicitly projects
  `Allowlist::entries` only after `read_allowlist()` has validated the
  allowlist read, explicit-empty marker, and empty-list policy. The marker
  cannot be bypassed by this projection.
- Closed the source-root symlink escape by checking the initial `src/` root
  with `symlink_metadata` through the `RatchetIo` seam before the first
  `read_dir`. Symlinked roots now fail with the same explicit refusal as
  symlinked entries.
- Applied both changes identically to the core, media, and metrics helper
  copies; their byte-identity regression remains in place.

## Newly exposed source reconciliation

No additional source file was newly exposed by this round's fixes. The
previously identified `crates/thegn-host/src/profile.rs` remains explicitly
reconciled in `test/platform-cfg-host-ratchet.txt` with the written reason
that its SIGUSR2/pprof conditional is a small profiling seam rather than
production platform abstraction code. The root-symlink fix changes traversal
safety only and does not add an allowlist entry.

## Files changed

- `crates/thegn-core/src/test_support/ratchet.rs`
- `crates/thegn-media/src/ratchet.rs`
- `crates/thegn-metrics/src/ratchet.rs`
- `.thegn/pipeline/THE-488/maintenance-code/559.md`

## Checks actually run

- `rustfmt --edition 2021 --check crates/thegn-core/src/test_support/ratchet.rs crates/thegn-media/src/ratchet.rs crates/thegn-metrics/src/ratchet.rs`
- `cmp` and SHA-256 comparison confirmed all three helper copies are
  byte-identical.
- `git diff --check`
- `nix develop --command cargo check -p thegn-core --all-targets` — passed.
- `nix develop --command cargo check -p thegn-media --all-targets` — passed;
  emitted the existing `sources` dead-code warning for the lib test target.
- `nix develop --command cargo check -p thegn-metrics --all-targets` — passed;
  emitted the same existing `sources` dead-code warning for the lib test
  target.

The requested legacy `.thegn/maintenance/issue.json` path is absent; task data
was read from `.thegn/maintenance/THE-488/issue.json` as well as the primary
revision brief.

## Unverified validation

Not run: `cargo test`, nextest, builds, Clippy, `just lint`, `just test`,
`just ci`, focused Rust tests, or any full test suite. The primary must run:

    nix develop --command just lint
    nix develop --command just test

No full gate result is claimed. No concrete design blocker remains within the
approved scope. No merge, push, tracker closure, row-status change,
child-agent action, or main-branch edit was performed.
