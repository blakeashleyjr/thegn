# THE-49 security/test/bug review — dispatch 323

Verdict: **PASS**. The branch is ready for the merge queue.

## Finding fixed during review

- The decision record made a future memory reader immutable, attributed, and
  workspace-partitioned, but did not define the semantic trust boundary for
  retrieved prose. Pipeline artifacts, reports, notes, tracker text, and
  session summaries are operator-, provider-, or agent-authored and can carry
  prompt-like instructions even after terminal control characters are
  stripped. Commit `7d65a729` now requires readers to present that material as
  attributed data, never instruction authority, never auto-execute embedded
  commands/links/tool calls, and test that adversarial content remains inert.

No blocking finding remains.

## Adversarial review

- Confirmed the branch remains documentation-only: no Rust, config, schema,
  capability, help, provider, MCP, daemon, platform, migration, or ratchet file
  differs from `main`.
- Checked the cited dispatch/report/note implementation. Reports and notes are
  length-bounded and strip terminal control characters; missing roster rows
  error rather than silently accepting an ineffective update.
- Checked the artifact path and completion boundary. Generated paths use a
  component allowlist, final-component symlinks are rejected, and the decision
  record accurately warns that index membership plus a dirty worktree does not
  prove the read bytes are in `HEAD`. The future contract therefore reads
  pipeline content from a named commit.
- Checked session discovery. It is bounded to the newest 500 candidate files,
  degrades unreadable stores and malformed records, and supports exact
  worktree filtering. The decision record now also calls out summary privacy
  and semantic prompt-injection risk.
- Checked the current memex README at `https://github.com/nicosuave/memex`.
  Its BM25 transcript search, optional local embeddings, shared skill,
  optional continuous indexing, and SSH multi-machine behavior support the
  prior-art summary.
- `git diff --check main...HEAD` passed. The worktree was clean before this
  mandatory artifact was created.

## Verification

- `env XDG_RUNTIME_DIR=/tmp RUSTC_WRAPPER= just quick thegn-core` — passed.
  The first sandboxed attempt could not create Just's temp directory under the
  read-only `/run/user/1000`; setting the runtime temp root resolved it.
- `env XDG_RUNTIME_DIR=/tmp RUSTC_WRAPPER= cargo nextest run -p thegn-core pipeline_report`
  — passed, 10 tests; 3,602 skipped.
- `env XDG_RUNTIME_DIR=/tmp RUSTC_WRAPPER= cargo nextest run -p thegn-core pipeline_run`
  — passed, 17 tests; 3,595 skipped, including traversal and artifact failure
  paths.
- `THEGN_ALLOW_HEAVY=1 just test` — environmental failure before discovery:
  the configured sccache wrapper returned `Operation not permitted`; Cargo's
  `rustc -vV` probe identified it as OS error 1.
- Required environmental retry,
  `RUSTC_WRAPPER= THEGN_ALLOW_HEAVY=1 just test` — passed: **7,048 tests
  passed, 22 skipped**. This includes the workspace-level configuration,
  environment-overlay, validation, pipeline, and session suites.

## Not verified / next hints

- The `understand-diff` knowledge-graph overlay could not be generated because
  `.understand-anything/knowledge-graph.json` is absent. Run `/understand` if a
  graph visualization is wanted; direct diff, history, source, and test review
  were completed without it.
- The merge step remains `thegn integrate` and was not run by this reviewer.

## Dispatch-report delivery blocker

- The required live command used the current workspace binary and did not set
  `XDG_STATE_HOME`:
  `./target/debug/thegn dispatch report 323 --text '<PASS report>'`.
  It reached `/home/blake/.local/state/thegn/thegn.db` but failed with SQLite
  error 8, `attempt to write a readonly database`. The database and directory
  are owned by `blake`, but `findmnt` reports the containing `/home` mount as
  `ro`; host-level command approval did not make it writable.
- Report text for the supervisor to replay from a writable live environment:
  `PASS; docs-only branch ready for merge queue; fixed semantic prompt-injection trust boundary in 7d65a729; artifact c7bf2e23 (superseded by the commit containing this delivery note); scoped core suites green; wrapper-disabled mandatory full gate green (7048 passed, 22 skipped) after configured sccache failed before discovery; unable to generate understand-diff overlay because knowledge graph absent; next: thegn integrate.`
