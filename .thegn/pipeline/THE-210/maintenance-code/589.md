# THE-210 maintenance code — native dispatch 589

## Verdict

revisions-needed

## Implemented

- Corrected `gate_isolation_never_advances_or_blames_candidate` to exercise the
  actual isolation fallback: the reusable lock is held, then the isolated gate
  exits with infrastructure status 127. The test now requires an
  `GateOutcome::Errored`, no CAS advance, no landed branch, and no candidate
  blame. The prior failure was a test-scenario defect: a successful isolated
  gate is allowed to advance, so asserting `!report.advanced` while the
  fallback passed was asserting against the wrong path.
- Corrected `root_git_mapping_is_revalidated_before_reporting_success` to
  resolve its gate path before intentionally corrupting `.git/commondir`. The
  test still verifies that the live workspace refuses the changed mapping, but
  the test helper's canonical-identity `expect` can no longer define or mask
  the production contract.

## Validation actually run

Passed:

```text
git diff --check
rustfmt --edition 2024 --check crates/thegn-host/src/integrate_gate_tests.rs
```

The primary-authorized check could not start compilation. Each command failed
before entering Cargo because Nix attempted to create a temporary directory in
the read-only store:

```text
nix develop --command cargo check -p thegn-host --all-targets
→ error: creating directory "/nix/store/tmp-372-2460162864": Read-only file system

TMPDIR=/tmp nix develop --command cargo check -p thegn-host --all-targets
→ error: creating directory "/nix/store/tmp-391-696934386": Read-only file system

nix develop --option sandbox false --command cargo check -p thegn-host --all-targets
→ error: creating directory "/nix/store/tmp-496-2317856238": Read-only file system
```

No Cargo test result is claimed. The focused tests remain unverified because
the required dev shell could not be created.

## Exact validation still needing execution

```text
nix develop --command cargo check -p thegn-host --all-targets
nix develop --command cargo test -p thegn-host --lib gate_isolation_never_advances_or_blames_candidate
nix develop --command cargo test -p thegn-host --lib root_git_mapping_is_revalidated_before_reporting_success
```

The broader `nextest`, Clippy, build, and full-suite gates remain outside this
dispatch lane. No row status was changed.
