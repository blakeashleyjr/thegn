# THE-210 maintenance code — native dispatch 588

## Verdict

implementation-ready

## Implemented

- Imported the existing `#[cfg(test)]` `gate_base_for_repo` helper into
  `integrate_gate.rs`'s test-module scope.
- Kept the helper test-only, so non-test library builds do not acquire a
  production-only dependency on the test identity adapter.
- Preserved the approved production design and behavior: canonical Git common
  directory identity, stable `RepositoryId` gate roots, isolation fallback on
  lock/state failure, private isolated target directories, and concrete
  `GateVerdict::Error` diagnostics.

## Regression coverage

The existing THE-210 regression tests in `integrate_gate_tests.rs` now resolve
the helper through the same module path used by the test target. This restores
coverage for lock-acquisition isolation, concurrent distinct-OID gates,
canonical linked/aliased worktree identity, and Windows isolation without
changing their behavior.

## Validation

Ran successfully:

```text
git diff --check
rustfmt --edition 2024 --check crates/thegn-host/src/integrate.rs crates/thegn-host/src/integrate_gate.rs crates/thegn-host/src/integrate_gate_tests.rs
```

Cargo, builds, nextest, Clippy, and full suites were not run because the
dispatch instruction forbids them and Rust validation is centrally scheduled.
No compilation or test result is claimed.

## Exact validation commands still needing execution

```text
nix develop --command cargo check -p thegn-host --all-targets
nix develop --command cargo nextest run -p thegn-host gate
```

The primary revision's requested Cargo validation remains unverified in this
lane; the source-level failure was reconciled without a behavioral or design
change.
