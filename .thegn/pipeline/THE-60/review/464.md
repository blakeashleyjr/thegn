# THE-60 security/test/bug review — row 464

## Verdict

PASS — ready for the merge queue.

## Review findings and fixes

Adversarial review found and fixed these failure-path issues:

- Local, probe, and target-side config reads now reject symlinked path
  components, preventing trusted config resolution from escaping the scoped
  worktree/target path.
- Resolver thread-spawn failures now clear in-flight state, so a failed spawn
  does not permanently suppress future refreshes.
- Degraded activation-cache records no longer count as successful cache hits;
  transient mise/plugin failures remain retryable.
- Host provisioning markers are shell-quoted and versioned, and are written
  only after every provisioning step succeeds. Failed toolchain/provisioning
  steps therefore retry on the next claim instead of being hidden by a stale
  marker.

No remaining blocking injection, path, permission, swallowed-error, or
concurrency defect was found in the reviewed scope.

## Verification

- Required queue-boundary gate: `XDG_STATE_HOME=/home/blake/.superzej/pipeline-state THEGN_ALLOW_HEAVY=1 just test` — 7,628 passed, 22 skipped.
- Post-final-fix gate with the wrapper disabled: same workspace gate — 7,628 passed, 22 skipped.
- Scoped core and host suites, including mise provider, provisioning, doctor,
  hydrate, environment-plan, capability, and ratchet tests — green.
- `git diff --check` and the commit hook's treefmt check — green.
- No socket tests were excluded; no sandbox-denied test occurred.

## Could not verify

Live SSH/OCI/mise-provider execution and cross-platform target behavior were
not available in this sandbox. Direct integration coverage for live remote
transport, cache refresh/waker timing, and a concurrent config mutation during
an explicit install also remains unexercised; the full gate covers the
available unit and integration suites.

The workspace-wide cargo-format check still reports pre-existing formatting in
the merged `thegn-core` database tests, unrelated to this review's changes;
the reviewed changes are treefmt-clean.

## Next hint

The merge step remains `thegn integrate`.
