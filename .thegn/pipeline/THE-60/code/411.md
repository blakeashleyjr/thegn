# THE-60 row 411 completion — explicit toolchain install action

## Verdict

Done. Added the provider-generic `toolchain-install` palette action and
documented its trust-gated, off-loop behavior.

## Implemented

- Added `Action::InstallToolchain`, stable key `toolchain-install`, palette
  metadata, search keywords, and no default chord.
- Dispatches installation from the active worktree through `spawn_blocking`;
  completion sends a toast, model refresh, and `TerminalWaker` pulse.
- Added the provider-owned bounded `mise install` entry point. It rejects
  missing/remote/off targets, requires the canonical current `mise.env` trust
  approval (including shims mode), sanitizes the child environment, discards
  command output, and enforces a timeout.
- Updated `docs/help/sandboxing.md` with detection, shims/env precedence,
  trust, degraded tokens, remote behavior, and explicit install guidance.
- Help ratchets remain clean after regeneration; the two empty action ratchets
  dropped stale explanatory comments, while the panel-prose ratchet was
  unchanged. Completion-slot and control-schema snapshots are unchanged.

## Verification

- `cargo fmt --all -- --check`
- `git diff --check`
- `just quick thegn-host`
- `cargo nextest run -p thegn-host keymap` — 61 passed
- `cargo nextest run -p thegn-host palette` — 45 passed
- `cargo nextest run -p thegn-host help` — 75 passed
- `cargo nextest run -p thegn-host completion` — 15 passed
- Help ratchet/prose/panel-prose updater tests — all passed; empty ratchets
  regenerated and validated
- `RUSTC_WRAPPER= cargo nextest run -p thegn-svc --test control_schema control_wire_matches` — 1 passed

The first control-schema attempt was blocked before compilation by the known
inherited sccache read-only-filesystem failure; the wrapper-free rerun passed.
Full workspace, coverage, CI, smoke, e2e, and live mise/network checks were not
run per the chunk policy.

## Commit

`feat(ui): expose explicit toolchain install action`
