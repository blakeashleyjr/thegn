# THE-60 code row 450 completion

## Verdict

Done. Implemented revision 2 (`architect-review/revision-2.md`) with commit
`d6a3ff4e` (`fix(host): make remote toolchain activation cache-first`).

## Changes

- Made remote mise detection, target-byte identity, binary/shims probes, and
  approved environment resolution cache-first for launches; cold target work
  runs off-loop and returns `Reserved`/safe fallback immediately.
- Added identity/location-keyed remote cache records with bounded refresh,
  redacted name-only probe data, in-flight dedupe, and existing model refresh
  channel plus `TerminalWaker` delivery for local and remote completion/failure.
- Added target-aware `repo trust` resolution for remote/provider worktrees,
  including current target identity invalidation and placeholder avoidance.
- Persisted presence-only missing-tool and degraded-resolution facts for local
  and remote hydrated status; exposed them through the environment panel and
  tab token.

## Verification

- `cargo fmt --all -- --check` — passed.
- `git diff --check` — passed.
- `RUSTC_WRAPPER= cargo check -p thegn` — passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host mise_provider host_provision doctor hydrate toolchain` — 114 passed.
- `RUSTC_WRAPPER= just quick thegn-host` — attempted; the long clippy
  process exceeded the 30-second tool PTY window and its result was not
  retained by the harness.
- Full workspace, coverage, CI, smoke/e2e, live mise, and live remote target
  behavior were not run per the chunk policy.
