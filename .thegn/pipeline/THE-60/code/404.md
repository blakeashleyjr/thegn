# THE-60 chunk 2 completion — host provider, launch parity, trust, status

## Verdict

Done. Added the host-side toolchain provider and consumed the shared core
activation plan from normal, sandbox, remote, and daemon launch paths through
`agent::launch_spec_full`.

## Implemented

- Added a cache-first host provider with identity-stamped 0600 environment
  cache files, deduplicated background resolution, bounded/sanitized child
  processes, shims fallback, and fail-closed trust handling.
- Added trust request listing/approval integration for the canonical `mise.env`
  config-set request; resolved environment values remain out of logs/UI.
- Replaced separate devshell launch injection with one bundle → devshell →
  provider → base activation application for sandbox and host launches. Remote
  targets return `Reserved` without inspecting local files.
- Added cache-only hydrated worktree status, environment-panel reporting, terse
  tab-bar status tokens, and doctor JSON/text fields for tier, mode, trust,
  files, shims, version, missing tools, and degradation state.
- Preserved the daemon path by leaving it on the existing
  `agent::launch_spec_full` call; no second daemon provider was added.

## Verification

- `RUSTC_WRAPPER= cargo check -p thegn-host --bin thegn`
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host mise_provider` — 5 passed
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host doctor` — 19 passed
- Earlier focused host groups (`toolchain doctor hydrate`) — 106 passed
- `cargo fmt --all` and `git diff --check`

The normal `just quick thegn-host` attempt was blocked before compilation by
the inherited sccache read-only-filesystem failure; the equivalent wrapper-free
scoped check passed. Full `just test`, coverage, CI, cross-target, smoke, and
e2e gates were not run per the chunk specification.

## Commit

`feat(host): wire mise provider into launches`
