# THE-60 row 428 completion — provider-target install and remote parity

## Verdict

Done. Provider-target toolchain installation is now routed through the existing
bounded OCI/SSH execution seam, and remote activation derives detection and
trust identity from target-side worktree bytes.

## Implemented

- Added a core byte-based config-set identity helper so remote/provider trust
  requests use the same canonical hash as local worktrees without persisting
  declaration contents.
- Moved target declaration detection into the host mise provider adapter and
  added bounded remote/provider command capture, target file identity reads,
  remote binary/shims checks, and remote `env` activation.
- Added the provider-owned target install operation with trust refusal for
  missing/edited/unapproved config, sanitized target environment, bounded
  execution, discarded output, and no host mise bootstrap.
- Wired `ToolVersions` host provisioning to an explicit progress-tracked
  provider step; target detection failures remain visible instead of silently
  dropping the declared toolchain.
- Preserved cache-only local hydration/doctor behavior and the existing
  daemon launch path; no control/API/MCP surface changed.

## Verification

- `cargo fmt --all`
- `git diff --check`
- `RUSTC_WRAPPER= cargo check -p thegn-host --bin thegn`
- `RUSTC_WRAPPER= cargo nextest run -p thegn-core toolchain_activation envplan` — 74 passed
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host mise_provider host_provision` — 7 passed
- New target detection/identity regression — 1 passed
- `just quick thegn-host` — passed
- Executable-name audit: no mise process construction outside
  `crates/thegn-host/src/mise_provider.rs`

Full workspace, coverage, CI, smoke/e2e, live remote, and live mise/network
behavior were not run per the revision chunk policy.

## Commit

`9ab00213 fix(host): route mise install through provider targets`
