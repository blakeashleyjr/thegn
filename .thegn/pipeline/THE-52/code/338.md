# THE-52 code handoff — dispatch row 338

Verdict: PASS WITH TEST-SPEC CORRECTION

Implementation commit: `ed3908ef feat(nix): add thegn batteries launcher`

## Completed

- Added a separate `nix/batteries.nix` composition that launches the stable
  wrapped thegn package in the flake-pinned Alacritty with FiraCode Nerd Font
  exposed through a launcher-scoped fontconfig file.
- Added the `thegn-batteries` launcher with `meta.mainProgram`. It creates the
  user-owned Alacritty profile only when absent, exports
  `THEGN_ALACRITTY_CONFIG` and `FONTCONFIG_FILE`, quotes immutable/user paths,
  forwards all arguments, and executes stable `thegn` through Alacritty.
- Exposed `packages.<system>.batteries` without changing the default, thegn, or
  dev outputs, and added the new Nix module to the explicit source allowlist.
- Documented direct `nix run` and existing Home-manager composition, the
  `thegn doctor` verification step, x86_64 Linux build status, Ghostty's
  non-pinned status, and the host/signing/driver evidence required before any
  deferred packaging path can be advertised.
- Kept the Home-manager schema, runtime config, help frontmatter/actions,
  release archives, installer, macOS app generator, Cargo manifests, and all
  runtime ratchets unchanged.

## Verification

- `nix build .#batteries` — passed on x86_64 Linux.
- `nix build .#thegn-nobridge` — passed on x86_64 Linux.
- `nix eval ...batteries.meta.mainProgram` for `x86_64-linux`,
  `aarch64-linux`, and `aarch64-darwin` — each resolved to
  `thegn-batteries`.
- Generated launcher `bash -n` check — passed; inspection confirmed pinned
  Alacritty, stable wrapped thegn, generated fontconfig, profile path, and
  argument forwarding.
- Isolated noninteractive launcher check with temporary `XDG_CONFIG_HOME` and
  `XDG_STATE_HOME` — the first run copied the exact bundled profile, the second
  preserved its changed mode, ownership remained with the user, and no SQLite
  database was created.
- `just quick thegn-host` — passed (sandbox runtime moved to `/tmp` and
  `sccache` disabled after both were denied by the execution sandbox).
- `cargo nextest run -p thegn-host --bins` — 2,673 passed, 9 skipped, after
  running outside the restrictive sandbox for the suite's Unix-socket tests.
- `python3 -m unittest discover -s packaging/tests -p 'test_*.py'` — 14 passed.
- Alejandra and Prettier checks, `git diff --check`, and the commit's treefmt
  hook — passed.

## Not verified / next hints

- The specified `cargo nextest run -p thegn-host --lib` cannot run because
  `thegn-host` has no library target. Cargo returned “no library targets
  found”; the real binary test target was used as the scoped replacement and
  passed completely.
- Only x86_64 Linux was built locally. The other two declared systems evaluate
  the launcher metadata, but need native builders for build proof.
- No clean-host GUI launch or in-terminal `thegn doctor` rehearsal was
  performed. Keep additional hosts, macOS font-picker/alternate-emulator
  parity, and every explicitly deferred distribution form unverified until
  their documented host/signing/driver rehearsals exist.
- Full-workspace tests, CI, coverage, and e2e were intentionally not run per
  the chunk's dev-loop policy.
