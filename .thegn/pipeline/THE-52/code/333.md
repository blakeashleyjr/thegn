# THE-52 code handoff — dispatch row 333

Verdict: PASS

Implementation commit: `47d675cc feat(packaging): add deterministic release manifest renderer`

## Completed

- Added the versionless/checksumless release catalog with the active GNU,
  musl, and Apple-silicon targets plus the disabled future MSVC target.
- Added a standard-library-only offline validator/renderer. It enforces the
  Cargo workspace version/tag match, archive/checksum names and contents,
  lowercase SHA-256 shape, safe target/output paths, exact placeholders,
  inactive Windows lanes, deterministic output, and staged atomic replacement.
- Converted the Homebrew formula to a template and added AUR, nfpm, Scoop, and
  winget templates. Active output is Homebrew, `thegn-bin`, and parameterized
  deb/rpm nfpm specs; Scoop/winget remain deliberately blocked.
- Added renderer tests for the binstall/archive contract, tag and checksum
  failures, required archive roots, Arch prerelease normalization, deterministic
  output, inactive/enabled Windows behavior, traversal/placeholder rejection,
  and failure isolation.
- Added `release-package-dry-run` and `release-package-validate` recipes. Both
  use temporary directories and invoke neither the project binary nor a package
  manager, network operation, credential read, or live state directory.

## Verification

- `python3 -m py_compile packaging/release.py packaging/tests/test_release.py` — passed.
- `python3 -m unittest discover -s packaging/tests -p 'test_*.py'` — 14 passed.
- `just release-package-dry-run tag=v0.1.0-alpha.2` — passed.
- Scoped `yamllint` over the JSON/YAML manifest templates and `taplo lint` over
  the Cargo manifests carrying the version/binstall contract — passed in the
  pinned Nix dev shell.
- `just quick thegn-host` — passed.
- `nix develop --command just openspec-validate` — 170 passed, 0 failed.
- `git diff --check` and the commit's ShellCheck/treefmt hooks — passed.

## Not verified / next hints

- `cargo nextest run -p thegn-host --lib` cannot run on this branch because
  `thegn-host` has no library target; after explicitly clearing the sandboxed
  sccache wrapper, Cargo reported that no library targets exist in the package.
  The scoped host quick check passed. A downstream chunk should keep using a
  real target (for example the binary test target) if it needs a nextest run.
- Full-workspace tests, CI, coverage, and e2e were intentionally not run, per
  the chunk's dev-loop policy.
- Chunk 2 can consume `packaging/release.py validate` or `render` with `--tag`
  and `--assets-dir`; rendering also requires `--output-dir`. `--manager` is
  repeatable, and the default is every active manager.
