# THE-52 code handoff — dispatch row 335

Verdict: PASS WITH TEST LIMITATION

Implementation commit: `b5848513 ci(release): render and attest package artifacts`

## Completed

- Added least-privilege job permissions and keyless provenance attestation for
  the exact archive emitted by each upload-matrix leg.
- Added a post-matrix package job that downloads only the requested tag's draft
  assets, validates every active archive/checksum through Chunk 1's renderer,
  renders all active manager metadata, resolves nfpm from the flake-locked
  nixpkgs input, and uploads deterministic deb/rpm and metadata filenames only
  after every input and package succeeds. Source archives are never staged for
  the job's `--clobber` upload.
- Added a visible package summary with output names, target/checksum mapping,
  the `gh attestation verify` command, and external-publication enablement.
- Split external Homebrew/AUR publication into the `publish-external` manual
  operation. It skips the destructive create-release path, requires dispatch
  from `main`, refuses draft releases, runs behind the protected
  `package-publication` environment and explicit repository flag, checks every
  scoped credential, emits the exact pending setup checklist when disabled,
  and makes idempotent tap/AUR commits from generated release metadata.
- Updated `RELEASING.md` with the safe build/retry/publish order, generated
  metadata flow, provenance verification, standalone deb/rpm limitation,
  verified-before-advertised rule, one-time external setup/rehearsal checklist,
  and deferred Scoop/winget, crates.io, and hosted apt/rpm work.

## Verification

- `python3 -m unittest discover -s packaging/tests -p 'test_*.py'` — 14 passed.
- `just release-package-dry-run tag=v0.1.0-alpha.2` — passed.
- `just quick thegn-host` — passed.
- `yamllint .github/workflows/release.yml` — passed in the pinned Nix dev shell.
- Flake-locked `actionlint .github/workflows/release.yml` — passed.
- Flake-locked tool resolution was checked: nfpm `2.47.0` and pacman/makepkg
  `7.1.0-unstable-2026-01-25` both resolve through `--inputs-from .`.
- `git diff --check` and the commit's treefmt/yamllint hooks — passed.

## Not verified / next hints

- The specified `cargo nextest run -p thegn-host --lib` cannot run because
  `thegn-host` has no library target (`Cargo.toml` declares only the `thegn`
  binary target). This is the same test-spec mismatch recorded by row 333.
- A fallback `cargo nextest run -p thegn-host --bin thegn` completed dependency
  compilation without diagnostics but remained in final link work for several
  minutes and was stopped before test execution (exit 130). No nextest result
  is claimed; the scoped quick check is green.
- No live tag/release, attestation API, Homebrew tap, AUR repository, deploy key,
  or protected environment was exercised. The publication path intentionally
  remains disabled until `RELEASING.md`'s external setup and local rehearsal
  checklist is completed.
- Full-workspace tests, CI, coverage, and e2e were intentionally not run per the
  chunk's dev-loop policy.
