APPROVED

Reviewed the complete branch diff from merge-base `ea378764` through
`f7ad2ce0` against the THE-17 architecture/chunk design and the completion
report in `code/350.md`. No revision chunk is required.

The implementation now satisfies the design:

- one core `Editor` seam owns validated worktree/file targets, logical provider
  selection, structured argv, capability declarations, config precedence, and
  doctor probes;
- one host `ide_handoff` edge owns off-loop resolution/revalidation, external
  CPU capping, detached reaping, pane placement, waker delivery, and visible
  degradation;
- sidebar, diff, PR-review, palette, search, and panel entry points converge on
  that edge without accepting provider argv or arbitrary absolute file paths;
- `editor.open` is a strict write-scoped queued intent across HTTP, gRPC,
  generic CLI, MCP, and plugin projection, with the compositor retaining launch
  ownership;
- capability, help, completion, idle-loop, QoS, config, and control-schema
  ratchets remain clean.

Small corrections applied and committed in `f7ad2ce0`:

- editor doctor probes now report actual PATH availability instead of claiming
  every registered provider is ready;
- `Editor::open_target` enforces advertised operation/location capabilities;
- unsupported line/column precision falls back safely with a visible status
  message instead of being silently discarded, while the legacy file-open
  adapter keeps its compatibility behavior.

Verification passed:

- `just quick thegn-core`, `just quick thegn-svc`, `just quick thegn-host`;
- core editor 16/16, capability 18/18, MCP state 25/25;
- config example/env-overlay integration tests 5/5;
- service control/schema 35/35 (outside the restricted sandbox because its
  protocol tests bind local IPC sockets);
- host handoff 6/6, keymap 61/61, diff 14/14, PR 17/17, sidebar 12/12, help
  77/77, action registry 2/2, idle poll 3/3, QoS ratchet 1/1;
- CLI catalog/completion ratchets 2/2;
- `cargo fmt --all -- --check` and `git diff --check`.

Not verified, non-blocking:

- no real external IDE or terminal editor was launched, and no live
  control-daemon-to-compositor handoff was exercised;
- full-workspace, coverage, smoke/e2e, and cross-platform gates were not run,
  consistent with the lane design;
- `.understand-anything/knowledge-graph.json` is absent, so the optional graph
  diff overlay could not be generated;
- `main` has advanced with THE-51 i18n work and a read-only merge-tree check
  shows overlapping host action/run files. The fold/integration step should
  reconcile that work and rerun the host quick/keymap/help gates.
