# THE-17 code chunk 2 — completion summary

Verdict: PASS

Implemented:

- Added the `editor.open` write capability across the canonical capability
  catalog, control verb policy, MCP state-tool catalog, HTTP route catalog,
  gRPC surface, CLI projection, and plugin projection without adding a surface
  gap.
- Added a strict `EditorOpenRequest` wire type for a required absolute
  worktree plus optional worktree-relative path, line, and column. HTTP, gRPC,
  MCP, and the control client all validate requests through the core
  `EditorTarget` policy, including containment and coordinate-shape checks.
- Added the HTTP and gRPC control handlers and the typed control client method.
  Successful requests acknowledge only that the handoff was queued.
- Implemented the daemon control service as a mailbox boundary: it persists an
  `open_editor` intent with the validated target and source, and does not select
  an editor, construct launch argv/environment, or spawn a child process.
- Regenerated the committed control-v1 schema snapshot; its change is additive
  and includes the strict request schema and `/v1/editor/open` endpoint.

Validation:

- `just quick thegn-core` — PASS
- `just quick thegn-svc` — PASS
- `just quick thegn-host` — PASS
- `cargo nextest run -p thegn-core capability` — PASS (18 tests)
- `cargo nextest run -p thegn-core mcp::state` — PASS (25 tests)
- `cargo nextest run -p thegn-core ratchet_pins_surface_gaps` — PASS (1 test)
- `cargo nextest run -p thegn-svc control` — PASS (35 tests). The initial
  sandboxed run was denied permission to bind its loopback fixture; the same
  scoped suite passed with loopback access.
- `cargo test -p thegn-svc --test control_schema` with
  `THEGN_UPDATE_SNAPSHOTS=1` — PASS (1 test)
- `cargo nextest run -p thegn-svc control_wire_matches_the_committed_snapshot`
  — PASS (1 test)
- `cargo nextest run -p thegn-svc --features control-grpc grpc_methods_cover_catalog`
  — PASS (1 test)
- `cargo nextest run -p thegn-host cli_control_verbs_cover_catalog` — PASS (1
  test)
- `cargo nextest run -p thegn-host completion_slots_are_bound_or_pinned` — PASS
  (1 test)
- `cargo nextest run -p thegn-host open_editor_only_queues_a_safe_target_intent`
  — PASS (1 test)
- `cargo nextest run -p thegn-host mcp_scope_mapping` — PASS (5 tests)
- `cargo fmt --all -- --check` and `git diff --check` — PASS

Not verified:

- Full-workspace/heavy gates (`just test`, `just lint`, `just ci`, coverage,
  e2e) were intentionally not run under the chunk's dev-loop policy.
- The chunk-spec filters `control_schema` and `cli_control_caps` select no tests
  in the current suite. Their actual named tests passed as listed above, and
  the control schema test also passed through its dedicated test target.
- No editor process was launched; this chunk intentionally stops at the durable
  mailbox handoff.

Next hints:

- Chunk 3 should consume `open_editor` intents with the payload fields
  `worktree`, `path`, `line`, `col`, and `source`, revalidate the target, and
  perform provider selection and spawning only in the compositor.
- A queued control acknowledgment does not imply that an editor was launched;
  downstream handling should preserve that distinction in diagnostics.

Commit subject: `feat(the-17): expose editor handoff through control and MCP`
