# THE-17 code chunk 3 — completion summary

Verdict: PASS

Implemented:

- Added `ide_handoff` as the compositor's shared IDE launch edge. It accepts
  only core-validated targets, resolves the effective workspace editor on a
  utility-QoS blocking worker, revalidates filesystem targets, CPU-caps and
  reaps detached external launches, and returns pane launches and visible
  diagnostics through a woken host channel.
- Added structured-argv tab and split helpers so terminal editor plans retain
  normal pane placement without injecting shell text. Slow pane results are
  refused if their worktree is no longer focused.
- Hydrated and drain-all processed `open_editor` mailbox intents. Payloads use
  strict serde parsing, the core target containment policy, and exact known
  worktree identity; malformed, escaping, stale, and arbitrary paths are
  dropped with diagnostics.
- Added the palette-only `open-in-ide` action and action spec while preserving
  the existing terminal `editor` action. Added the selected-worktree sidebar
  menu entry, including safe dormant-row behavior.
- Added `e` IDE handoffs to worktree diffs and THE-27's anchored PR review
  projections. New-side lines carry exact coordinates; deleted/outdated rows
  fall back to file-only, top-level review rows fall back to the worktree, and
  structural diffs refuse to invent a file target.
- Routed Search & Replace, search-palette file opening, and panel Ctrl-O
  through the shared target/launch seam instead of direct environment lookup,
  command construction, or live-shell injection.
- Documented the palette, sidebar, diff, and PR entry points in the existing
  help pages without adding a default chord.

Validation:

- `just quick thegn-host` — PASS
- `cargo nextest run -p thegn-host ide_handoff` — PASS (5 tests)
- `cargo nextest run -p thegn-host keymap` — PASS (61 tests)
- `cargo nextest run -p thegn-host diff_view` — PASS (14 tests)
- `cargo nextest run -p thegn-host pr_view` — PASS (17 tests)
- `cargo nextest run -p thegn-host sidebar_keys` — PASS (12 tests)
- `cargo nextest run -p thegn-host help` — PASS (77 tests)
- `cargo nextest run -p thegn-host every_action` — PASS (2 tests)
- `cargo nextest run -p thegn-host idle_poll` — PASS (3 tests)
- `cargo nextest run -p thegn-host long_lived_threads_declare_a_qos_class` —
  PASS (1 test)
- `cargo fmt --all` and `git diff --check` — PASS

Not verified:

- Heavy/full-workspace gates (`just test`, `just lint`, `just ci`, coverage,
  and e2e) were intentionally not run under the chunk's dev-loop policy.
- No real external IDE or terminal-editor process was launched; launch
  behavior was verified through focused planning, mapping, policy, and host
  ratchet tests rather than mutating a developer desktop/session.
- A live control-daemon-to-compositor handoff was not exercised; chunk 2's
  focused mailbox tests and this chunk's strict intent-consumer tests cover
  the two sides independently.

Next hints:

- Review the single `ide_handoff` edge when changing editor placement or
  diagnostics; UI and control callers should continue passing targets, never
  provider argv.
- Keep active workspace slug derivation pure on the compositor loop; provider
  probing, filesystem access, DB access, and spawning belong off-loop.

Commit subject: `feat(the-17): add IDE handoff UI surfaces`
