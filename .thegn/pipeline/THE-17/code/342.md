# THE-17 code chunk 1 — completion summary

Verdict: PASS

Implemented:

- Extended the existing `Editor` seam with validated, worktree-contained
  `EditorTarget` values; explicit file/directory operations; honest capability
  bits; and structured argv/cwd/provider/placement launch plans.
- Added logical providers for VS Code, Cursor, Zed, JetBrains, Neovim Remote,
  and Emacs. Vendor argv and executable spellings stay in their provider
  modules. Neovim Remote explicitly rejects directory opens; JetBrains and
  Neovim Remote degrade columns to line-only according to their caps.
- Preserved the trusted custom-template and program/tool/`VISUAL`/`EDITOR`/vi
  ladder under `provider = "auto"`, with a non-empty `[editor].command` still
  highest priority and no PATH scan during launch planning.
- Added `[editor].provider`, `THEGN_EDITOR_PROVIDER`, and trusted
  `[workspace.<slug>].editor` selection with command > workspace provider >
  global provider > auto-ladder precedence.
- Registered all logical editor providers for doctor enumeration, updated the
  config enum schema ratchet, and documented the global, environment, and
  workspace keys in the example and configuration help.

Validation:

- `just quick thegn-core` — PASS
- `just quick thegn-svc` — PASS (scoped check for the doctor registry change)
- `cargo nextest run -p thegn-core editor` — PASS (15 tests)
- `cargo nextest run -p thegn-core config` — PASS (551 tests, including the
  config-example/schema checks). The first sandboxed run reached an unrelated
  local-UDP test and was denied permission; the same scoped filter passed when
  allowed to bind its test socket.
- `cargo nextest run -p thegn-core env_overlay` — PASS (8 tests)
- `git diff --check` — PASS

Not verified:

- Full-workspace/heavy gates (`just test`, `just lint`, `just ci`, coverage,
  e2e) were intentionally not run under the chunk's dev-loop policy.
- No editor executable was spawned or PATH-probed; edge launch behavior and
  installed-binary failures remain for the host handoff chunk.

Next hints:

- Chunk 2 should construct targets through `EditorTarget::new`/`file`/`project`
  so invalid wire shapes cannot bypass containment validation.
- Chunk 3 should resolve workspace overrides with `editor_for_workspace`, call
  `open_target`, and execute `EditorLaunch.argv` with `EditorLaunch.cwd`; the
  legacy `command` field remains only to keep pre-handoff host callers building.

Commit subject: `feat(the-17): extend editor seam for IDE handoff`
