# THE-17 row 367 — merge main and re-gate

Verdict: PASS

Merged `main` at `c28a507e` into `tg/the-17-ide-handoff` in merge commit
`959eec54`. All six content conflicts were independent additions and were
resolved additively:

- `thegn-core` retains both `OpenEditor` / `editor.open` and main's
  `PreviewFetch` / `preview.fetch` control and MCP registrations.
- `thegn-host/src/main.rs` retains both the IDE-handoff module and main's i18n
  surface module (plus main's already-merged preview modules).
- The service client, gRPC projection, and HTTP projection retain both editor
  handoff and preview-fetch request paths, imports, methods, and capability
  declarations.

The resolved files were inspected against both merge parents. Main's preview
and localization content remains present, and the prior THE-17 editor-handoff
and security-review fixes remain present.

Main's new required `ActionSpec::message_key` field exposed one post-conflict
`E0063` compile break for THE-17's `open-in-ide` action. Added the stable
`action-open-in-ide` key to that action and to both shipped locale catalogs,
preserving the strict locale-parity ratchet.

Validation:

- `RUSTC_WRAPPER= just quick thegn-host` — PASS after the companion fix.
- `git diff --check` and `git diff --cached --check` — PASS before commit.
- Pre-commit `treefmt`, `shellcheck`, and `yamllint` hooks — PASS (non-applicable
  checks skipped).
- `RUSTC_WRAPPER= THEGN_ALLOW_HEAVY=1 just test` — PASS: 7,363 passed,
  22 skipped, 0 failed.

The first sandboxed full-gate attempt reported two failures before fail-fast:
`dns_filter::tests::forward_query_times_out_when_no_resolver` and
`dns_filter::tests::forward_query_round_trips_against_fake_resolver`. Both
failed with OS `PermissionDenied` because the restricted sandbox forbids their
local UDP fake-resolver sockets. The exact full gate was rerun with loopback
socket permission; both tests and the complete 7,363-test suite passed.

Nothing required by this row remains unverified.
