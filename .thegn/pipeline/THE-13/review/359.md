# THE-13 security/test/bug review — dispatch 359

Verdict: **PASS — ready for the merge queue.**

## Findings and fixes

The adversarial review found and fixed four boundary and coverage gaps in commit
`77f9456e` (`fix(the-13): harden preview input boundaries`):

- Preview diagnostics passed free-form pane output through argv-only redaction,
  allowing common `Authorization: Bearer ...`, `password: ...`, and sensitive
  key/value forms to reach logs. Free-form secret masking and regression tests
  now cover these forms.
- Preview hint scanning could interpret text hidden inside unterminated OSC/DCS
  terminal control sequences, particularly after tail truncation evicted the
  sequence opener. Scanning now uses the shared stateful ANSI stripper before
  retaining pane tails, with truncated-sequence regression coverage.
- Package-script discovery opened `package.json` without first rejecting
  non-regular filesystem objects. It now accepts only regular files, preventing
  blocking or unbounded reads from device/FIFO-like targets; the failure path is
  covered.
- The network `preview.fetch` audit policy lacked transport-level coverage. A
  service test now proves a successful audited read emits the expected audit
  record.

No unresolved security, correctness, failure-path, race, or ratchet finding was
identified after those fixes.

## Verification

- Mandatory queue gate: `RUSTC_WRAPPER= THEGN_ALLOW_HEAVY=1 just test` — **7062
  passed, 22 skipped**.
- The initial exact `THEGN_ALLOW_HEAVY=1 just test` attempt died before test
  discovery with `sccache: error: Operation not permitted (os error 1)`, so the
  required wrapper-disabled retry was used.
- The first wrapper-disabled gate exposed a platform-ratchet violation in the
  review's new test. The test was made platform-neutral, the targeted ratchet
  passed, and the full gate above was rerun to green.
- Scoped preview core suite — 32 passed.
- Scoped host preview fetch suite — 4 passed.
- Scoped preview watcher and platform-ratchet suite — 3 passed.
- Scoped service preview fetch suite — 2 passed.
- Scoped service network-read audit regression — 1 passed.

The repository knowledge graph required by the diff-review helper was absent,
so a graph overlay could not be generated; changed call sites and trust
boundaries were reviewed directly. No manual interactive browser session was
performed. The automated fetch, lifecycle, rendering, configuration, audit,
and workspace-wide suites were exercised.

`thegn integrate` was intentionally not run.
