# THE-13 code chunk 2 — completion (dispatch 344)

Verdict: PASS. Implemented the event-driven preview target lifecycle and host projections specified by chunk 2.

## Implemented

- Added a loop-local, memory-only `PreviewSupervisor` with bounded per-pane diagnostic tails, deterministic source/status selection, pane EOF/exit handling, and sandbox forward lifecycle integration.
- Added one-shot off-loop `package.json`/config discovery with generation rejection, channel delivery, `TerminalWaker` pulses, bounded manifest reads, and no preview timer/idle poll.
- Wired normal PTY output/exit handling into preview discovery without changing pane damage into full chrome damage unless the selected preview projection actually changes.
- Added `PreviewView` to `FrameModel`, preserved it across hydration, and projected port/source plus honest `up`/`down`/`unknown` state into the existing Forward panel and active-worktree sidebar row.
- Preserved the existing Forward Enter/`o` URL affordances with preview-first cursor ordering.
- Documented config/package/pane discovery, lifecycle states, external open, and the optional `preview` drawer occupant contract.
- Verified the existing help ratchets without changing action ids or panel contexts.

## Validation

- `env XDG_STATE_HOME=/tmp/thegn-the13-344 just quick thegn-host` — PASS.
- `env RUSTC_WRAPPER= XDG_STATE_HOME=/tmp/thegn-the13-344 cargo nextest run -p thegn-host preview` — PASS, 32/32 (before the final bounded-state tightening); final `preview::tests` rerun PASS, 3/3.
- `env RUSTC_WRAPPER= XDG_STATE_HOME=/tmp/thegn-the13-344 cargo nextest run -p thegn-host forward` — PASS, 13/13, with permission for the existing loopback-listener fixtures.
- Targeted Forward panel projection test — PASS, 1/1.
- `env RUSTC_WRAPPER= XDG_STATE_HOME=/tmp/thegn-the13-344 cargo nextest run -p thegn-host drawer` — PASS, 15/15.
- `env RUSTC_WRAPPER= XDG_STATE_HOME=/tmp/thegn-the13-344 cargo nextest run -p thegn-host help` — PASS, 75/75, including help/action/prose/context ratchets.
- `cargo fmt --all -- --check` and `git diff --check` — PASS.

## Not verified / dependency note

- `cargo nextest run -p thegn-host panel_system` matches zero tests on this branch (nextest exit 4). The named artifact is a Muse snapshot; per chunk policy no E2E/snapshot recording was run.
- THE-11's public drawer runtime-occupant registry/context seam is not present on this branch. Per the chunk contract, preview drawer registration remains unavailable instead of introducing a second registry, pane kind, or command table. External `o` remains functional. Once THE-11 lands, register the ordinary contained PTY occupant named `preview` against `PreviewView.url`.
- Chunk 3 can consume `PreviewSupervisor::diagnostic(worktree)` for bounded pane/manifest response context and `FrameModel.preview` for the selected live target.
