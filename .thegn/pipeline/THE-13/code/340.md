# THE-13 code row 340 completion

Verdict: PASS — chunk 1 is implemented and ready for the serial chunk-2 handoff.

Commit: `feat(the-13): add preview core policy and config` (this commit).

## Implemented

- Added the `[preview]` schema, defaults, normalization/clamps, all-optional
  overlay, and atomic comma-separated port environment parsing in
  `config_preview.rs`.
- Wired `Config::preview`, `ConfigOverlay`, post-processing, schema/`--set`, and
  all five `THEGN_PREVIEW_*` overrides without adding preview policy to
  `config.rs`.
- Extended the existing substrate-free `preview.rs` with bounded ANSI/control
  stripping, explicit loopback/flag/`PORT=` detection, `scripts.dev` and
  `scripts.start` parsing, deterministic config → pane → package merge,
  live target/status values, and exact localhost/loopback fetch + redirect
  validation with explicit external opt-in.
- Documented every `[preview]` default and environment name, the localhost-only
  SSRF boundary, redirect revalidation, and that configured ports/package
  scripts never launch a process.
- Verified `test/env-overlay-ratchet.txt` remains unchanged and needs no new
  preview debt line.
- Preserved the existing forwarding configuration and `browser.drive` stub;
  neither surface was edited.

## Scoped verification

- PASS: `just quick thegn-core` (sandbox-safe temp runtime, `RUSTC_WRAPPER=`).
- PASS: `cargo nextest run -p thegn-core preview` — 31 passed.
- PASS: `cargo nextest run -p thegn-core env_overlay` — 8 passed.
- PASS: corrected example filter `cargo nextest run -p thegn-core example_config`
  — 5 passed. The chunk's literal `config_example` filter selected zero tests;
  the integration test binary is named `config_example`, while its tests are
  named `example_config_*`.
- PASS: `cargo nextest run -p thegn-core -E 'binary(env_overlay_coverage)'` —
  both shrink-only env coverage tests passed.
- PASS: `cargo fmt --all` and `git diff --check`.

## Not run / next hints

Per the chunk's dev-loop policy, no full-workspace build, `just test`,
`just lint`, `just ci`, coverage, or E2E was run. Chunk 2 can consume
`PreviewConfig`, `PortHint`/`PortHintSource`, `parse_pane_port_hints`,
`parse_package_script_hints`, `merge_port_hints`, `PreviewTarget`,
`PreviewStatus`, and `select_target`. Chunk 3 can consume
`validate_preview_url` and `validate_preview_redirect`; relative redirect
locations must be resolved to an absolute URL by the host before revalidation.
