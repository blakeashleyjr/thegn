# THE-13 code chunk 3 — completion (dispatch 349)

Verdict: PASS. Implemented the read-scoped `preview.fetch` capability and its bounded control projections specified by chunk 3.

## Implemented

- Added exactly one `preview.fetch` catalog row and one `PreviewFetch` verb mapped to `Scope::Read`; retained `browser.drive` unchanged as the sole compatibility stub.
- Added additive HTTP, gRPC/protobuf, typed control-client, generic CLI/plugin route, and read-scoped MCP projections with mirrored route/capability tables.
- Added bounded request/reply wire types and regenerated the deterministic `docs/api/control-v1.json` snapshot with only the new route/types added.
- Added a host-owned GET-only `reqwest` executor with no proxy, cookies, auth, browser profile, or shell command; it applies the core loopback policy to the initial URL and every manual redirect.
- Enforced whole-call timeout, redirect count/URL, request, content-type, response body, and diagnostic line/count bounds; preserved HTTP status as response data and classified invalid input, precondition, limit, timeout, and transport failures through stable control errors.
- Bridged chunk 2's bounded same-process diagnostic snapshot into the response, returning redacted dev-server-pane error lines only when requested and honestly reporting `unavailable` when no compositor snapshot exists.
- Made the HTTP handler authenticate through the catalog scope before decoding its bounded request body, and routed MCP calls through the typed control client.
- Left `test/surface-gaps-ratchet.txt`, `test/completion-slot-ratchet.txt`, and `test/env-overlay-ratchet.txt` unchanged; coverage tests confirm no `preview.fetch` surface gap.

## Validation

- `just quick thegn-core` — PASS.
- `cargo nextest run -p thegn-core capability` — PASS, 18/18.
- `cargo nextest run -p thegn-core mcp::state` — PASS, 24/24.
- `just quick thegn-svc` — PASS.
- `cargo nextest run -p thegn-svc control::tests` — PASS, 16/16.
- `cargo nextest run -p thegn-svc control::routes` — PASS, 5/5.
- `cargo nextest run -p thegn-svc --features control-grpc control::grpc` — PASS, 2/2.
- Snapshot regeneration with `THEGN_UPDATE_SNAPSHOTS=1 cargo test -p thegn-svc --test control_schema`, followed by `cargo nextest run -p thegn-svc --test control_schema` — PASS, 1/1.
- `just quick thegn-host` — PASS.
- `cargo nextest run -p thegn-host preview_fetch` with isolated `XDG_STATE_HOME` and loopback permission — PASS, 4/4.
- `cargo nextest run -p thegn-host mcp` with isolated `XDG_STATE_HOME` — PASS, 5/5.
- `cargo fmt --all -- --check` and `git diff --check` — PASS.

## Not verified / next hints

- The broad requested substring command `cargo nextest run -p thegn-svc control` compiled but was interrupted by the pre-existing `control::client::tests::attach_refuses_an_incompatible_daemon_proto` fixture failing to create its socket under the restricted sandbox (`Operation not permitted`). The changed router, route table, schema, and feature-gated gRPC modules were rerun directly and all passed as listed above.
- Per the dev-loop policy, no full-workspace test, coverage, CI, or E2E gate was run.
- A detached daemon has no compositor-owned pane snapshot by design, so `include_console` returns an empty list with `diagnostics_source: "unavailable"`; same-process deployments consume the bounded chunk-2 snapshot.
