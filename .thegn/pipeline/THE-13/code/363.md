# THE-13 row 363 — merge main and re-gate

Verdict: PASS

Merged `main` at `56270f8e` into `tg/the-13-browser-preview` and resolved all
five forecast conflicts by preserving both independent feature sets:

- `daemon/service.rs`, `control/grpc.rs`, and `control/http.rs` retain THE-13
  preview-fetch request/reply projections alongside main's session-fork
  projections.
- `run.rs` retains preview supervisor refresh and main's drawer runtime mirror
  synchronization.
- `sidebar_view.rs` retains preview rendering while accepting main's merge-queue
  width and hit-geometry arguments, including companion updates to main's new
  tests.

The resolved files were inspected against both merge parents; main's fork,
drawer, and merge-queue additions remain present, and the prior THE-13 preview
and security-review fixes remain present.

Validation:

- `git diff --cached --check` — PASS.
- `RUSTC_WRAPPER= THEGN_ALLOW_HEAVY=1 just test` — PASS: 7,338 passed,
  22 skipped, 0 failed.

An initial `just quick thegn-host` compile was interrupted after it remained
quiet while waiting/building; it produced no diagnostics. The required full
gate subsequently compiled the entire merged workspace and passed, so nothing
required by this row remains unverified.
