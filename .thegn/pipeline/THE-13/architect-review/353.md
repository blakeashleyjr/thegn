# THE-13 architect review — dispatch 353

Verdict: APPROVED

No revision chunks are required.

## Review scope

- Diffed `tg/the-13-browser-preview` from its `origin/main` merge base
  (`bbeaa606`) and isolated the THE-13 design plus three implementation commits
  from the inherited THE-4 branch history.
- Checked the implementation against the architecture in
  `.thegn/pipeline/THE-13/architect/design.md`, all three chunk specifications,
  and the completion report in `.thegn/pipeline/THE-13/code/349.md`.
- Reviewed core detection/config/URL policy, event-loop and render-damage
  wiring, preview target lifecycle, Forward/sidebar projections, catalog and
  scope mappings, HTTP/gRPC/CLI/MCP/plugin projections, schema synchronization,
  and the host fetcher's SSRF, redirect, credential, timeout, body, and
  diagnostic bounds.
- Checked repository standards in `CLAUDE.md`, `CONTRIBUTING.md`, and the
  relevant architecture/help/config/ratchet surfaces.

## Architect correction

The implementation made `preview.fetch` read-scoped as designed, but the
existing control audit predicate excluded all read-scoped calls. I corrected
that narrow security-contract gap so this network-reaching read operation is
audited by both control adapters, with a focused core assertion. Committed as
`f102268c` (`fix(the-13): audit preview fetch calls`).

## Verification

Passed:

- `just quick thegn-core`, `just quick thegn-svc`, and `just quick thegn-host`.
- Core preview/config policy tests (31), catalog/MCP/env tests (50), config
  example and env-overlay ratchets (4), and the corrected control-audit tests
  (3).
- Service HTTP/control tests (16), route/catalog mirroring (5), deterministic
  control schema (1), feature-gated gRPC mapping (2), and focused post-fix
  preview-fetch adapter tests (2).
- Host preview lifecycle/watch tests (4), bounded fetch tests (4), Forward
  projection test (1), MCP scope mapping (5), and exact Forward supervisor
  lifecycle tests (3).
- `cargo fmt --all -- --check` and `git diff --check origin/main...HEAD`.

The loopback-listener tests initially failed only because the review sandbox
forbids socket binding; the exact fetch and Forward tests passed after rerunning
with permission for test-only loopback listeners.

## Notes / not independently verified

- No full-workspace, coverage, CI, or Muse E2E gate was run, consistent with
  the design's scoped-review policy. The visible panel/sidebar delta was
  reviewed in code and through its focused projection test.
- THE-11's drawer runtime-occupant registry is not present on this branch. The
  implementation correctly leaves the optional terminal-browser occupant
  unavailable and preserves external `o`, as the chunk contract directs; the
  adapter remains follow-up work after THE-11 lands.
- `.understand-anything/knowledge-graph.json` is absent, so the optional
  knowledge-graph blast-radius overlay could not be produced; dependencies and
  call sites were reviewed directly instead.

The change is correct and safe for the designed scope. The remaining items are
dependency/verification notes, not reasons for another coder round.
