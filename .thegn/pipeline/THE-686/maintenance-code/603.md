# THE-686 implementation handoff — dispatch 603

Verdict: implementation-ready

Implemented the primary-approved plan completely within scope. No concrete
design blocker was found. No child dispatch, merge, push, tracker closure, row
status change, main-branch edit, or new feature was performed.

## Implementation

- Preserved the existing bounded `git status --porcelain=v1 -z
--ignored=matching` probe and added fail-closed parsing. Empty status is
  clean; one or more well-formed `!!` records are ignored-only; tracked,
  untracked, malformed, and unknown records remain `Refusal::Dirty`.
- Stored the raw bounded status bytes in `Verified` and compared a fresh
  observation during final revalidation. A changed admissible snapshot returns
  typed `Refusal::Changed`, so a newly appearing ignored entry cannot be
  silently removed.
- Kept the 2 MiB bounded Git capture conservative: hitting the bound still
  refuses cleanup.
- Propagated ignored-only admission through cleanup and sweep reporting as
  `discarded build state`; clean removals remain ordinary sweeps.
- Distinguished admission-time edits (`edited since landing`) from a snapshot
  changing during cleanup (`changed during cleanup`) in lifecycle, CLI,
  background, and in-app summary reporting.
- Left `force` as a TTL-clock bypass only; tracked and non-ignored untracked
  work remains retained under both force modes.

## Regression coverage

- Added direct status-observation coverage for empty, ignored-only, tracked,
  untracked, malformed, and unknown records.
- Revised the cleanup admission regression so ignored-only state is admissible
  while tracked and untracked state remains refused; filter-driver, hidden-index,
  replacement-directory, and other safety guards remain covered.
- Updated final validation to require `Refusal::Changed` for a newly appearing
  ignored file.
- Added the landed-worktree matrix for clean, ignored-only, tracked-modified,
  and untracked-non-ignored states under both expired `force = false` and
  `force = true`, asserting physical removal/retention, queue/cache behavior,
  and report categories.
- Added fresh-row grace-period coverage proving force bypasses only the clock.

## Changed files

- `crates/thegn-host/src/merge_cleanup.rs`
- `crates/thegn-host/src/merge_cleanup_tests.rs`
- `crates/thegn-host/src/merge_lifecycle.rs`
- `crates/thegn-host/src/merge_sweep.rs`
- `crates/thegn-host/src/cmd/merge.rs`
- `crates/thegn-host/src/handlers/merge_queue.rs`

## Validation performed

```text
rustfmt --edition 2024 crates/thegn-host/src/merge_cleanup.rs crates/thegn-host/src/merge_cleanup_tests.rs crates/thegn-host/src/merge_lifecycle.rs crates/thegn-host/src/merge_sweep.rs crates/thegn-host/src/cmd/merge.rs crates/thegn-host/src/handlers/merge_queue.rs
rustfmt --edition 2024 --check crates/thegn-host/src/merge_cleanup.rs crates/thegn-host/src/merge_cleanup_tests.rs crates/thegn-host/src/merge_lifecycle.rs crates/thegn-host/src/merge_sweep.rs crates/thegn-host/src/cmd/merge.rs crates/thegn-host/src/handlers/merge_queue.rs
git diff --check
```

All commands above completed successfully. No Cargo, build, nextest, Clippy,
or full test-suite command was run, as required by the dispatch CPU constraint.
No full gate result is claimed; Rust compilation and test behavior remain
unverified by this dispatch.

## Exact commands requiring primary execution

```text
nix develop --command cargo check -p thegn-host --all-targets
nix develop --command cargo nextest run -p thegn-host merge_cleanup
```

The primary must record the actual outcomes, including any `/nix/store`
read-only sandbox failure, rather than treating these commands as passed here.
