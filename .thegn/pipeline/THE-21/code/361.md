# THE-21 code chunk 2 completion — dispatch 361

Verdict: complete.

Implemented the host runtime, control surfaces, CLI, and visibility requested by chunk 2:

- Added one canonical notification/event seam and migrated every scoped producer while preserving append versus emit-once behavior. Normalized notification, session attention/done/failure/exit, PR/merge, disk, and idle facts now feed automation exactly once at their producer edges.
- Added a bounded off-loop runtime with a bounded channel, concurrency semaphore, per-action timeout, blocking SQLite isolation, persisted rule transitions, skipped/planned/terminal audit outcomes, retention pruning, and structured rule/event/cap/run/outcome logs.
- Routed `sessions.open`, `merge.add`, `notify.push`, and configured `tools.run` through cataloged control behavior. Generated sessions, notifications, and delayed idle facts carry wire-safe automation ancestry so core loop suppression remains structural while results stay visible as `automation` / `automation_failed` inbox rows.
- Added `thegn automations list|test` with JSON output, fixture/inline-event support, an injected clock, and a pure dry-run path that neither executes nor opens a store.
- Added HTTP, gRPC, MCP-state, client, generic API-call, capability, scope, and schema coverage for `automations.list`, `automations.test`, and `tools.run`. The committed control-v1 schema was regenerated additively.
- Added automation rule completion from trusted config, classified every new CLI value slot, grouped CLI help, and a registered Automations help page. Existing env-overlay and surface-gap ratchets gained no debt.

Verification:

- `just quick thegn-core` — passed.
- `just quick thegn-svc` — passed.
- `just quick thegn-host` — passed.
- `cargo nextest run -p thegn-core env_overlay` — 8 passed.
- `cargo nextest run -p thegn-svc --test control_schema` — passed against the regenerated snapshot.
- Focused core catalog/completion/surface/MCP/plugin checks — 11 passed.
- Focused service route/scope/serde checks — 4 passed.
- `cargo nextest run -p thegn-host automations` — passed; the final combined dry-run/idempotence/runtime/completion/notify check ran 5 passed.
- `cargo nextest run -p thegn-host notification` — 10 passed.
- Help registry/page-registration checks — 2 passed.
- `cargo fmt --all` and `git diff --check` — clean.

Could not verify the real-socket portions of the broad `control` and `daemon` filters inside this managed sandbox. `cargo nextest run -p thegn-svc control` stopped at `attach_refuses_an_incompatible_daemon_proto`, and `cargo nextest run -p thegn-host daemon` ran 51 passing tests before `ws_warm_attach_pipeline_over_a_real_socket` failed; both failures were `Operation not permitted` while creating/binding a local socket. The socket-independent catalog, route, scope, wire, daemon-notification, and automation checks passed. No full-workspace, CI, coverage, e2e, or live-state command was run.

Next hints:

- Re-run the two real-socket control/daemon filters in an environment that permits local Unix/TCP/WebSocket sockets.
- Exercise one enabled rule per action against a disposable daemon/worktree to validate external program/forge behavior without touching production state.
