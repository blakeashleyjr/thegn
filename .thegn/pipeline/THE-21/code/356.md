# THE-21 code chunk 1 completion — dispatch 356

Verdict: complete.

Implemented the substrate-free automation foundation requested by chunk 1:

- Added normalized automation events, typed predicates, catalog action templates/plans, origin loop suppression, injected-time evaluation, debounce, once-per-key, sliding rule/action rate limits, deterministic ordering, state transitions, and explicit skip reasons.
- Added trusted `[automations]` / `[[automations.rules]]` configuration with safe disabled defaults, active-profile overlays, strict event/action/parameter/template/glob/regex/bounds validation, and documented every key in `config/config.toml.example`.
- Added detection and named warnings for repo `.thegn.toml`, `.thegn.yaml`, and `.thegn.json` automation content without adding automation data to `RepoConfigFile` or effective repo config.
- Added object-safe automation state/audit store types and the SQLite implementation, additive schema v62 tables/indexes, schema verification, newest-first bounded queries, and per-rule retention pruning. No live database or built binary was opened/migrated.
- Added `automation` and `automation_failed` notification kinds with exhaustive ids, priorities, glyphs, labels, counts, and config prose.

Verification:

- `just quick thegn-core` — passed.
- `cargo nextest run -p thegn-core automation` — 21 passed.
- `cargo nextest run -p thegn-core notification_kind` — 3 passed.
- Additional focused `cargo nextest run -p thegn-core 'notification::tests'` — 11 passed, including exhaustive count/priority/config-prose tests.
- `cargo fmt --all -- --check` / `git diff --check` — clean after formatting.

Test environment note: direct `cargo nextest` initially could not start the configured sccache daemon in the sandbox (`Operation not permitted`), so the same scoped commands were run with `RUSTC_WRAPPER=`. No full-workspace, coverage, CI, e2e, host/service, live-state, or migration command was run, per the chunk and dev-loop policy.

Next hints for chunk 2:

- Use `Config::effective_automations()` and surface `Config::repo_automation_warnings()` alongside the existing repo command-collector warning path.
- Resolve/register the four validated action ids (`sessions.open`, `merge.add`, `notify.push`, `tools.run`) through the one capability catalog; do not add a generic invoke/command escape hatch.
- Convert `AutomationStateRow` fields into the pure `EvaluationState`, persist each returned `StateTransition`, and use `AutomationStore` for pre-dispatch and terminal audit updates plus configured retention.
- Preserve `AutomationOrigin` through notifications/sessions so core loop suppression remains structural.
