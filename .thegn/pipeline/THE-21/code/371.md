# THE-21 code completion — dispatch 371

Verdict: complete.

Implementation commit: `462d6752` (`feat(the-21): make automation runtime durable and truthful`).

Implemented the architect revision against current main (`6eff1d66`):

- preserved main's v62 session-fork and v63 PR-review-cache migrations and moved automation state/audit to verified schema v64;
- normalized routed notification events with typed notification kind, final routed priority, unique inserted-row identity, drop behavior, and append/emit-once semantics;
- added authoritative typed PR checks/review edges, live workspace/repo/branch/dispatch-role enrichment, independent validated idle duration, and targeted idle deadlines;
- made evaluation, accepted throttle transition, and started audit atomic under `BEGIN IMMEDIATE`, with cross-process race coverage, action-wide disjoint rate ledgers, bounded windows/once keys, and durable non-blocking overflow audit;
- moved SQLite/control discovery off async workers, added explicit transient runtime drain, and made `automations test` store-free before common DB setup;
- made list output explain daemon-disabled/unavailable inert rules in both human and JSON projections;
- enforced strict configured `[[tools]]` versus `[[agents]]` identity, killed timed-out tool sessions, emitted origin-tagged success/failure outcomes, and carried merge ancestry through a durable single-use handoff;
- added real router/scope/catalog, producer/runtime, process, migration, and action-identity regression coverage.

Verification (all passed, each state-touching command used an isolated temporary `XDG_STATE_HOME`):

- `just quick thegn-core`
- `just quick thegn-svc`
- `just quick thegn-host`
- `cargo nextest run -p thegn-core automation` (24/24)
- `cargo nextest run -p thegn-core env_overlay` (8/8)
- core `migration` (23/23), `ladder` (25/25), `pre_v6` (4/4), `newer` (4/4), and `v64_schema` (1/1) filters
- `cargo nextest run -p thegn-svc --test control_schema` (1/1)
- focused three-verb control catalog/route/scope test (1/1)
- focused strict tool/agent registry test (1/1)
- host `automation` filter (8/8), merge-origin handoff test (1/1), and `notification` filter (18/18, including both isolated process tests)
- `cargo fmt --all -- --check`
- `git diff --check` and `git diff --cached --check`

Not verified: real-socket control/daemon filters were not run in this restricted workspace environment. The in-process HTTP router, control schema, service compile/quick checks, and daemon resolver unit coverage passed.

Next hints: review the merge resolution and the canonical notification fallback configuration in `automation_events.rs`; then run the real Unix/TCP/WebSocket filters on a socket-capable host.
