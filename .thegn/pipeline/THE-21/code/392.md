# THE-21 code handoff — row 392

## Verdict

BLOCKED — the merge resolution is present in the worktree from row 391, but
this environment cannot write the shared Git metadata, so the required merge
commit, tracked handoff, and green full gate cannot be completed.

## Work completed

- Read `CLAUDE.md` and `merge-main-391.md`.
- Confirmed `git merge-base --is-ancestor main HEAD` fails as required.
- Attempted the required `git merge main`; Git stopped before changing the
  worktree because it could not create
  `/home/blake/code/thegn/.git/worktrees/tg-the-21-automations/ORIG_HEAD.lock`.
- Confirmed the preserved row-391 tree retains main's embedded-skills
  additions in both `crates/thegn-core/src/completion/catalog.rs` and
  `crates/thegn-core/src/control.rs`, including `Skill`, `skills show`,
  `SkillsList`, and `SkillsSeed`.
- Inspected the three-way merge preview; the current tree has no conflict
  markers and remains clean before this handoff artifact.

## Verification

- `RUSTC_WRAPPER= THEGN_ALLOW_HEAVY=1 just test` — attempted as required;
  compilation remained silent for several minutes after starting
  `thegn-core`/`thegn-host`, so it was interrupted with exit `130`. No test
  failure names or test results were emitted.
- `cargo fmt --all -- --check` — PASS.
- `git diff --check` — PASS.
- Git metadata probes — FAIL: the shared `.git` mount is read-only, including
  index-lock and branch-ref-lock creation.

## Blocking condition / next hint

The worktree is writable, but `/home/blake/code/thegn/.git` is mounted
read-only by the execution sandbox. `git update-index --refresh` failed while
creating `index.lock`, and a no-op `git update-ref` failed while creating the
branch ref lock. After the shared Git metadata is remounted writable, rerun
`git merge main`, inspect/resolve any conflicts, run the mandated full gate to
completion, stage this file, commit the merge and handoff, then run the live
`thegn dispatch report 392 --text ...` command.

The required live report was attempted with the installed `thegn` binary (it
does not provide the `dispatch` subcommand) and with `target/debug/thegn` (it
reached the live state store but failed with `attempt to write a readonly
database`).
