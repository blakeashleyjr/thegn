# THE-21 code handoff — row 391

## Verdict

BLOCKED — the merge-equivalent worktree is prepared, but this environment cannot
write the shared Git metadata, so the required merge commit and tracked handoff
cannot be completed.

## Work completed

- Read `CLAUDE.md` and `merge-main-379.md`.
- Reconstructed the `git merge main` result from the merge base and resolved
  every reported conflict while retaining both independent additions:
  automation and skills completion/control entries, plus the main branch's
  tempfile-based sandbox isolation.
- Preserved main's THE-20 skill files and all related registry, config, CLI,
  host, service, documentation, and schema changes.
- Removed all merge conflict markers.

## Verification

- `cargo fmt --all -- --check` — PASS.
- `git diff --check` — PASS.
- `RUSTC_WRAPPER= THEGN_ALLOW_HEAVY=1 just test` — attempted twice as required;
  both runs stalled during compilation without emitting test results and were
  interrupted (`130`). No test failure names were emitted.
- `RUSTC_WRAPPER= XDG_RUNTIME_DIR=/tmp TMPDIR=/tmp just quick thegn-core` —
  attempted after the full gate; it also stalled during compilation and was
  interrupted (`130`) without diagnostics. The default quick invocation was
  additionally blocked by the read-only runtime temp path, and sccache was
  unavailable under the sandbox.

## Blocking condition / next hint

`git merge main` failed before changing the worktree because Git could not
create `/home/blake/code/thegn/.git/worktrees/tg-the-21-automations/ORIG_HEAD.lock`;
`git add -A` likewise failed on `index.lock`. `/home` is mounted read-only.
Remount the shared Git metadata writable, then run the real merge, stage the
prepared result including this file, commit it, and rerun the live dispatch
report for row 391. The required report was attempted with both the installed
and repository-built `thegn` binaries; the installed binary lacks `dispatch`,
and the repository-built binary reached the live state store but failed with
`attempt to write a readonly database`.
