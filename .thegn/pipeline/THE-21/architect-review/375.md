# THE-21 architect review — dispatch 375

Verdict: **APPROVED**

## Review basis

Reviewed the branch against merge-base `6eff1d66` (`main` at the design
baseline), `.thegn/pipeline/THE-21/architect/design.md`, the chunk specs and
completion report, repository contribution standards, the v62/v63/v64
migration ladder, core automation/config/store code, host producers/runtime/
executor, daemon/control/catalog surfaces, and the notification routing seam.

The knowledge-graph overlay could not be generated because
`.understand-anything/knowledge-graph.json` is absent; dependency and blast
radius checks were performed directly from source and call sites.

## Corrections applied

- Queue and PR queue `MergeLanded` facts are now submitted even when the
  human-facing queue notification is dropped by routing. Typed automation facts
  are no longer coupled to inbox visibility.
- Queue-overflow audit work remains counted by transient drain until its durable
  `dropped` row has been written; a failed audit-thread spawn also releases the
  pending count.

## Verification

Passed:

- `cargo nextest run -p thegn-core automation` — 24/24.
- `cargo nextest run -p thegn-host automation` — 9/9, including process dry-run
  isolation and overflow audit coverage.
- `cargo nextest run -p thegn-svc --test control_schema` — 1/1.
- `cargo fmt --all -- --check` and `git diff --check`.

The migration reconciliation is additive at schema v64 and preserves main's
v62/v63 tables. Catalog/control snapshots, trusted tool-vs-agent identity,
typed PR facts, bounded state/audit, cross-process admission, origin
suppression, idle deadlines, and one-shot dry-run behavior are covered by the
focused implementation tests.

## Notes / limits

Real Unix/TCP/WebSocket socket paths, live external tool/provider actions, and
full CI/e2e were not run in this restricted workspace. The existing code report
also records those limits. Session ancestry is carried through the daemon's
automation-origin handoff; a future hardening pass could make that handoff
durable across daemon restarts, but it is not a reason for another full build
round for this change.

The two local corrections could not be committed because the linked worktree's
Git index is outside the writable workspace; `git commit` failed with
`Read-only file system` while creating
`/home/blake/code/thegn/.git/worktrees/tg-the-21-automations/index.lock`.

The live `thegn dispatch report 375` attempt also could not update the roster:
the installed binary was stale, and the current workspace binary reached the
live state but failed with SQLite `attempt to write a readonly database`.
