PASS

Ready for the merge queue. Adversarial review found and fixed:

- bounded automation message regexes to prevent oversized-pattern resource exhaustion;
- stopped daemon-event subscription cleanly on a closed broadcast channel;
- registered automation session ancestry before spawn and cleaned it up on spawn failure;
- cleared persisted merge ancestry when client discovery or merge dispatch fails;
- required tools.run to report both a matched execution and exit_code 0;
- logged queue-overflow audit persistence failures and avoided installing a dead runtime sender;
- made local OCI/systemd secret env-file failure fail closed instead of putting secrets on argv;
- repaired config-example, env-overlay, capability-scope, and MCP scope ratchets.

Verification:

- `cargo fmt --all` and `git diff --check` pass.
- Focused automation suites: thegn-core 25/25 and thegn-host 9/9; MCP scope/control checks pass.
- Queue-boundary gate: `RUSTC_WRAPPER= cargo nextest run --workspace` with the exclusions below: 7,329 passed, 93 skipped.
- The initial `THEGN_ALLOW_HEAVY=1 just test` hit the documented sccache sandbox denial; the wrapper-disabled retry reached the suite and exposed the fixed ratchets before the green filtered gate.

Unverified only because this sandbox denies socket/listener creation:

- thegn-core DNS: `dns_filter::tests::forward_query_*`, `server_end_to_end_singleton_and_events`, `server_forwards_allowed_query_to_configured_upstream`; `sandbox_dns_e2e` a1-a4.
- thegn-core `ssh_creds::tests::unix_socket_alive_probes_connectability`.
- thegn-host bridge/daemon/forward/preview fixtures: `bridge_sup::tests::connect_registers_routes_and_forwards_then_disconnect`, `daemon::service::tests::ws_warm_attach_pipeline_over_a_real_socket`, forward remap/start/stop trio, the worktree-attach silent-daemon probe, and the four `preview_fetch` loopback tests.
- thegn-svc bridge cluster: the 18 connection/exec/fs-watch/proc tests named by `bridge::tests::*` in the gate exclusion; control incompatible-proto attach; Fly 4xx teardown; the eight `host::cloud` mock-server tests; two IPC bind tests; two iroh reach tests; five revtunnel tests; `create_list_destroy_round_trip_with_ledger` in vps/vps-do mocks; and the 12 sprites mock tests.

Those tests were each rerun in isolation and failed at socket setup with `PermissionDenied (Operation not permitted)`; the runnable remainder was green. The understand-diff knowledge graph was unavailable, so review used direct diff/code inspection. `thegn integrate` was not run.

Handoff limitation: committing this artifact and the fixes was attempted, but
the linked worktree's shared Git metadata under `/home/blake/code/thegn/.git`
is read-only in this environment (`index.lock`, commit metadata, and object
writes fail). The live dispatch report was also attempted and failed with
`attempt to write a readonly database`.
