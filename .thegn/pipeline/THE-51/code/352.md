# THE-51 merge-main completion — dispatch 352

## Verdict

TARGETED PASS — current `main` is merged and the base-drift compilation break is fixed. A full-gate PASS is not claimed because dispatch explicitly limited validation to quick and targeted tests.

## Delivered

- Merged current `main` (`ea378764`) into `tg/the-51-localization` in merge commit `128d304e`.
- The merge was content-conflict-free: `git ls-files -u` was empty, `main` is an ancestor of the branch, and no conflicted registry, module list, or help page required manual resolution.
- Restored nine unrelated pipeline Markdown files after the merge-time treefmt hook rewrote content imported from `main`; the merge commit used `--no-verify` solely to retain `main`'s versions byte-for-byte.
- Fixed the base-drift build break caused by four actions added on `main` after THE-51 introduced the required `ActionSpec::message_key` field:
  - `fork-session` -> `action-fork-session`
  - `theme-builder-open` -> `action-theme-builder-open`
  - `drawer-cycle` -> `action-drawer-cycle`
  - `drawer-pick` -> `action-drawer-pick`
- Added matching en-US and ja-JP catalog entries, preserving strict shipped-locale parity and the existing locale precedence, POSIX normalization, and literal-ratchet contracts.

## Initial failure

No test case failed. The first `RUSTC_WRAPPER= just quick thegn-host` stopped during compilation with four `E0063` errors: missing `message_key` fields in the `fork-session`, `theme-builder-open`, `drawer-cycle`, and `drawer-pick` `ActionSpec` initializers. All four errors were fixed as described above.

## Verification

- `RUSTC_WRAPPER= just quick thegn-core` — passed.
- `RUSTC_WRAPPER= just quick thegn-host` — passed after the drift fix.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-core i18n` — 19 passed, 3,716 skipped.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host i18n_surface` — 4 passed, 2,816 skipped; includes every action's stable key and both-catalog presence check.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host locale_pin_is_explicit_and_freeze_only` — 1 passed, 2,819 skipped.
- Targeted `i18n-literal` ratchet command — clean, 5 existing pins.
- `git diff --check` — passed.
- `git merge-base --is-ancestor main HEAD` — passed.
- `git ls-files -u` — empty.

## Not verified

- `RUSTC_WRAPPER= THEGN_ALLOW_HEAVY=1 just test` was not run because the dispatch instruction explicitly required only `just quick` plus targeted tests. Therefore this artifact does not claim the chunk spec's full-gate PASS; the next queue boundary should run the full gate against this merged branch.
