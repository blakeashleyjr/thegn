# Row 337 completion — THE-51 chunk 2

Verdict: implemented the bounded statusbar and command-palette localization proof surface.

## Delivered

- Expanded the embedded en-US catalog for the complete bounded surface and added a strict-parity ja-JP proof locale with representative Japanese statusbar, palette, and action translations.
- Registered ja-JP in the single shipped-locale source table.
- Added `i18n_surface.rs` as the typed host adapter for palette labels, status labels, mode variants, interpolation, plural selection, and registered action ids.
- Added stable Fluent message keys to all 137 `ActionSpec` rows while retaining canonical English `label` values for generated help.
- Routed palette title/badge/filter/footer/count/action/new-terminal/folder labels and statusbar offline/LOC/free/mode/state/prompt labels through the catalog without translating user data, custom/plugin labels, chords, numeric ranges, or glyphs.
- Added Japanese cell-width tests for palette rows/footer hints and statusbar mode/keyhint fitting, plus adapter tests for action-key parity, translated labels, plural counts, and unknown-action fallback.
- Kept the i18n literal ratchet shrink-only and documented why its remaining file pins include Rust `matches!` syntax and canonical generated-help labels.

## Verification

- `just quick thegn-core` — passed
- `cargo nextest run -p thegn-core i18n` — 17 passed
- `just quick thegn-host` — passed
- `cargo nextest run -p thegn-host palette` — 47 passed
- `cargo nextest run -p thegn-host statusbar` — 41 passed (rerun after final helper cleanup: 41 passed)
- `cargo nextest run -p thegn-host chrome` — 89 passed
- `cargo nextest run -p thegn-host help` — 75 passed
- `cargo nextest run -p thegn-host i18n_surface japanese_labels` — 5 passed
- Required `bash test/ratchet.sh i18n-literal ...` invocation — clean (5 pinned)
- `cargo fmt --all -- --check` and `git diff --check` — passed

Per the dev-loop policy, full-workspace `just test`, `just lint`, `just coverage`, `just ci`, e2e, and full-workspace compilation were not run.
