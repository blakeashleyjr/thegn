# Dispatch 332 completion — THE-51 chunk 1

Verdict: complete. Implemented the embedded localization substrate exactly
within chunk 1's file scope.

## What changed

- Reworked `i18n` into a thin facade over a pure locale resolver with explicit
  config → `LC_ALL` → `LANG` → `en-US` precedence, diagnostic fallback, e2e
  freeze precedence, developer-only pseudolocale selection, and once-only
  startup installation.
- Added a single explicit `include_str!` shipped-locale source registry and a
  deterministic strict parity fold that reports every locale/key orphan and
  missing default key.
- Added a Fluent text-only pseudolocale transform that leaves keys, selectors,
  placeholders, and caller arguments intact while producing non-ASCII,
  cell-width-expanded message values.
- Added pure plural-category, integer, and short-date helpers for English,
  Japanese, and safe invalid-locale fallback.
- Pinned the e2e locale to `en-US` before startup initialization and documented
  restart-only, TUI-only locale selection precedence in the example config.
- Added and wired the shrink-only five-file raw i18n literal ratchet without
  changing env-overlay, completion, control-schema, or help allowlists.

## Verification

- `just quick thegn-core` — passed (final run).
- `cargo nextest run -p thegn-core i18n` — 17 passed (final run).
- `cargo nextest run -p thegn-core env_overlay` — 8 passed.
- `cargo nextest run -p thegn-core i18n_format` — 3 passed.
- `just quick thegn-host` — passed (final run, 2m56s).
- `cargo nextest run -p thegn-host e2e_freeze` — 4 passed. The cold test-profile
  build was interrupted at the per-invocation ten-minute ceiling; the retained
  artifact retry completed within its own ceiling and all filtered tests passed.
- `cargo nextest run -p thegn-host help` — 75 passed.
- Required `bash test/ratchet.sh i18n-literal ...` invocation — clean, 5 pinned.
- `cargo fmt --all -- --check` and `git diff --check` — passed.

Per the chunk's dev-loop policy, full workspace tests, CI, coverage, build, and
e2e were intentionally not run. No binary invocation was needed.

Commit subject: `feat(i18n): harden embedded locale substrate`
