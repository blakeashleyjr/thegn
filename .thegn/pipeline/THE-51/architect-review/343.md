# Architect review 343 — THE-51

APPROVED

No revision chunks are required.

## Scope reviewed

- Compared `tg/the-51-localization` with its local `main` merge base,
  `76d85c9c`, including the design/chunk commit and both implementation
  commits.
- Checked the implementation against the row 337 completion record, the
  authoritative adjacent architecture in `architect/design.md`, both chunk
  specifications, `CLAUDE.md`, `CONTRIBUTING.md`, and
  `docs/ARCHITECTURE.md`.
- Audited locale precedence and startup-only initialization, the e2e and
  pseudolocale precedence, strict embedded-catalog parity, Fluent
  interpolation, action-key coverage, palette/statusbar routing, preservation
  of user data and generated English help, cell-width fitting, capability-glyph
  seams, ratchet scope, and unchanged config/help/control/completion contracts.

## Architect correction

- Commit `d17d23cd` removes the now-unused `sys-locale` direct dependency and
  lockfile entry. The pure environment-snapshot resolver supersedes it, and
  leaving it in place would violate the repository's unused-dependency gate.

## Verification

- `cargo nextest run -p thegn-core i18n` — 17 passed.
- `cargo nextest run -p thegn-core env_overlay` — 8 passed.
- `cargo nextest run -p thegn-host i18n_surface` — 4 passed.
- `cargo nextest run -p thegn-host palette` — 47 passed.
- `cargo nextest run -p thegn-host statusbar` — 41 passed.
- `cargo nextest run -p thegn-host chrome` — 89 passed.
- `cargo nextest run -p thegn-host help` — 75 passed.
- `cargo nextest run -p thegn-host e2e_freeze` — 4 passed.
- `just quick thegn-core` and `just quick thegn-host` — passed with the sandbox
  cache wrapper disabled.
- Required i18n literal ratchet — clean with five intentional file pins.
- `cargo fmt --all -- --check` and `git diff --check` — passed.
- `cargo check -p thegn-core` — passed after the dependency cleanup.

## Notes / unverified

- `cargo machete` was not installed in this execution environment, so the full
  dependency-audit command was not runnable; direct search confirmed that no
  `sys-locale`/`sys_locale` references remain after the cleanup.
- Per repository dev-loop policy, full-workspace test/lint/coverage/CI, cross
  builds, live TUI inspection, and e2e snapshots were not run in this review.
- The optional understand-anything knowledge graph was absent, so affected
  components were traced directly from the diff and call sites rather than via
  a generated graph overlay.

The implementation is correct and safe for the deliberately bounded Japanese
statusbar/command-palette proof surface. Broader chrome/help/CLI localization,
RTL handling, and expanded date/time formatting remain the planned follow-up
work and are not approval blockers for THE-51.
