# THE-283 adversarial source review — dispatch 594

verdict: source-review-clear

Reviewed the current implementation through `0eb87889` (`fix(control): fail
closed when remote client setup fails`) against `CLAUDE.md`,
`docs/ARCHITECTURE.md`, `.thegn/maintenance/issue.json`,
`.thegn/maintenance/THE-283/primary-revision-1.md`, and the prior implementation
and review artifacts. No production-code revisions are requested.

## Source review result

No exploitable bypass, race, resource-growth defect, or missing unary call path
was found in the reviewed scope.

- `crates/thegn-svc/src/control/client.rs:78-104` stores one cloneable
  reqwest transport result only for `HttpOrigin`; clones share reqwest's
  internal pool, and initialization failure is represented as a typed,
  fail-closed error.
- `crates/thegn-svc/src/control/client.rs:325-368` constructs the transport
  once per `ControlClient` and routes every unary `HttpOrigin` request through
  that stored result. There is no request-level `reqwest::Client::new()` path.
- `crates/thegn-svc/src/control/client.rs:1250-1313` is the single unary
  construction/use point and retains `Policy::none()`. The existing redirect
  regression covers 3xx refusal and credential/body non-replay.
- `crates/thegn-svc/src/control/client.rs:345-357` leaves Unix and raw TCP on
  the existing Hyper one-connection-per-request path. WebSocket construction at
  `:834-890` and `:940-997` remains separate as required.
- `crates/thegn-svc/src/control/client.rs:1444-1500` covers keep-alive reuse
  across `ControlClient` clones; `:1540-1600` covers independently constructed
  clients serving replacement origins without cancelling old in-flight work.

The accepted primary boundary remains explicit: this branch has no config-reload
owner or client-swap API, so the replacement test does not claim live reload
coverage. It proves only the independently constructed-client property named by
the test. A reload-owner test is a follow-up when that owner exists.

The primary-corrected scope intentionally does not add THE-273 deadlines or
response-body caps; `response.bytes()` and the absence of those policies are
therefore recorded as unverified follow-up scope, not silently claimed as
implemented here. TLS remains reqwest's existing WebPKI path and all current
transport policy is centralized in the builder.

## Added regression coverage

Added the test-only regression
`failed_http_transport_initialization_is_fail_closed_and_typed` at
`crates/thegn-svc/src/control/client.rs:1520-1538`. It injects a failed
transport state and proves the request cannot fall back to a policy-free client
or reach the endpoint, while preserving the typed error.

## Validation actually run

```text
rustfmt --edition 2024 crates/thegn-svc/src/control/client.rs
git diff --check
rg -n 'reqwest::Client::(builder|new)|redirect\(|send_origin_request\(' crates/thegn-svc/src/control/client.rs
rg -n 'ControlAddr::HttpOrigin|ControlClient::new\(ControlAddr::HttpOrigin' crates/thegn-host crates/thegn-svc/src/control/client.rs
```

Not run: Cargo, builds, Clippy, nextest, or any full test suite. Full Rust
validation remains explicitly pending and is owned by the primary. Exact
commands still needing execution are:

```text
nix develop --command cargo check -p thegn-svc --all-targets
nix develop --command cargo test -p thegn-svc --lib http_origin_reuses_one_connection_across_client_clones
nix develop --command cargo test -p thegn-svc --lib only_http_origins_construct_a_reqwest_transport
nix develop --command cargo test -p thegn-svc --lib failed_http_transport_initialization_is_fail_closed_and_typed
nix develop --command cargo test -p thegn-svc --lib different_http_origin_clients_keep_in_flight_work_on_old_client
nix develop --command cargo test -p thegn-svc --lib control_requests_never_follow_redirects_or_replay_credentials
```

No new features, child agents, merge, push, tracker closure, row-status
change, or main-branch edit was performed.
