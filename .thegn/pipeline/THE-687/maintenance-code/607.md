# THE-687 maintenance-code — native dispatch 607

Verdict: source-review-clear

## Scope delivered

Implemented the primary-approved THE-687 repair within the stated scope. No
concrete design blocker was found. This is a clean source review only; the
required Rust compilation and focused test execution remain unverified, so
this handoff does not claim `implementation-ready` or a full gate result.

- Added the shared ancestry-based landed-identity helper using
  `rev-list --ancestry-path --reverse --max-count=1 <tip>..<target>`, with
  commit validation and an independent ancestry proof before derivation.
- Changed `AttemptOutcome::UpToDate` to carry that verified identity and wired
  the three queue-aware landed writers to persist it.
- Added central `landed`/nonempty-result-OID guards to both merge-status write
  APIs. The legacy COALESCE update still preserves an existing nonempty OID,
  but cannot create or preserve a missing/empty identity.
- Added an exact compare-and-write backfill keyed by worktree, branch, target,
  landed status, and observed timestamp. The sweep reloads the row after a
  successful backfill before entering the unchanged cleanup admission path.
- Preserved fail-closed behavior for missing refs, non-ancestor tips, failed
  backfills, remote rows, cleanup holds, dirty worktrees, and `--force` (which
  bypasses only the TTL clock).
- Added regressions for cached identities, plain merges, an octopus merge with
  the tested tip outside parent slot 2, an absorbed branch shape, unmerged
  rows with and without `--force`, writer guards, successful backfill, and
  stale backfill rejection.
- Did not touch THE-688 layout/ownership predicates or tables. Existing report
  vocabulary remains unchanged.

## Source checks performed

```text
rustfmt --edition 2024 crates/thegn-core/src/db_aux.rs crates/thegn-core/src/db_merge_status_tests.rs crates/thegn-core/src/store/worktree_aux.rs crates/thegn-host/src/cmd/land.rs crates/thegn-host/src/cmd/merge.rs crates/thegn-host/src/handlers/merge_queue.rs crates/thegn-host/src/integrate.rs crates/thegn-host/src/merge_driver.rs crates/thegn-host/src/merge_driver_status_tests.rs crates/thegn-host/src/merge_sweep.rs
rustfmt --edition 2024 --check crates/thegn-core/src/db_aux.rs crates/thegn-core/src/db_merge_status_tests.rs crates/thegn-core/src/store/worktree_aux.rs crates/thegn-host/src/cmd/land.rs crates/thegn-host/src/cmd/merge.rs crates/thegn-host/src/handlers/merge_queue.rs crates/thegn-host/src/integrate.rs crates/thegn-host/src/merge_driver.rs crates/thegn-host/src/merge_driver_status_tests.rs crates/thegn-host/src/merge_sweep.rs
git diff --check
```

The formatting and whitespace checks passed. A small standalone Git plumbing
probe also confirmed that the bounded ancestry command returns the plain
two-parent merge commit. No Cargo, build, nextest, Clippy, smoke, or full test
suite command was run under the dispatch CPU constraint.

## Exact validation requiring central execution

```text
nix develop --command cargo check -p thegn-host --all-targets
nix develop --command cargo nextest run -p thegn-host merge_sweep
nix develop --command cargo nextest run -p thegn-host landed_identity_derivation_covers_merge_octopus_absorbed_and_unmerged_shapes
nix develop --command cargo nextest run -p thegn-host missing_landed_identity_is_backfilled_from_a_plain_merge
nix develop --command cargo nextest run -p thegn-host missing_landed_identity_stays_refused_for_unmerged_branch_even_when_forced
nix develop --command cargo nextest run -p thegn-core landed_status_requires_an_effective_nonempty_result_oid
nix develop --command cargo nextest run -p thegn-core landed_identity_backfill_is_exact_and_rejects_stale_rows
nix develop --command just smoke
```

Compilation, focused Rust tests, Clippy, smoke, and the broader gate remain
unverified until the primary executes the centrally scheduled commands. No
child dispatch, merge, push, tracker closure, row-status change, or main-branch
edit was performed.
