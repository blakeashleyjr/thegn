# THE-23 architect review — verdict

Verdict: **APPROVED**

Reviewer: architect stage, row 417
Branch: `tg/the-23-devcontainer`
Base: `caef2f0e`
Design: `.thegn/pipeline/THE-23/architect/design.md`

## What I checked

Reviewed the complete branch diff against the base, the binding design, the
three implementation chunk specs and handoffs, the prior row-406 review and
revision, `CLAUDE.md`, `CONTRIBUTING.md`, `docs/ARCHITECTURE.md`, the
devcontainer OpenSpec contract, and the relevant trust/env/chrome plumbing.

The implementation satisfies the design's core parser/selection/inventory,
trust-gated overlay, config opt-out, host provider seam, native OCI fallback,
doctor, transient status, and ratchet/documentation boundaries. Row 410's
trust-boundary revision is also satisfied: explicit backend/profile/network
policy cannot be silently replaced by the provider; provider `up` uses a
cleared environment plus runtime/allowlisted values; and provider exec uses
the opaque adapter and existing CPU-cap path.

## Small corrections applied

- Provider eligibility/status now rejects any blocked `${localEnv:...}`
  substitution, forcing the native path so vendor raw-file expansion cannot
  bypass the effective allowlist.
- Provider capability checks now reject configured `network_audit`, `oci_host`,
  and `oci_runtime`, which the CLI cannot preserve.

These are committed with this verdict. No revision chunk is required.

## Verification

- `RUSTC_WRAPPER= cargo clippy -p thegn-host --bins -- -D warnings` — passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host devcontainer_provider` — 4
  passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host devcontainer_provider
devcontainer doctor switch_cache` — 29 passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-core devcontainer
config::tests::env_overlay` — 60 passed.
- `cargo fmt --all` and `git diff --check` — passed.

## Unverified

Full-workspace gates, coverage, migrations, live provider execution, and e2e
were not run, consistent with the scoped devcontainer validation policy. The
real `devcontainer` CLI was not used; the provider environment boundary is
covered with a fake executable.
