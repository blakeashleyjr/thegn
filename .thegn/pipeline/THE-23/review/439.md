# THE-23 security/test/bug review

Reviewer: dispatch row 439
Branch: `tg/the-23-devcontainer`

## Verdict

PASS — ready for the merge queue.

## Findings and fixes

- Fixed the residual raw-path TOCTOU at the devcontainer provider boundary. Trust selection now retains the exact parsed source bytes; the provider verifies those bytes, writes them to an owner-only temporary snapshot beside the repository config, and passes only that snapshot to `devcontainer up` and `exec`. The original path remains digest-checked only for stale-session invalidation, while same-directory placement preserves relative Dockerfile, Compose, and mount resolution.
- Added a regression that changes the original config after provider startup and verifies the provider reads the approved snapshot rather than the changed repository file.
- Reviewed provider eligibility, environment clearing/allowlisting, stale-session checks, error propagation, and path handling. No additional concrete security or failure-path defect found.

## Verification

- `THEGN_ALLOW_HEAVY=1 just test` — passed: 7,050 tests passed, 22 skipped by the workspace test configuration. No sandbox-denied tests were encountered, so no tests were excluded.
- `RUSTC_WRAPPER= cargo clippy -p thegn-host --bins -- -D warnings` — passed.
- Focused suites: thegn-host devcontainer provider 9/9; thegn-core devcontainer suites 59/59; core config/overlay/capability suites 85/85; service control schema 1/1; host completion/help/catalog/ratchet suites 102/102.
- `cargo fmt --all -- --check` and `git diff --check` — passed.

## Not verified

Real `devcontainer` CLI/container integration and platform-specific behavior were not exercised by the unit-test environment. The change is ready for the merge queue; `thegn integrate` remains the merge step.
