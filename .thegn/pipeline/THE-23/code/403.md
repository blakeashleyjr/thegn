# THE-23 row 403 completion

Verified the chunk-3 help and OpenSpec implementation already present on this
branch. The requested documentation commit is `5e6643e6` (`docs(the-23):
document devcontainer contract`); this handoff artifact is committed
separately for row 403.

## Delivered

- `docs/help/sandboxing.md` documents discovery and explicit variant selection,
  `auto`/`off`, untrusted repository JSON, TOFU category approvals, the
  `${localEnv:NAME}` clamp, refused/reserved fields, lifecycle frequency,
  provider fallback, doctor output, and sidebar/tab-bar state vocabulary.
- The OpenSpec proposal, design, task list, and devcontainer spec describe the
  implemented supported subset and reserve feature metadata ordering,
  generated Dockerfile layers, image-label merging, and moving-reference
  parity.
- No implementation, generated config-reference, or ratchet files were changed
  by the chunk-3 implementation.

## Verification

- `env RUSTC_WRAPPER= just quick thegn-host` — passed.
- `env RUSTC_WRAPPER= cargo nextest run -p thegn-host help` — 75 passed,
  2598 skipped.
- `env XDG_RUNTIME_DIR=/tmp RUSTC_WRAPPER= nix develop --command just openspec
validate complete-devcontainer-support --strict` — passed.
- `git diff --check` — passed.
- The literal `cargo nextest run -p thegn-core openspec` filter compiled the
  core test targets but matched no tests and exited 4; the repository's
  focused OpenSpec validator above is the applicable check.

## Not verified

- Full-workspace gates, coverage, e2e, migrations, live-state binary behavior,
  and external provider calls were not run, per the chunk and dev-loop policy.
