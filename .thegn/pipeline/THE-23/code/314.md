# THE-23 row 314 completion

Implemented the `repo_trust.rs` lint cleanup from the lead work order. The code
change and this handoff are committed together on `tg/the-23-devcontainer`.

## Delivered

- Removed `TrustedDevcontainer.config`, `source_approved`, and `status` instead
  of suppressing `dead_code`.
- These fields were vestigial: the launch-path consumer reads only
  `config_path` and `provider_eligible`. Hydration and doctor compute their own
  devcontainer status independently, so the status probe and status computation
  in `overlay_devcontainer` existed only to populate an unread field. The
  original config clone and pending-state boolean likewise had no consumer.
- Removed that population-only clone/probe/status code. Trust gating,
  `provider_eligible`, and provider launch behavior are unchanged.
- Applied clippy's two mechanical needless-borrow suggestions (`&dc` to `dc`).
- Added no lint allowances and changed no ratchet allowlists.

## Verification

- `env RUSTC_WRAPPER= cargo clippy -p thegn-host --bins -- -D warnings` — passed.
  (`RUSTC_WRAPPER` was disabled because sandboxed `sccache` exits with
  `Operation not permitted`.)
- `env RUSTC_WRAPPER= cargo nextest run -p thegn-host
prepare_sandbox_none_backend_falls_to_host` — 1 passed, 2672 skipped.
- `git diff --check` — passed.

## Not verified

- The branch-wide ignored-result ratchet is not green due to two pre-existing
  THE-23 violations outside this work order's owned file:
  `crates/thegn-core/src/devcontainer_select.rs` (introduced by `f2a49cba`) and
  `crates/thegn-host/src/devcontainer_provider.rs` (introduced by `b0015859`).
  Direct `test/ratchet.sh ignored-result ...` reports both files as new and
  neither is pinned in `test/ignored-result-ratchet.txt`. This row did not edit
  or pin them because the work order authorizes `repo_trust.rs`, forbids
  unrelated behavior changes/refactors, and ratchet allowlists are
  shrink-only.
- Full-workspace gates, coverage, e2e, and cross-platform checks were not run,
  per the dev-loop policy and this lint-only chunk's scope.
