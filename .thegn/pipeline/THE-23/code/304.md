# THE-23 row 304 completion

Implemented chunk 3's help and OpenSpec reconciliation without touching
implementation, generated config-reference, or ratchet files.

## Delivered

- Added sandbox help for devcontainer discovery/selection, `auto`/`off`
  configuration, category TOFU, the `${localEnv}` allowlist clamp, absolute
  refusal of isolation-weakening fields, reserved/unknown inventory, lifecycle
  frequency, CLI-ready versus native OCI fallback, doctor, and sidebar/tab-bar
  state vocabulary.
- Reworked the proposal and design around the implemented supported subset
  instead of complete moving containers.dev parity.
- Updated the capability delta with exact selection, trust, substitution,
  fallback, lifecycle, provider, doctor, and transient chrome contracts.
- Marked the landed parser, trust-gated fold, native OCI feature planner, host
  provider/provisioning path, doctor/chrome status, and existing Tier 2 test as
  satisfied.
- Explicitly reserved fetched feature-metadata dependency ordering,
  generated-Dockerfile feature layering, image-label metadata merging, and
  future-reference parity.

## Verification

- `env RUSTC_WRAPPER= cargo nextest run -p thegn-host help` — 75 passed.
  This filter includes all three help ratchets; no help ratchet changed.
- `nix develop --command just openspec validate complete-devcontainer-support --strict`
  — valid.
- `git diff --check` — passed.
- Read-only diffs confirmed the help, completion-slot, control-schema, and
  env-overlay ratchet/snapshot files are byte-identical to `HEAD`.

## Not verified

- `just quick thegn-host` reached the host crate but failed on pre-existing
  chunk-2 diagnostics in `handlers/repo_trust.rs`: unused `config`,
  `source_approved`, and `status` fields plus two `needless_borrow` findings.
  Chunk 3 forbids implementation edits, so these were not changed here.
- The existing Tier 2 `devcontainer_e2e`, full-workspace gates, migrations,
  e2e, and live-state binary were not run, per the chunk and dev-loop policy.
