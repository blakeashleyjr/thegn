# THE-23 row 425 completion

## Verdict

Done. Closed the trusted devcontainer config raw-path TOCTOU finding by binding
provider sessions to the SHA-256 of the selected config content and checking it
before `devcontainer up`, immediately before the child spawn, on session lookup,
and before every generated `devcontainer exec` argv. Changed or unreadable
configs invalidate the cached session and cause a safe host fallback. The
original config path is retained so Dockerfile, Compose, and mount paths keep
their existing config-relative semantics.

## Commit

The implementation and this tracked handoff are committed together as:

`fix(the-23): reject changed devcontainer config`

## Verification

- `cargo fmt --all -- --check` — passed.
- `git diff --check` — passed.
- `RUSTC_WRAPPER= just quick thegn-host` — passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host devcontainer_provider` — 8
  passed, 2672 skipped.

## Not verified

Full workspace/CI/coverage/e2e gates, live `devcontainer` CLI/container
integration, and cross-platform process semantics were not run per the scoped
dev-loop policy.

## Next

Supervisor should verify this tracked artifact and advance THE-23 to review.
