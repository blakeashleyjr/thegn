PASS

Security/test/bug review for THE-62 is complete and the branch is ready for the merge queue.

Review focus:

- Audited named sink validation and selector parsing for output/control-character injection and ambiguous routing.
- Audited live notification reload ordering for config/worker mismatches and producer races.
- Audited provider payload construction, including untrusted notification text reaching Discord mentions.
- Checked error-path coverage, queue/provider wiring, and existing ratchet-sensitive suites.

Fixes made during review:

- Discord webhook payloads now set `allowed_mentions.parse` to `[]`, preventing issue/forge text from broadcasting `@everyone`, `@here`, user, or role mentions.
- Sink names reject leading/trailing whitespace and control characters, and `push:` selectors accept case variants without weakening exact configured-name matching.
- Notification config and push sender replacement now publish atomically under one lock order, preventing a producer from pairing a reloaded route/flavor with the previous worker.

Verification:

- `XDG_STATE_HOME=/home/blake/.superzej/pipeline-state THEGN_ALLOW_HEAVY=1 just test`: 7,653 passed, 22 skipped.
- Scoped core, svc push/conformance, and host notify/monitor/doctor/platform suites: 57, 37, and 129 passed respectively.
- `cargo fmt --all -- --check` and `git diff --check` pass.

No sandbox-denied tests or socket exclusions were encountered. Live external webhook delivery and cross-platform behavior were not exercised; the provider/unit and full workspace suites pass.
