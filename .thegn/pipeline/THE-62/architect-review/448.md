# THE-62 architect review — row 448

Verdict: REVISE

Reviewed `caef2f0eb7ba180026418668aca56a1f7edf163c..HEAD` on
`tg/the-62-chat-sinks` against `.thegn/pipeline/THE-62/architect/design.md`,
all three chunk specifications and handoffs, the core/service/host seam
boundaries, `docs/ARCHITECTURE.md`, and the repository ratchet conventions.

Blocking revision:

- The host push worker never constructs or consults the implemented
  `thegn_svc::push::rate_limit::TokenBucket`; all configured sinks therefore
  run unrestricted, and `RateLimitDrop` is unreachable in production.
- `notification_render` uses one generic `kind.label(): message` shape rather
  than the required exhaustive built-in per-event template table.

Revision chunk: `.thegn/pipeline/THE-62/architect-review/revision-1.md`

Small corrections applied and committed in `3d56d14e`:

- inert default push sinks are excluded from route decisions;
- source references are carried into rendered webhook payloads;
- delivery counter changes pulse the existing terminal waker;
- ntfy transport errors are URL-redacted before worker logging;
- affected route tests now explicitly configure their intended sink.

Verification passed:

- `cargo fmt --all -- --check`
- `git diff --check`
- core targeted config/route/render suite: 52/52
- service push/conformance suite: 33/33
- host notify/monitor/doctor suite: 123/123
- required core, service control-schema, and host ratchets: 26/26, 1/1,
  and 102/102
- `just quick thegn-core`, `just quick thegn-svc`, and `just quick thegn-host`

Not verified: full workspace/CI/coverage/e2e gates, live webhook delivery or
refusal smoke behavior, cross-platform runtime/build behavior, and live daemon
reload behavior. The optional `.understand-anything/knowledge-graph.json` was
not present, so no graph overlay was generated.
