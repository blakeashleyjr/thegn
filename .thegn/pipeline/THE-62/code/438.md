# THE-62 code handoff (row 438)

verdict: done

Implemented the host wiring for named chat sinks:

- Routed `RouteDecision.push_sinks` through one bounded, non-blocking queue per configured sink and the existing off-loop provider workers.
- Added provider-neutral rendering, bounded delivery outcome accounting, config-reload worker replacement, and per-sink Monitor visibility for queue/rate drops, retries, sent, and dead letters.
- Carried the counter-only delivery snapshot through `FrameModel` and added the conditional Notifications Monitor tab without touching `attention_status.rs`.
- Preserved the legacy ntfy message path and removed the duplicate event-bus push emission.

verification:

- `cargo fmt --all -- --check`
- `RUSTC_WRAPPER= just quick thegn-host`
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host notify monitor doctor platform_cfg --status-level fail` — 124 passed
- `git diff --check`

unverified: full workspace/CI gates, end-to-end external webhook delivery, cross-platform builds, and live refusal/secret-resolution smoke behavior.

next: review the host integration against the serially landed core and service chunks; run the full pipeline gates when landing.
