# THE-62 code row 433 — completion

Verdict: done

Implemented chunk-1 core named chat-sink routing and pure notification rendering.

- Added backward-compatible `PushConfig::effective_sinks()` materialization and
  explicit `[[notifications.push.sinks]]` validation, including duplicate/name
  bounds, per-sink priority floors, reserved-kind checks, and SecretRef-only
  webhook URLs with endpoint redaction.
- Added deterministic `RouteDecision::push_sinks` fan-out/targeting for `push`
  and `push:<name>`, with DND and per-sink floors applied after final priority.
- Added substrate-free `notification_render` with all notification-kind
  templates, CommonMark/Discord/Slack/plain escaping, stable generic fields,
  caller-supplied timestamps, and Unicode-safe truncation.
- Updated config/help/OpenSpec documentation for nested sinks, routing, secret
  custody, and the offline doctor boundary.

Commit: `feat(the-62): extend core notification routing for named chat sinks`

Verification:

- `RUSTC_WRAPPER= just quick thegn-core` — passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-core config_push notification_route notification_render config_example env_overlay_coverage crate_boundaries` — 52 passed.
- `cargo fmt --all -- --check` and `git diff --check` — passed.

Unverified: svc provider implementation, host wiring/Monitor/doctor integration,
full workspace compilation (the next chunks own those dependent files), and
full CI/heavy gates.

Next: chunk-2 can consume `PushSinkConfig`, `PushKind::{Webhook,Discord,Slack}`,
`RenderedNotification`, and `RouteDecision::push_sinks`; chunk-3 should update
the host `RouteDecision` construction sites for the renamed field.
