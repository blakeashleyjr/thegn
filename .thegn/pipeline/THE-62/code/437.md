# THE-62 code row 437 — completion

Verdict: done

Implemented chunk-2 svc webhook providers and bounded delivery policy.

- Added generic JSON webhook, Discord incoming-webhook, and Slack incoming-webhook
  providers on the existing object-safe `PushProvider` seam.
- Added provider caps, provider-neutral rendered-notification delivery, request
  shaping, priority colors, Discord visible Unicode truncation, bounded retries,
  status classification, and URL/transport diagnostic redaction.
- Added per-sink token-bucket rate limiting with Discord/Slack defaults,
  bounded `Retry-After`, and send/defer/drop transitions.
- Extended the push registry for one offline, redacted probe per named sink;
  reserved kinds remain reserved and legacy ntfy behavior remains available.
- No dependency, capability, completion, control-schema, or env-overlay changes.

Commit: `feat(the-62): add webhook push providers and bounded delivery`

Verification:

- `RUSTC_WRAPPER= just quick thegn-svc` — passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-svc push conformance --no-fail-fast`
  — passed: 31/31 tests, including provider payloads, rate limiting, conformance,
  and implemented/reserved factory coverage.
- `rustfmt --edition 2024 --check` on the touched svc files — passed.
- `git diff --check` on the touched svc files — passed.

The post-test assertion-only rerun was not completed because another worker held
the shared Cargo target lock; the prior targeted run was green before those final
two assertions were added. Full workspace tests/CI and host integration remain
unverified and belong to later pipeline stages.

Next: chunk-3 should use `PushProvider::publish_rendered` when wiring routed
notifications so generic webhook payloads retain source, worktree, timestamp,
and event kind fields; existing `publish(&PushMessage)` remains for the durable
ntfy command-inbox reply path.
