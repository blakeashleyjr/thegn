# THE-62 code row 454 — completion

Verdict: done

Implemented the revision-1 fixes for the THE-62 chat-sink pipeline:

- Added an exhaustive built-in template table for all 26
  `NotificationKind::ALL` entries. Rendering remains substrate-free, caller
  timestamp-driven, flavor-escaped, Unicode-safe, and secret-free; event
  templates now provide distinct stable titles/prefixes.
- Wired the host push worker to one `TokenBucket` per configured provider/sink.
  Jobs consume tokens before publishing, defer only on the worker runtime
  within a fixed two-second retry window, and record/drop jobs when the bounded
  window cannot carry them. Queue, provider retry, dead-letter, and durable
  inbox behavior remain unchanged.
- Added a worker-policy test covering configured Slack token consumption,
  deferral, and bounded drop decisions.

Commit: `feat(the-62): enforce chat sink delivery policy and event templates`

Verification:

- `cargo fmt --all -- --check` — passed.
- `git diff --check` — passed.
- `just quick thegn-core` — passed.
- `just quick thegn-host` — passed.
- `cargo nextest run -p thegn-core notification_render` — 5 passed.
- `cargo nextest run -p thegn-host push_notify` — 3 passed.
- Focused rerun of `push_notify::tests::worker_policy_consumes_defers_and_drops_tokens` — passed.

Unverified: full workspace/CI/coverage/e2e gates, live webhook delivery,
cross-platform builds, and daemon reload behavior.

Next: architect review can re-check the production worker's limiter path and
the exhaustive renderer table; the prior full-pipeline gate remains
appropriate for landing.
