# THE-454 maintenance implementation — native dispatch row 485

Verdict: implementation-ready. The primary-approved revision is implemented
within the stated calendar transport scope. No Rust build, test, nextest,
Clippy, or full gate was run; those checks remain centrally scheduled by the
primary.

## Implemented

- Corrected bounded response accumulation to reserve against the requested
  target length, avoid growth when spare capacity exists, use bounded
  geometric growth, fail on arithmetic/allocation errors, and assert the
  retained Vec capacity never crosses the hard cap.
- Corrected public IPv6 admission to conservatively exclude the full
  2001::/23 protocol-assignment range, with accurate ORCHID/ORCHIDv2
  documentation and boundary coverage. IPv4 conversion is now limited to
  mapped IPv6, preserving native `::1` and rejecting deprecated compatible
  forms.
- Strengthened the resolver fixture to use the actual URL port, force a
  same-client second resolution after connection close, mutate the answer set,
  count resolutions, and prove no forbidden target is contacted. Proxy child
  setup now clears upper/lowercase bypass variables and sets all proxy spellings
  to a failing proxy address.
- Added separate injected idle/read and absolute-operation deadline fixtures,
  bounded never-ending error-body coverage, and timeout/body-limit transport
  classification. CalDAV 409 and 507 recovery now each prove one full-fetch
  retry, shared deadline exhaustion, token non-replay, and no third REPORT.
- Reworked oversize fixtures to assert exact error variants for chunked,
  declared-length, error, and cap-plus-one responses, and to accept a valid ICS
  body exactly at the cap. Added independent redirect-target, cross-origin,
  and downgrade Location fixtures for all redirect statuses.
- Expanded MIME coverage to every documented ICS/DAV variant and repeated
  Content-Type rejection. The pool fixture now counts accepted connections
  across reconstructed routers/backends and records request-local query,
  authorization, ETag, cookie, and CalDAV sync-token values for two accounts.
- Updated Cargo.lock only for the existing direct `url` dependency using the
  explicitly permitted offline metadata command.

## Source-only checks performed

```text
cargo metadata --offline --format-version 1 > /tmp/thegn-metadata-485.json
cargo fmt --all -- --check
git diff --check
```

These are not Rust compilation or test gates. No full gate result is claimed.

## Exact validation commands still required

```text
cargo test -p thegn-core config_calendar::tests
cargo test -p thegn-svc http::tests
cargo test -p thegn-svc calendar::tests
cargo test -p thegn-svc issue::http::tests
just lint
just test
just ci
```

All commands above remain unexecuted by dispatch row 485.
