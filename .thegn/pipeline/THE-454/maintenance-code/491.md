# THE-454 maintenance code — native dispatch 491

Verdict: implementation-ready

Implemented the primary-approved revisions on top of `d3e46c60`:

- Added a finite two-connection-per-host idle bound and 30-second idle lifetime
  to both process-wide calendar HTTP policy pools. Request-local authorization,
  validators, and CalDAV bodies remain unchanged. Added a behavioral fixture
  that creates four simultaneous connections, verifies pool reuse, and proves
  that only two idle connections are retained.
- Added a typed DNS destination-policy error marker. The marker is detected
  through reqwest/hyper's source chain, while ordinary DNS failures remain
  network errors. Same-client changed-answer and mixed-answer tests now assert
  `DestinationRefused`; ordinary resolver failure asserts `Network`; both
  calendar backends map policy refusal to non-transient `CalendarError::Policy`.
- Added a deterministic test-only connector-establishment gate and timeout
  fixture. It does not use public networking or guessed unroutable addresses.
- Added a raw HTTP CalDAV fixture covering an exactly capped success body,
  chunked oversized success, and chunked oversized error body. The raw server
  avoids framework normalization of response framing and Content-Length.
- Repaired all errors and warnings named by the frozen focused-build log:
  imported `axum::serve::Listener`, checked resolver test ports as `u16`,
  imported `futures_util::StreamExt`, captured the redirect request path before
  consuming the body, renamed the unused deadline-fixture request, and gated
  the test-only `parse_multistatus` wrapper with `cfg(test)`.

Source-only checks run:

```text
git diff --check
rustfmt --edition 2024 --check crates/thegn-svc/src/http.rs crates/thegn-svc/src/calendar/caldav.rs crates/thegn-svc/src/calendar/ics_url.rs crates/thegn-svc/src/calendar/tests.rs crates/thegn-svc/src/issue/http.rs
```

The following remain unverified and must be run by the primary centrally; no
Cargo command, build, Clippy, nextest, or test suite was run in this dispatch:

```text
cargo nextest run --offline --locked --test-threads 1 --no-tests fail --workspace -E 'test(config_calendar) | test(http::tests) | test(calendar::tests) | test(hydrate_calendar)'
cargo test -p thegn-core config_calendar::tests
cargo test -p thegn-svc calendar::tests
cargo test -p thegn-svc http::tests
cargo test -p thegn-svc issue::http::tests
just lint
just test
just smoke
just ci
```

The central focused log remains `/home/blake/code/thegn/target/maintenance-review-20260917/next-native-batch/calendar-focused.log`.
