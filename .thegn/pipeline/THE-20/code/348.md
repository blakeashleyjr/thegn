# THE-20 code row 348 — chunk 1 completion

Verdict: DONE

Implemented the substrate-free embedded-skill foundation:

- Added `SkillsConfig` with compatibility-preserving defaults, strict name and
  directory-list syntax validation, all three shallow env overlays, and example
  config documentation.
- Added the bounded typed frontmatter parser, deterministic built-in/user
  registry, SHA-256 managed marker rendering/inspection, and pure seed planner.
  The planner distinguishes absent/current/changed-managed files, preserves
  unmarked and hash-mismatched adopted files, and removes excluded/deprecated
  files only when their marker and actual hash prove they are unmodified.
- Embedded `mq`, `pipeline`, and `supervise` with their existing prose and gate
  semantics plus Claude/Codex/Pi target and create/startup/explicit metadata;
  `tui-check` is not in the manifest.
- Added native relative project skill roots to the closed harness seam:
  `.claude/skills`, `.agents/skills`, and `.pi/skills`; unsupported harnesses
  return `None`.
- Updated the existing exhaustive `ConfigOverlay` coverage companion test in
  `config_tests_coverage.rs`; this minimal test-only edit was required for the
  three new overlay fields to compile, although that companion file was omitted
  from the chunk's enumerated file list.

Verification:

- `just quick thegn-core` — PASS.
- `cargo nextest run -p thegn-core skills` — PASS (14 selected).
- `cargo nextest run -p thegn-core config` — PASS (554 selected; run outside
  the socket-restricted sandbox because the broad filter selects an existing
  DNS socket test).
- `cargo nextest run -p thegn-core env_overlay` — PASS (8 selected).
- `cargo nextest run -p thegn-core --test env_overlay_coverage` — PASS (2/2);
  no `test/env-overlay-ratchet.txt` delta was produced.
- Final canonical embedded-source preservation test — PASS.

Not run, per the chunk/dev-loop policy: full workspace tests, coverage, CI,
e2e, or a full-workspace build.

Next hint: chunk 2 can consume `SkillRegistry::embedded`, `parse_document`,
`validate_registry`, `plan_seed`, `inspect_managed`, `Harness::skill_layout`,
and `Config::skills`; the host remains responsible for discovery, filesystem
survey/application, lifecycle scheduling, and diagnostics.
