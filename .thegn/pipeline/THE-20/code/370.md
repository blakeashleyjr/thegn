# THE-20 row 370 — merge main and re-gate

## Verdict

PASS. Merged `main` at `c28a507e` into `tg/the-20-embedded-skills`, preserved
the prior THE-20 review fixes, and completed the required full gate against the
merged tree.

## Merge resolution

Resolved the nine expected content conflicts by keeping both independent
additions:

- completion catalog: THE-20 skill sources plus main's theme/config sources
- config, overlays, env handling, and tests: THE-20 skills plus main's preview
  configuration
- config validation: THE-20 skill validation plus main's drawer validation
- control scopes/tests: THE-20 skill verbs plus main's preview verb
- core module exports: THE-20 skills plus main's session modules
- CLI/configuration help: THE-20 skills prose plus main's session/hook prose

A comparison of every conflicted file against `main` confirmed that main's tip
content remains present. The only deletions relative to main were expected
single-line replacements in assertions/table formatting; no landed registry,
module, control, config, or documentation entry was dropped.

The first required full-gate attempt stopped before tests with one base-drift
compile error:

```text
error[E0063]: missing fields `drawer_cwd` and `drawer_scope` in initializer of `NamedCommand`
  --> crates/thegn-host/src/skill_seed.rs:1002:9
```

Added both fields as `None`, matching the neutral value used by main's other
test initializers.

## Verification

- `just quick thegn-core` — PASS
- targeted `cargo nextest run -p thegn-core` for the five conflict-adjacent
  config overlay, skills validation, control scope, and completion source tests
  — 5 passed
- `just quick thegn-host` after the E0063 fix — PASS
- `RUSTC_WRAPPER= THEGN_ALLOW_HEAVY=1 just test` — PASS: 7,365 passed,
  22 skipped

No test failed. Nothing required by this merge/re-gate chunk remains
unverified.
