# THE-20 row 373 — merge main and re-gate

Verdict: PASS

Merged `main` at `6eff1d66` into `tg/the-20-embedded-skills` in merge commit
`f8c4fe71`. The four expected conflicts were independent additions and were
resolved additively:

- `control/mod.rs` retains both THE-20's `SkillInfo` / `SkillsList` projection
  and THE-17's validated `EditorOpenRequest` projection.
- `control/routes.rs` retains both `skills.list` and `editor.open` in the
  control API registry.
- `control_schema.rs` and `docs/api/control-v1.json` retain both wire types and
  both route records.

The resolved files were compared against both merge parents. Relative to
`main`, the resolutions contain only THE-20 additions; relative to the prior
branch tip, they contain only THE-17 additions. No landed content or prior
THE-20 review/security fix was dropped. The merged tree introduced no
post-conflict compile breaks.

Validation:

- `just quick thegn-svc` — PASS.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-svc --test control_schema` — PASS:
  1 passed, 0 skipped, 0 failed.
- `RUSTC_WRAPPER= THEGN_ALLOW_HEAVY=1 just test` — PASS: 7,390 passed,
  22 skipped, 0 failed.
- `git diff --check` and `git diff --cached --check` — PASS before commit.
- Pre-commit `treefmt`, `shellcheck`, and `yamllint` hooks — PASS (non-applicable
  checks skipped).

The first targeted-test attempt could not start because sandboxed `sccache`
was denied IPC access; rerunning with the work-order-prescribed
`RUSTC_WRAPPER=` environment passed. No test failed during the completed
targeted or full runs.

Nothing required by this row remains unverified.
