# THE-20 code row 355 — chunk 3 completion

Verdict: DONE (documentation chunk)

Implemented the final user-facing embedded-skills contract:

- Added the `skills list`, `show`, and per-worktree `seed` grammar to the CLI
  reference, including offline inspection, configured package discovery,
  Claude/Codex/Pi project layouts, gates/phases, exclusions, and conservative
  marker/hash conflict handling.
- Added and registered the authored `skills` help page with no fake action or
  context claims. It documents prose-only safety, package layout, bounded flat
  frontmatter, path-safe names, harness/gate/phase values, a concise writing
  example, and the bundled-command drift contract.
- Documented all three `[skills]` keys in configuration help; the generated
  config-reference already consumes the landed `[skills]` block from
  `config/config.toml.example`.
- Updated the pipeline-board page to describe `mq`, `pipeline`, and `supervise`
  as embedded registry entries while retaining their existing gates.
- Help validation and ratchets required no generated allowlist changes.

Verification:

- `just quick thegn-host` — PASS with `XDG_RUNTIME_DIR=/tmp` and
  `RUSTC_WRAPPER=` after the sandbox rejected just's `/run/user` temp path and
  sccache operation.
- `cargo nextest run -p thegn-host help` — PASS (75 selected).
- `cargo nextest run -p thegn-host help::pages` — PASS (5 selected).
- `cargo nextest run -p thegn-host cli_help` — PASS (2 selected).
- The broad `help` filter exercised the action, prose, context, and panel-prose
  ratchet checks; all passed and no ratchet file changed, so the update command
  was not needed.
- `git diff --check` — PASS.

Could not verify / next hint:

- The landed chunk-1/chunk-2 source contains embedded manifest validation but
  no host test that extracts fenced `thegn` commands from embedded skill prose
  and parses them against the live clap tree. The documentation describes the
  architected command-drift contract as required, but implementing that missing
  behavioral gate is outside this documentation-only chunk and should return
  to chunk 2 rather than be added here.
- Per policy, no full-workspace tests, CI, e2e, migrations, or live-state binary
  invocation were run.
