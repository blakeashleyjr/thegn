# THE-20 code row 351 — chunk 2 completion

Verdict: DONE

Implemented the host seeding adapter and public skill surface:

- Replaced `mq_assets.rs` with one marker-aware `skill_seed.rs` filesystem
  adapter. It performs bounded immediate-package discovery, built-ins-first
  registry merging, configured harness resolution, native Claude/Codex/Pi
  layout selection, safe survey/application, atomic writes, and conservative
  removal that preserves unmarked or user-modified managed files.
- Migrated the legacy Claude MQ command assets with their exact paths and merge
  queue gate. Exact legacy content is adopted once into managed form; changed
  command files survive. Automatic create/startup hooks remain best-effort and
  compositor work is dispatched off-loop.
- Added deterministic `thegn skills list`, `show`, and `seed` text/JSON
  commands plus read-only doctor reporting from a shared serializable state
  model.
- Added `SkillsList`/`SkillsSeed` verbs and capability rows, skill-name and
  structural worktree completion slots, CLI coverage, the metadata-only
  `GET /v1/skills` control route, daemon/fake implementations, and the additive
  generated control schema snapshot.

Verification:

- `just quick thegn-core` — PASS.
- `cargo nextest run -p thegn-core skills` — PASS (14 selected).
- `cargo nextest run -p thegn-core capability` — PASS (18 selected).
- `cargo nextest run -p thegn-core completion` — PASS (42 selected).
- `just quick thegn-host` — PASS.
- `cargo nextest run -p thegn-host skills` — PASS (3 selected).
- `cargo nextest run -p thegn-host skill_seed` — PASS (4 selected adapter
  tests; the requested plural filter does not select the singular module).
- `cargo nextest run -p thegn-host doctor` — PASS (20 selected).
- `cargo nextest run -p thegn-host cli_help` — PASS (2 selected).
- `cargo nextest run -p thegn-host complete` — PASS (13 selected).
- Targeted CLI capability catalog coverage — PASS (1 selected).
- `just quick thegn-svc` — PASS.
- Control schema snapshot update mechanism and subsequent exact snapshot test
  — PASS (1 selected).
- Exact `control::routes` module — PASS (5 selected).
- Metadata-only skills route/auth test — PASS (1 selected).
- `cargo fmt --all -- --check` and `git diff --check` — PASS.

The broad substring command `cargo nextest run -p thegn-svc routes` selected an
unrelated pre-existing bridge test, which could not create its required socket
inside this sandbox (`Operation not permitted`); all five exact control-route
tests passed when scoped to `control::routes`. Per the chunk/dev-loop policy,
full workspace tests, CI, e2e, migrations, and full-workspace compilation were
not run.

Next hint: chunk 3 owns authored documentation and help ratchets. The final CLI
grammar is `skills list [--json]`, `skills show <name> [--json]`, and
`skills seed [--worktree <path>] [--json]`; doctor state names are current,
stale, modified, unmarked, and absent.
