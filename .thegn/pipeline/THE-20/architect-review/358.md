# THE-20 architect review — row 358

## Verdict

APPROVED

No revision chunks are required.

## Review basis

- Compared `tg/the-20-embedded-skills` with its exact branch base,
  `caef2f0eb7ba180026418668aca56a1f7edf163c` (the parent of the architect
  design commit).
- Checked the implementation against
  `.thegn/pipeline/THE-20/architect/design.md`, the requested coder handoff at
  `.thegn/pipeline/THE-20/code/355.md`, all three chunk specifications, and the
  repository conventions in `CLAUDE.md` and `CONTRIBUTING.md`.
- Reviewed the registry/frontmatter/parser/planner boundary, marker and hash
  rules, harness-native layouts, config/env overlays, host discovery and
  lifecycle wiring, explicit CLI and doctor surfaces, capability/scope rows,
  completion classification, read-only HTTP route/schema, and help/docs.

## Corrections applied

- `9d5648ca fix(the-20): harden embedded skill seeding`
  - Restored the missing bundled-command drift gate. Fenced `thegn ...`
    examples now parse through the live Clap tree, including flags and typed
    arguments.
  - Fixed two shipped pipeline examples that omitted the required
    `session open --worktree` argument.
  - Rejected ordinary directories as explicit seed targets instead of writing
    worktree assets into them.
  - Made incomplete registry discovery conservative: unreadable, malformed,
    or over-limit packages can no longer be mistaken for retired packages and
    cause managed copies to be deleted.
  - Skipped incomplete destination surveys, kept preserved unmarked paths
    visible to Git, and used create-new semantics for temporary files.
- `b4f17a99 fix(the-20): bound skill package discovery`
  - Made the 1,024-package discovery/survey cap real by stopping after one
    sentinel entry and preserving/skipping over-limit roots, rather than first
    collecting an unbounded directory.

## Verification

- `just quick thegn-core` — PASS.
- `cargo nextest run -p thegn-core skills` — PASS (14).
- `cargo nextest run -p thegn-core capability` — PASS (18).
- `cargo nextest run -p thegn-core completion` — PASS (42).
- `cargo nextest run -p thegn-core env_overlay` — PASS (8).
- `just quick thegn-host` — PASS on the final correction state.
- `cargo nextest run -p thegn-host skills` — PASS (3).
- `cargo nextest run -p thegn-host skill_seed` — PASS (8 on the final state).
- `cargo nextest run -p thegn-host doctor` — PASS (20).
- `cargo nextest run -p thegn-host cli_help` — PASS (2).
- `cargo nextest run -p thegn-host complete` — PASS (13).
- `cargo nextest run -p thegn-host help` — PASS (75), including help ratchets.
- `just quick thegn-svc` — PASS.
- Exact `control_schema` binary — PASS (1).
- Exact `control::routes` filter — PASS (5).
- Exact `skills_list_is_metadata_only_and_read_scoped` filter — PASS (1).
- `cargo fmt --all -- --check` and `git diff --check` — PASS.

## Notes / not verified

- `.understand-anything/knowledge-graph.json` is absent, so the optional
  graph-based one-hop blast-radius analysis and `diff-overlay.json` could not
  be produced. Run `/understand` first if that visualization is desired.
- Per the issue design and repository dev-loop policy, no full-workspace test,
  full CI, coverage, smoke/e2e, migration, cross-platform, or live-state
  binary invocation was run.
- The final implementation is correct and conservative for the reviewed
  contract. Remaining opportunities to add more host lifecycle/doctor-state
  fixtures are test depth, not blockers for this change.
