APPROVED

No revision chunks.

Reviewed the full `git diff main...HEAD` against local `main` at
`8328d5a5`, the THE-27 architecture design, the revision-3 findings, and
`code/revision-3-done.md`. The branch now satisfies the required review
snapshot, exact anchoring, shared row projection, late-delivery, cache
identity, presentation, and agent-handoff contracts.

## Architect corrections

- `98072ca5 fix(the-27): preserve review projection cursor invariants`
  restores the original Worktree DiffView selection model (hunk headers are
  rendered but not selectable), keeps top-level/general feedback visible for
  an empty PR diff, orders thread navigation unresolved-first, and clamps PR
  Files selection after refresh/resolved-toggle projection changes.
- `9823f23b fix(the-27): label unavailable review source` removes the inert
  `Tab source` hint when no review snapshot exists and labels the source as
  loading or unavailable until a compatible late snapshot arrives.

These were small local corrections with focused regressions; no new coder
revision is warranted.

## What was checked

- Additive schema v63 is correct after the branch merged the current mainline
  v62 session-fork allocation. The cache stores one complete identity-bearing
  snapshot and rejects malformed or branch/PR/head mismatches.
- Conversation and PR-diff reads, cache writes, modal deliveries, and headless
  agent runs stay off the compositor loop; transient partial fetches preserve
  the last complete snapshot.
- PR Files and PR-review DiffView use the shared exact-new-side projection for
  inline, outdated, and general rows. Rendering, counts, selection, Enter,
  reply, handoff, and resolved filtering use compatible row scopes; Worktree
  diff semantics remain separate.
- Submitted reviews and top-level comments render in PR-review DiffView.
- Live handoff is scoped to an actual agent foreground program in the active
  worktree's tabs, uses the existing bracketed-paste writer without a trailing
  newline, and does not auto-submit. Headless fallback is explicitly confirmed
  and reuses `TaskKind::PrReview` and its safety rules.
- Remote handoff bodies are bounded and stripped of terminal controls; the
  pane writer neutralizes embedded bracketed-paste markers.
- Help/OpenSpec changes document `Tab`, `n/N`, Enter, `p/P`, `v`, stale or
  unavailable data, confirmation, and no-auto-submit behavior.

## Verification

- Passed `cargo nextest run -p thegn-core review db_migrate` (47 tests).
- Passed `cargo nextest run -p thegn-host pr_view diff_view review_handoff actions`
  (51 tests).
- Passed the final DiffView suite after the availability correction (12 tests).
- Passed `just quick thegn-host` with writable runtime paths and
  `RUSTC_WRAPPER=`.
- Passed strict clippy for thegn-core and thegn-host test targets, plus a final
  strict thegn-host clippy run.
- Passed `cargo fmt --check`, `git diff --check`, and commit-time treefmt hooks.

## Not verified / notes

- `.understand-anything/knowledge-graph.json` is absent, so the
  `understand-diff` graph overlay could not be generated.
- `openspec validate --all --strict` was unavailable because `openspec` is not
  installed; `test/ratchet-check.sh` is absent. The focused host suite did run
  the applicable help/action ratchets, and commit-time treefmt passed.
- Full workspace/e2e/`just test`/`just ci`, live forge, live pane paste,
  headless-agent execution, migration against live state, and binary/live-state
  probes were not run. No live state DB was touched.
