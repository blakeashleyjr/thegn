# THE-27 security/test/bug review — row 305

PASS — ready for the merge queue.

## Findings corrected

- Fixed the manual headless-review handoff so it honors the active repository's
  `[workspace.<slug>.pr_queue]` agent/command overlay. The previous path used
  only the global queue and also ignored an explicit `agent_command` when no
  default named agent existed.
- Enforced the configured PR-agent sandbox and isolation-floor policy for the
  new manual handoff. A fail-closed floor miss now blocks dispatch with an
  explicit status instead of silently running the agent on the host.
- Cleared stale cached/unavailable fetch notices after a later successful PR
  review refresh, restoring the modal's action hints.
- Removed the new review-handoff file's unratcheted swallowed waker error; wake
  failures are now diagnosed at debug level.

These fixes and their regressions are committed as
`4f1564a1 fix(the-27): honor review handoff policy`.

## Adversarial checks

- Review cache rows are identity-bearing and rejected on worktree, branch, PR,
  or head mismatch; partial remote fetches do not replace a complete cache row.
- Conversation/diff/cache/headless work remains off the compositor loop.
- Remote review text is bounded, strips terminal controls, is shell-quoted for
  headless commands, and uses the atomic paste writer whose bracketed-paste
  marker injection tests pass.
- Live pane discovery is limited to the active worktree's tabs and checks the
  actual foreground agent program.
- Headless fallback is confirm-gated, uses `TaskKind::PrReview`, and now honors
  repository agent and isolation policy.
- Exact new-side anchoring, outdated/general buckets, resolved filtering,
  projection cursor clamping, late deliveries, and worktree/PR source separation
  are covered by the scoped suites.

## Verification

- `cargo nextest run -p thegn-core review db_migrate db_cache`: 48 passed.
- `cargo nextest run -p thegn-host pr_view diff_view review_handoff actions pane_writer platform_ratchet`: 66 passed.
- `cargo clippy -p thegn-host --tests -- -D warnings`: passed.
- `cargo fmt --check` and `git diff --check`: passed.
- Ignored-result and forge-leak shell ratchets: passed.
- Commit-time treefmt hook: passed.

## Not verified

- Full workspace, e2e, `just test`, and `just ci` were intentionally not run in
  this scoped review lane.
- No live forge, live PTY paste, headless-agent execution, or live-state DB probe
  was performed.
- `.understand-anything/knowledge-graph.json` is absent, so the
  `understand-diff` graph overlay could not be generated; run `/understand`
  before dashboard-based blast-radius inspection.
