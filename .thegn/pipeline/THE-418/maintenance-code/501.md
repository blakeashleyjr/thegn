# THE-418 maintenance code — dispatch 501

verdict: implementation-ready

## Implemented

- Added `automatic_prewarm_drains_host_result_without_spawning_or_evaluating`.
  It creates a hostile `.envrc`/flake and executable fake shell, `direnv`, and
  `nix` sentinels; resolves through the automatic prewarm launch builder;
  applies the remembered-agent substitution; sends the resulting
  `SpecOrigin::Prewarm` batch through `drain_specs`; and proves the host shell
  and evaluators never run. The skipped batch settles `prewarm_inflight` without
  creating a pane or failure mark.
- Added `drain_specs_prewarm_skip_attaches_live_target_without_fresh_spawn`.
  It sends `PrewarmSkipped` plus a live attach target through the real drain,
  exercises `attach_prewarm_sessions` → `graft_surplus` → the daemon-backed
  spawn chokepoint, verifies the exact session is adopted as a daemon pane, and
  checks `prewarm_inflight`, `prewarm_failed`, `dirty`, and `need_relayout`.
- Added a narrowly scoped test-only daemon effect seam at the existing
  `spawn_daemon_backed` route and a daemon-shaped fake pane fixture. The test
  does not connect to or start a user daemon/session; production selection and
  graft logic remain unchanged.

## Checks performed

- `git diff --check`
- `rustfmt --edition 2024 --check crates/thegn-host/src/panes.rs crates/thegn-host/src/pane.rs crates/thegn-host/src/agent_tests.rs crates/thegn-host/src/handlers/provision.rs`

No Cargo, build, nextest, Clippy, or full-suite command was run, as required by
the CPU constraint. The formatting and whitespace checks above completed
successfully.

## Exact validation still required

```text
cargo nextest run -p thegn-host --lib automatic_prewarm_drains_host_result_without_spawning_or_evaluating
cargo nextest run -p thegn-host --lib drain_specs_prewarm_skip_attaches_live_target_without_fresh_spawn
cargo nextest run -p thegn-host --lib automatic_prewarm_rejects_host_reintroduced_by_remembered_agent_relaunch
cargo nextest run -p thegn-host --lib remote_native_resolution_never_uses_the_local_none_host_fallback
cargo nextest run -p thegn-host --lib automatic_prewarm_rejects_host_specs_but_keeps_contained_specs
cargo nextest run -p thegn-host --lib removed_direnv_warm_is_inert_across_launch_seams_and_cache_leaf_shapes
cargo nextest run -p thegn-host --lib invalidated_devcontainer_exec_refuses_before_invoking_a_host_shell
cargo nextest run -p thegn-core --lib sandbox_warm_direnv_and_prepare_parse
cargo nextest run -p thegn-core --lib legacy_warm_direnv_is_a_warning_but_never_an_error
cargo nextest run -p thegn-core --lib every_legacy_mode_is_inert
cargo nextest run --workspace
cargo clippy --workspace --all-targets --all-features
```

## Unverified work and blockers

Rust compilation, focused test execution, daemon/provider runtime behavior, and
the workspace/final gate remain unverified by this dispatch. No concrete design
blocker was found within the approved revision scope; central validation must
confirm the test-only spawn seam and the new prewarm drain coverage compile and
behave as intended.
