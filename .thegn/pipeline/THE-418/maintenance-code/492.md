# THE-418 maintenance-code dispatch 492

verdict: implementation-ready

## Implemented

- Changed the final `prepare_sandbox_env` refusal to use the computed host-fallback authorization directly. Remote/provider placements with nested `backend = "none"` remain remote-native when a real remote spec resolves, while an unresolved requested sandbox cannot silently become a host shell. Existing local auto-exhaustion and explicit local-host paths remain covered by their actual resolver tests.
- Replaced the failed devcontainer-session fallback shell with typed `DevcontainerLaunchRefused` propagation. A stale/rejected provider session now stops before any host `shell -lc` argv is constructed.
- Added an explicit automatic-prewarm policy. Host launch specs are rejected before and after remembered-agent relaunch. The focused materialize/launch paths retain explicit host behavior. Prewarm-only daemon attachment remains available without synthesizing a shell.
- Refreshed stale lifecycle comments so `launch_spec_full` is described as ordinary sandbox/provider resolution rather than removed direnv warming.
- Added regressions for remote/provider/disabled resolution, host-prewarm rejection, fake-shell devcontainer refusal, all legacy/default warm modes through shared launch seams, hostile `.envrc`/flake evaluator sentinels, and symlink/hardlink/FIFO/directory `.direnv` leaves with external bytes/mtimes.

## Changed files

- `crates/thegn-host/src/agent.rs`
- `crates/thegn-host/src/agent_tests.rs`
- `crates/thegn-host/src/devcontainer_provider.rs`
- `crates/thegn-host/src/direnv_warm.rs`
- `crates/thegn-host/src/run.rs`
- `crates/thegn-host/src/handlers/provision.rs`
- `crates/thegn-host/src/daemon/agent_open.rs`
- `crates/thegn-host/src/daemon/service.rs`

## Checks performed

- `rustfmt --edition 2024 --check` on every changed Rust file: passed.
- `git diff --check`: passed.
- No Cargo/build/nextest/Clippy/full-suite command was run; CPU-constrained central validation is still required.

## Exact validation still required

- `cargo nextest run -p thegn-host remote_native_resolution_never_uses_the_local_none_host_fallback`
- `cargo nextest run -p thegn-host automatic_prewarm_rejects_host_specs_but_keeps_contained_specs`
- `cargo nextest run -p thegn-host removed_direnv_warm_is_inert_across_launch_seams_and_cache_leaf_shapes`
- `cargo nextest run -p thegn-host invalidated_devcontainer_exec_refuses_before_invoking_a_host_shell`
- `cargo nextest run -p thegn-core env_overlay config_example capability`
- `cargo nextest run -p thegn-host complete help catalog_tests mq_assets platform_ratchet`
- `just ci` for the centrally scheduled final gate.

## Unverified / findings

The changed source is formatted and has no whitespace errors, but Rust compilation, focused tests, provider/daemon runtime behavior, and the workspace/final gate are unverified by this dispatch. No design blocker was found within the approved revision scope; central validation must confirm the new test-only provider seam and prewarm attachment drain compile and behave as intended.
