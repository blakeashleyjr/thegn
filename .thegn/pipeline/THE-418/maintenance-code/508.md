# THE-418 maintenance code — dispatch 508

verdict: implementation-ready

## Scope implemented

- Added stable `target_leaves` identity to every `SpecBatch` producer and
  fixture, including `Err(SpecError::PrewarmSkipped)` batches.
- Moved stale-target validation to the start of `drain_specs`, before inflight
  reservation settlement, remote/pool bookkeeping, loading changes, attach,
  dirty state, or relayout. A late result for a closed/shifted tab cannot
  settle a replacement tab's reservation.
- Extracted the automatic prewarm resolve → first host rejection → attach
  probe → remembered-agent relaunch → second host rejection ordering into
  `handlers::prewarm::resolve_automatic_with`. The real `run.rs` worker calls
  this helper, and the executable hostile-environment regression calls the
  same production helper with narrow injected effects.
- Added stale skipped-prewarm regressions with and without attach, preserved
  valid attach behavior, retained the corrected assertions that do not require
  `SpecError: Debug`, and added a separate focused-host positive control.

## Files changed

- `crates/thegn-host/src/handlers/prewarm.rs`
- `crates/thegn-host/src/handlers/mod.rs`
- `crates/thegn-host/src/handlers/provision.rs`
- `crates/thegn-host/src/handlers/materialize.rs`
- `crates/thegn-host/src/panes.rs`
- `crates/thegn-host/src/run.rs`
- `crates/thegn-host/src/agent_tests.rs`

## Checks performed

- `cargo fmt --all -- --check`
- `git diff --check`

These are formatting/whitespace checks only. No Rust compilation, build,
nextest, Clippy, or full test suite was run under the CPU constraint.

## Exact central validation still required

The host target is a binary package; these commands intentionally omit the
invalid `--lib` flag:

```text
cargo nextest run -p thegn-host drain_specs_prewarm_skip_attaches_live_target_without_fresh_spawn
cargo nextest run -p thegn-host drain_specs_drops_stale_prewarm_skip_before_attaching_to_shifted_tab
cargo nextest run -p thegn-host drain_specs_drops_stale_prewarm_skip_without_attach
cargo nextest run -p thegn-host automatic_prewarm_drains_host_result_without_spawning_or_evaluating
cargo nextest run -p thegn-host automatic_prewarm_rejects_host_reintroduced_by_remembered_agent_relaunch
cargo nextest run -p thegn-host focused_host_launch_remains_an_explicit_positive_control
cargo nextest run -p thegn-host remote_native_resolution_never_uses_the_local_none_host_fallback
cargo nextest run -p thegn-host automatic_prewarm_rejects_host_specs_but_keeps_contained_specs
cargo nextest run -p thegn-host removed_direnv_warm_is_inert_across_launch_seams_and_cache_leaf_shapes
cargo nextest run -p thegn-host invalidated_devcontainer_exec_refuses_before_invoking_a_host_shell
cargo nextest run -p thegn-core sandbox_warm_direnv_and_prepare_parse
cargo nextest run -p thegn-core legacy_warm_direnv_is_a_warning_but_never_an_error
cargo nextest run -p thegn-core every_legacy_mode_is_inert
cargo nextest run --workspace
cargo clippy --workspace --all-targets --all-features
```

## Limits and remaining scope

Rust compilation and all runtime tests remain unverified by this dispatch; no
full gate result is claimed. The broader detached generic provider/prewarm
worker lifecycle and process-tree supervision were not redesigned, per the
approved revision; that pre-existing scope remains THE-251/related scheduling
work. No concrete design blocker was found within the approved revision.

No merge, push, tracker closure, child dispatch, row-status change, or main
branch edit was performed.
