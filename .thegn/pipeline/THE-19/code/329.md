# THE-19 row 329 completion

## Verdict

PASS — the load-sensitive hook-drain regression now asserts the bounded drain
path directly instead of imposing a wall-clock completion threshold. The
targeted test passes both normally and under deliberate CPU load, and the full
workspace test gate is green.

## Implemented

- Removed the `< 1s` elapsed-time assertion from
  `background_descendant_cannot_hold_hook_completion_open`.
- Kept the background descendant alive through the inherited stdout writer and
  asserted `PIPE_DRAIN_TIMEOUT_MARKER`, which is emitted only when the bounded
  drain deadline wins the race against pipe EOF. This proves the hook reports
  completion through the intended causal path without depending on scheduler
  timing.
- Removed the unrelated `ready` output assertion so delayed pipe-reader
  scheduling under load cannot make the regression flaky.

## Behavior and invariants

No production behavior changed. The 250 ms pipe-drain grace, process-group
kill/reap behavior, shared stdout/stderr deadline, capture limit, output marker,
redaction, and logging semantics are untouched. No ratchet allowlist changed.

## Commit

- `fix(the-19): make hook drain regression load-safe` (test fix and this
  handoff artifact).

## Verification

- `git diff --check` — passed before this handoff was written.
- Targeted nextest run — 1 passed, 2,733 skipped:

  ```text
  RUSTC_WRAPPER= cargo nextest run -p thegn-host -E 'test(background_descendant_cannot_hold_hook_completion_open)'
  ```

- The same targeted nextest command under four concurrent CPU-saturating
  workers — 1 passed, 2,733 skipped. The test itself completed in 0.761 s
  despite the deliberately busy box.
- `XDG_RUNTIME_DIR=/tmp RUSTC_WRAPPER= just quick thegn-host` — passed. The
  runtime-dir override was needed because the managed sandbox mounts
  `/run/user/1000` read-only.
- Required full gate, rerun unrestricted after the sandbox rejected localhost
  UDP binds:

  ```text
  RUSTC_WRAPPER= THEGN_ALLOW_HEAVY=1 just test
  ```

  Result: 7,144 passed, 22 skipped, 0 failed in 185.236 s. The modified hook
  regression passed as test 4,711.

The first full-gate attempt inside the network-restricted sandbox reached
1,422 tests (1,420 passed) before two unrelated `dns_filter` tests failed with
`PermissionDenied` while binding local UDP sockets. Both passed in the
unrestricted required-gate rerun.

## Not verified / next hints

- Coverage, lint, cross-compilation, smoke, doctests, and e2e were outside this
  bounded timing-fix work order and were not run.
- No production timing or lifecycle behavior changed, so no frame snapshots or
  migration checks are expected.
