# THE-19 row 334 completion

## Verdict

PASS — main was merged and all four additive conflicts were resolved without
dropping either branch's platform helpers or CLI help text. The required full
workspace test gate is green.

## Merge resolution

- `crates/thegn-host/src/platform/mod.rs` keeps THE-19's owner-only permissions
  test seam alongside main's state-file writer and symlink/FIFO test seams.
- `crates/thegn-host/src/platform/unix.rs` keeps THE-19's private append and
  checked directory-permission helpers alongside main's no-follow open and
  symlink helper.
- `crates/thegn-host/src/platform/windows.rs` keeps the corresponding THE-19
  append/ACL helpers alongside main's reparse-safe open and symlink helper.
- `docs/help/cli.md` keeps both the worktree lifecycle-hook entry and main's
  session-profile move entry, ordered before the landing-work section.
- Comparing the resolved four-file diff against `main` showed only THE-19's
  intended additions, confirming every definition and help entry at main's tip
  remains present.

No ratchet allowlist was changed.

## Verification

- `git diff --check` — passed.
- `just quick thegn-host` — passed in 7m47s.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host 'help::ratchet_tests'` —
  8 passed, 2,800 skipped.
- Required gate, `RUSTC_WRAPPER= THEGN_ALLOW_HEAVY=1 just test` — 7,273 passed,
  22 skipped, 0 failed in 106.947s on the unrestricted rerun.

The first completed sandboxed full-gate attempt stopped at two unrelated DNS
tests because local UDP socket binds were denied (`PermissionDenied`); 1,438
tests had passed. The exact command was rerun outside that network restriction,
where all 7,273 tests passed. An earlier cold attempt was interrupted after an
extended link stall; it produced no test failure.

## Commit

- Merge commit: `merge(main): resolve THE-19 platform seam conflicts` (merge
  resolution and this handoff artifact).

## Not verified / next hints

- Native Windows and macOS runtime behavior was not exercised on those hosts.
- Coverage, cross-compilation, smoke, doctests, and e2e were outside this
  conflict-resolution chunk and were not run.
- The merge includes all changes landed on main through `724baf8b`.
