# THE-19 row 327 completion

## Verdict

BLOCKED — the authorized `hook_run.rs` platform-seam move is implemented and
all scoped checks are green, but the required full workspace gate hit a
load-sensitive failure in the pre-existing bounded pipe-drain regression and
cancelled the remaining tests.

## Implemented

- Moved the Unix owner-only mode-bit assertions from `hook_run.rs` behind the
  named `test_assert_owner_only_permissions` helper in `platform/mod.rs`.
- Kept `hook_run.rs` platform-free: it now calls the helper without any
  platform `#[cfg]` of its own.
- Left `test/platform-cfg-host-ratchet.txt` unchanged; no new debt was pinned.

## Behavior and invariants

No production behavior changed. The hook spawn path, grouped-process kill,
direct-child reap, shared 250 ms pipe-drain deadline, 1 MiB capture limit,
redaction, and private log creation are untouched. On Unix the moved helper
still asserts file mode `0600` and directory mode `0700`; on non-Unix it is a
no-op, matching the prior conditional assertion exactly.

## Commit

- `fix(the-19): restore hook platform cfg ratchet` (implementation and this
  handoff artifact).

## Verification

- `cargo fmt --all -- --check` — passed.
- `git diff --check` — passed before this handoff was written.
- `just quick thegn-host` — passed after rerunning with the temporary-file
  access required by this managed environment.
- Required platform ratchet — 1 passed, 2,733 skipped:

  ```text
  RUSTC_WRAPPER= cargo nextest run -p thegn-host -E 'test(platform_cfgs_live_in_platform_modules)'
  ```

- Focused security/timeout regressions — 3 passed, 2,731 skipped:

  ```text
  RUSTC_WRAPPER= cargo nextest run -p thegn-host -E 'test(timeout_kills_the_process_group) | test(background_descendant_cannot_hold_hook_completion_open) | test(hook_log_redacts_command_output_and_is_private)'
  ```

The initial targeted nextest invocation and the initial full-gate invocation
both stopped before running tests with the configured compiler wrapper:

```text
sccache: error: Operation not permitted (os error 1)
```

Both retries used `RUSTC_WRAPPER=`.

## Full-gate result

The wrapper-free `THEGN_ALLOW_HEAVY=1 just test` retry compiled the workspace
and started all 7,144 tests (22 skipped). It reached 4,719 executed tests:
4,718 passed and one failed, after which nextest cancelled the remaining 2,425.
The failure was:

```text
thegn-host::bin/thegn hook_run::tests::background_descendant_cannot_hold_hook_completion_open
inherited pipe delayed completion for 1.424399617s
```

That same regression passed in the focused run. Under the full suite's load,
its elapsed-time assertion (`< 1s`) exceeded the threshold even though the
production pipe-drain grace remains 250 ms. This row did not change the timing
threshold or the hardened drain behavior because the work order requires exact
behavior preservation.

## Not verified / next hints

- The final 2,425 full-suite tests did not run because nextest stopped after
  the timing-sensitive failure.
- A follow-up should decide whether to make the existing regression robust to
  full-suite scheduler contention, then rerun the full gate with
  `RUSTC_WRAPPER=` in this managed environment.
- Cross-compilation, coverage, smoke, and e2e were outside this bounded work
  order and were not run.
