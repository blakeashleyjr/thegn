# THE-19 row 322 completion

## Verdict

BLOCKED — the authorized validator fix is implemented and its scoped gates are
green, but the required full workspace gate is red on a pre-existing host
platform-ratchet violation outside this row's file scope.

## Implemented

- `anyOf` and `oneOf` schema nodes now validate branches independently and
  accept the union when any branch is clean. Errors from non-matching branches
  no longer leak into otherwise valid documents.
- When no branch accepts a value, validation emits one union-level diagnostic
  naming the accepted shapes and the actual shape.
- Added regressions for string/object union values, a rejected third shape,
  direct `oneOf` handling, mixed string/object entries nested in a `[hooks]`
  list, and unchanged strict checking of a non-union string field.
- The shipped `[hooks]` example now explicitly labels its string shorthand and
  object-policy forms.

## Strictness

The only validation made more permissive is the authorized union behavior:
an `anyOf`/`oneOf` succeeds when one branch validates. Single-type nodes,
`allOf`, enum checks, nested key checks, and all non-union traversal retain
their prior strict behavior. A dedicated non-union regression still reports
`expected string, got boolean`.

## Commit

- `5ffff668` — `fix(config): validate schema unions by alternatives`

## Verification

- `env CARGO_BUILD_RUSTC_WRAPPER= just quick thegn-core` — passed.
- Targeted validator suite — 18 passed, 3,638 skipped:

  ```text
  env CARGO_BUILD_RUSTC_WRAPPER= cargo nextest run -p thegn-core -E 'test(/config_validate|example_config_validates_clean/)'
  ```

- `cargo fmt --all -- --check` — passed.
- `git diff --check` — passed before the implementation commit.
- Pre-commit `treefmt` — passed.

## Full-gate result

The first `THEGN_ALLOW_HEAVY=1 just test` attempt died before tests with:

```text
sccache: error: Operation not permitted (os error 1)
```

The single required retry used `RUSTC_WRAPPER=` and
`CARGO_BUILD_RUSTC_WRAPPER=` to override the configured Cargo wrapper. It
compiled the workspace and ran 5,700 tests: 5,699 passed and one failed;
nextest then cancelled the remaining 1,444 tests. The failure was:

```text
thegn-host::bin/thegn platform_ratchet_tests::platform_cfgs_live_in_platform_modules
ratchet test/platform-cfg-host-ratchet.txt: new violation in ["hook_run.rs"]
```

Neither `crates/thegn-host/src/hook_run.rs` nor
`test/platform-cfg-host-ratchet.txt` has a working-tree diff in this row. Fixing
or pinning that committed host violation is outside the two-file authorization
in `validator-fix.md`.

## Not verified / next hints

- The final 1,444 full-suite tests did not run because nextest stopped after
  the unrelated ratchet failure.
- The next authorized row should move the platform-conditional code in
  `hook_run.rs` behind a `platform::` seam (preferred by the ratchet), then
  rerun `THEGN_ALLOW_HEAVY=1 just test` with the compiler wrapper disabled in
  this sandbox.
