# THE-69 code — dispatch 331

Verdict: complete

## Commit

- `feat(the-69): notify on model-proxy budget caps`

## Changes

- Added a pure core classifier for configured token/cost cap breaches with
  shared equality and half-open rolling-window semantics.
- Reused the core comparison helpers in model-proxy enforcement while
  preserving scope-chain, kill-switch, breach-action, and fail-open behavior.
- Projected breached scope/window/dimension facts from the off-loop usage
  worker through the existing emit-once `usage_limit` notification route, with
  durable-only fallback before live routing is installed.
- Added stable host formatting and worktree routing tests, passed the budget
  config through all three usage-spawn call sites, and documented the behavior.

## Verification

- `just quick thegn-core`
- `cargo nextest run -p thegn-core budget_alert` — 5 passed
- `just quick thegn-proxy`
- `cargo nextest run -p thegn-proxy budget` — 9 passed
- `just quick thegn-host`
- `cargo nextest run -p thegn-host usage_budget` — 4 passed
- `cargo nextest run -p thegn-host notify` — 15 passed
- `cargo fmt --all` and `git diff --check`

Targeted nextest commands ran through `nix develop` so the repository's
per-mount compiler-cache socket was initialized correctly.

## Not verified

- Full-workspace, coverage, CI, e2e, and cross-platform gates were not run per
  the chunk's dev-loop policy.
- No product/UI binary was invoked against live state; only the mandated
  post-commit `thegn dispatch report` handoff uses the live roster.
