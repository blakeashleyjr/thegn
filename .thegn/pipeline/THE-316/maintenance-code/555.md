# THE-316 implementation — dispatch row 555

verdict: revisions-needed

## Scope and findings

Implemented the round-3 Jira transition classification fix in
`crates/thegn-svc/src/issue/jira.rs` and its focused tests. The requested root
`.thegn/maintenance/issue.json` is absent in this checkout; task data was read
from `.thegn/maintenance/THE-316/issue.json`.

The original false-success swallow remains already fixed before this dispatch:
`JiraBackend::update_issue` calls `post_empty(...).await`, so this row did not
re-fix that criterion. No control wire, schema, main branch, tracker state,
merge queue, or push was changed.

## Implemented

- Jira transition responses are `NotApplied` only for the approved allowlist:
  400, 401, 403, 404, 409, and 422. HTTP 408, 418, 425, 429, other 4xx, and
  intermediary/5xx failures remain ambiguous.
- Added regression coverage for HTTP 408 and unrecognized HTTP 418, including
  the required final-fetch attempt and empty `unapplied` field set.
- The ambiguous final-fetch failure now retains the original typed POST error
  as `IssueError::PartialUpdate::source`, preserving `IssueError::is_transient`
  classification. Added a focused source-classification regression.

## Concrete blocker

The round-3 brief also requires the failed-verification Unknown result to keep
the bounded `verify before retrying` message. The existing `IssueError::PartialUpdate`
shape has only `applied`, `unapplied`, and one typed `source`: it cannot carry
both the original POST error (needed for transient classification) and separate
bounded verification context. Adding a context field or a dedicated wrapper
requires changing `crates/thegn-svc/src/issue/mod.rs`, while this round
explicitly limits scope to `jira.rs` and its tests. I did not silently change
that out-of-scope error API. The current implementation preserves the safer
typed source and represents Unknown with an empty `unapplied` set, but the
exact message criterion remains unresolved pending scope authorization.

## Validation

Executed:

- `rustfmt --edition 2024 crates/thegn-svc/src/issue/jira.rs`
- `rustfmt --edition 2024 --check crates/thegn-svc/src/issue/jira.rs`
- `git diff --check`

Not executed by instruction; the primary centrally schedules Rust validation:

- `cargo check -p thegn-svc --all-targets`
- `cargo nextest run -p thegn-svc issue::jira::tests::`
- `cargo clippy -p thegn-svc --all-targets --all-features -- -D warnings`
- `just ci`

No Cargo build, focused test, Clippy, or full-gate result is claimed.
