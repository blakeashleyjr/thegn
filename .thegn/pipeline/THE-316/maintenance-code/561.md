# THE-316 implementation — native dispatch 561

verdict: implementation-ready

## Scope and findings

Implemented the primary revision-4 authorization on
`blake/the-316-propagate-jira-transition-failures-instead-of-returning`.
Task data was read from `.thegn/maintenance/THE-316/issue.json`; the
requested root-level `.thegn/maintenance/issue.json` is absent in this
checkout.

There is no concrete design blocker in the authorized scope. The original
false-success swallow was already fixed before this dispatch:
`JiraBackend::update_issue` uses `post_empty(...).await`, so this row did not
re-fix that criterion. The control-wire item remains explicitly deferred by
the primary; `docs/api/control-v1.json` was not changed.

## Implementation

- Extended `IssueError::PartialUpdate` with the machine-readable
  `unverified` field set. Its Debug and Display output include the field set,
  and Display renders `verify before retrying` only when a field outcome is
  unresolved. Existing source chaining and error-class delegation remain
  intact.
- Jira transition failures after a title write continue to classify
  definitive 400/401/403/404/409/422 responses as `status` unapplied. An
  ambiguous POST followed by a failed verification now reports
  `status` unverified, preserves the typed POST error, and never invites a
  blind retry. A malformed fetched key follows the same unverified path.
- Updated regressions for title/status partial writes, HTTP 408, unrecognized
  4xx, typed transient-source preservation, bounded provider diagnostics, and
  successful final verification. Added a focused invariant test asserting
  that applied, unapplied, and unverified field sets are disjoint.
- No raw provider bodies, control-wire types, schema snapshots, merge state,
  tracker state, or unrelated files were changed.

## Validation performed

- `rustfmt --edition 2024 --check crates/thegn-svc/src/issue/mod.rs crates/thegn-svc/src/issue/jira.rs`
- `git diff --check`
- Source/diff inspection only; no Cargo command, build, test, nextest,
  Clippy, or full gate was run, per the dispatch CPU constraint.

## Exact validation still required

The primary must centrally execute the focused Rust validation:

```sh
nix develop --command cargo check -p thegn-svc --all-targets
nix develop --command cargo test -p thegn-svc --lib issue::jira::tests::
```

The primary batch gate remains unverified and must run through its configured
workflow, including the workspace build/test, Clippy/lint, smoke, and CI
checks. This artifact does not claim any of those gates passed.

No row status change, merge, push, tracker closure, child-agent dispatch, or
main-branch edit was performed.
