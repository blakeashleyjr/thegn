# THE-195 maintenance implementation — row 601

Verdict: implementation-ready

## Implemented scope

- Removed the duplicate CLI capability registry from `cmd/api.rs`.
- Made `cmd/session.rs::cli_control_caps` production-visible so API coverage,
  doctor, and the existing CLI catalog drift test consume the same registry.
- Added one `CoverageReport` snapshot with the build revision (or the explicit
  `unavailable` source-build fallback) and coverage schema version `1`.
- Made API human output, API JSON output, doctor human output, and doctor JSON
  output derive from that shared snapshot. Existing `SURFACE_GAPS` reasons and
  the four intentional CLI exclusions were not changed or closed.
- Added regression coverage for API/session ledger parity, API/doctor JSON
  parity, metadata presence, and updated the static CLI output acceptance test
  for the new metadata-bearing document.

## Validation actually run

- `rustfmt --edition 2024 --check crates/thegn-host/src/cmd/api.rs crates/thegn-host/src/cmd/doctor.rs crates/thegn-host/src/cmd/session.rs crates/thegn-host/tests/static_commands_process.rs` — passed.
- `git diff --check` — passed.
- Static source inspection confirmed the API-local `cli_control_caps` function
  is absent and the API/doctor paths call `coverage_report`.

## Validation not run / exact commands for primary

No Cargo command, build, Clippy run, nextest run, or full test suite was run by
this row, per dispatch CPU constraints. The primary should execute:

```text
cargo check -p thegn-host --all-targets
cargo nextest run -p thegn-host coverage_report_uses_the_session_registry_and_one_document_shape
cargo nextest run -p thegn-host doctor_and_api_expose_the_same_coverage_document
cargo nextest run -p thegn-host cli_control_verbs_cover_catalog
cargo nextest run -p thegn-core coverage_problems_reports_each_kind_of_drift
cargo nextest run -p thegn-core ledger_counts_stub_apart_from_working
cargo nextest run -p thegn-core ratchet_pins_surface_gaps
```

No full gate result is claimed. The source changes and focused tests are
unverified until those commands execute.
