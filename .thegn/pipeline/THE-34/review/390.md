# THE-34 security/test/bug review — dispatch row 390

Verdict: PASS — ready for the merge queue.

Review scope covered the branch diff for authorization ordering, event-filter
validation, error propagation, path/query encoding, permission boundaries,
socket/session races, and failure-path tests. No unresolved security or
correctness finding remains in the THE-34 changes.

Small fixes applied:

- `FeedFilter::parse_kinds` now bounds comma-separated input entries before
  deduplication, preventing unlimited duplicate valid kinds from bypassing the
  filter bound. Added a regression assertion.
- Added the new top-level `events` command to the grouped CLI help table;
  `groups_cover_visible_commands_exactly` now passes.

Verification:

- Scoped core wire/error/capability/completion tests: 77 passed.
- Scoped service event/error/schema tests: 6 passed.
- Final CLI help regression: passed.
- Formatting and `git diff --check`: clean.
- Queue-boundary gate (final tree): 6,970 passed, 85 skipped.

The initial mandatory `THEGN_ALLOW_HEAVY=1 just test` was blocked before test
execution by the documented sccache `Operation not permitted`; the wrapper-free
retry reached the suite. The final accepted gate was run with
`RUSTC_WRAPPER=` and excluded only sandbox-denied fixtures, listed below.

Unverified because this sandbox denies socket/state syscalls:

- DNS UDP/loopback: `dns_filter::tests::forward_query_times_out_when_no_resolver`,
  `dns_filter::tests::forward_query_round_trips_against_fake_resolver`,
  `dns_filter::tests::server_end_to_end_singleton_and_events`,
  `dns_filter::tests::server_forwards_allowed_query_to_configured_upstream`,
  and all four `sandbox_dns_e2e` tests (`a1`–`a4`).
- Unix/TCP socket fixtures: host `bridge_sup::tests::connect_registers_routes_and_forwards_then_disconnect`,
  `daemon::service::tests::ws_warm_attach_pipeline_over_a_real_socket`,
  `forward::tests::{remap_on_conflict_sets_flag,start_and_views_then_stop,stop_all_on_returns_ports}`,
  `handlers::worktree_attach::tests::silent_daemon_hits_the_probe_timeout_instead_of_hanging_the_worker`;
  service `bridge::tests::*`, `control::client::tests::attach_refuses_an_incompatible_daemon_proto`,
  `ipc::tests::{unix_bind_is_the_lock_and_round_trips,racing_binders_on_a_stale_socket_elect_exactly_one_daemon}`,
  `iroh_reach::tests::{dead_connection_is_evicted_from_registry,handshake_timeout_closes_stalled_pre_auth_connection}`,
  and all listed `revtunnel::tests` socket cases.
- Loopback mock-provider binaries: `fly_mock`, `sprites_mock`, `vps_mock`, and
  `vps_do_mock` (their test setup binds local HTTP fixtures).
- State-file permission fixtures:
  `sandbox::tests::oci_local_secrets_go_to_env_file_not_argv`,
  `sandbox::tests::systemd_local_secrets_go_to_environment_file_not_argv`.
  Both pass with writable `XDG_STATE_HOME=/tmp/...` and fail only when the
  sandbox blocks the real state path.
- `ssh_creds::tests::unix_socket_alive_probes_connectability` (Unix listener
  bind denied by the sandbox).

The verdict artifact is ready, but committing it and the two small fixes is
currently blocked by the sandbox rejecting writes to the worktree git metadata:
`.git/worktrees/tg-the-34-herdr-api/index.lock: Read-only file system`.
