# THE-34 code handoff — row 387

Verdict: implemented chunk 3, CLI event-tail projection.

Changes:

- Added `thegn events tail` with `--kinds`, `--session`, `--signal-lag`, and
  `--json`, reusing the typed subscription client and existing daemon
  discovery/auth path. The command is read-only, waits on the stream, keeps
  the greeting first, emits canonical NDJSON, and renders lag markers in human
  mode.
- Added parser, formatter, all-frame rendering, and isolated no-daemon tests.
- Added the CLI capability surface and both runtime/coverage ledger entries;
  classified `events tail --kinds` as `Structural` and `--session` as
  `Session` in the core completion catalog. No ratchet files changed.
- Documented filters, auth, bounded loss, no replay, resync via
  `sessions.list`/`worktrees.list`, and the session-input interlock in the
  authored CLI, daemon/session, and architecture docs.

Verification:

- `TMPDIR=/tmp XDG_RUNTIME_DIR=/tmp RUSTC_WRAPPER= just quick thegn-core`
- `TMPDIR=/tmp XDG_RUNTIME_DIR=/tmp RUSTC_WRAPPER= cargo nextest run -p thegn-core capability`
- `TMPDIR=/tmp XDG_RUNTIME_DIR=/tmp RUSTC_WRAPPER= cargo nextest run -p thegn-core completion`
- `TMPDIR=/tmp XDG_RUNTIME_DIR=/tmp RUSTC_WRAPPER= cargo check -p thegn-host`
- `cargo fmt --all -- --check`
- `git diff --check`

Could not verify: `just quick thegn-host` failed on the pre-existing chunk-2
`clippy::collapsible-if` in `crates/thegn-svc/src/control/grpc.rs`, outside
this chunk's exact file scope. Host `events` and
`completion_slots_are_bound_or_pinned` nextest filters were attempted, but
repeated multi-minute host test-target builds did not finish and were
interrupted; no test failure was reported. The host coverage/help filters
were not run after that build bottleneck. Full workspace gates, live-daemon/DB
checks, and e2e were not run per the chunk policy.

Commit subject: `feat(the-34): expose filtered event feed through CLI`
