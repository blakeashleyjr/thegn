# THE-34 code handoff — row 378

Verdict: implemented chunk 1, stable control error codes.

Changes:

- Added the substrate-free, serde/schemars-compatible `ControlErrorCode`
  taxonomy and pure stability/serialization tests.
- Added `ControlError::code()`, public `ErrorBody`, and structured HTTP
  envelopes for control, authentication, validation, and local-dispatch
  errors without changing status codes or prose messages.
- Added optional client-side code parsing/accessor with compatibility for old
  `{"error": ...}` responses.
- Regenerated `docs/api/control-v1.json` and documented the additive envelope.

Verification:

- `just quick thegn-core`
- `just quick thegn-svc`
- `cargo nextest run -p thegn-core control_error`
- `cargo nextest run -p thegn-svc control_error`
- `cargo nextest run -p thegn-svc --test control_schema`
- `git diff --check`
- `cargo fmt --all -- --check`

The repository's configured `sccache` was not permitted in this environment;
the scoped checks were rerun with `RUSTC_WRAPPER` unset. Full workspace gates,
daemon/e2e checks, and unrelated transports were intentionally not run per the
chunk's dev-loop policy.

Handoff blockers: Git staging/commit cannot write the shared read-only
`.git/worktrees` metadata. The installed `thegn dispatch report` command is
from an older binary and is unavailable; the checkout-source command reached
the live state directory but failed because its database is read-only.

Commit subject: `fix(the-34): add stable control error codes`
