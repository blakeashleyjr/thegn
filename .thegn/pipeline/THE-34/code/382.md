# THE-34 code handoff — row 382

Verdict: implemented chunk 2, filtered control event subscriptions with
opt-in visible lag.

Changes:

- Added substrate-free `FeedFilter`, bounded session/kind validation,
  `EventFrame::kind()`, session narrowing, and additive `Lagged { missed }`
  framing in `thegn-core`.
- Added per-connection WS/SSE query parsing and filtering, structured
  `bad_request` responses, SSE event names, and opt-in lag markers. The
  existing broadcast and 256-frame client buffer are unchanged.
- Added additive gRPC `EventsRequest` filter fields and `Lagged` event mapping.
- Added typed-client subscription options, URL encoding, and a shared JSON
  frame formatter; regenerated the control schema and documented the v1
  filter/lag contract.
- Deliberately did not implement the draft `State` snapshot, CLI tail, daemon,
  database, config, plugin, or compositor changes.

Verification:

- `TMPDIR=/tmp XDG_RUNTIME_DIR=/tmp RUSTC_WRAPPER= just quick thegn-core`
- `TMPDIR=/tmp XDG_RUNTIME_DIR=/tmp RUSTC_WRAPPER= just quick thegn-svc`
- `cargo nextest run -p thegn-core control_wire` (15 passed)
- `cargo nextest run -p thegn-svc control_events` (3 passed)
- `cargo nextest run -p thegn-svc --test control_schema` (passed)
- `cargo nextest run -p thegn-svc --features control-grpc grpc_methods` (passed)
- `cargo nextest run -p thegn-svc --features control-grpc every_frame` (passed)
- `cargo check -p thegn-svc --features control-grpc` (passed)
- `cargo check -p thegn-host` (passed)
- `cargo fmt --all -- --check`
- `git diff --check`

Could not verify: full workspace gates, live daemon/DB transport pumps, e2e,
and unrelated full test suites were intentionally not run per the chunk's
dev-loop policy. The configured sccache and default just runtime directory
were unavailable in this sandbox, so targeted commands used an isolated `/tmp`
runtime and `RUSTC_WRAPPER=`.

Commit subject: `feat(the-34): filter and signal control event subscriptions`
