# THE-34 architect review — row 388

Reviewed the cumulative branch diff from `caef2f0e` through `HEAD` on
`tg/the-34-herdr-api` against the THE-34 architect design, the three chunk
specifications and handoffs, the control-plane OpenSpec, and repository
ratchet/coverage conventions.

## Verdict

APPROVED

The implementation is correct and safe to hand off. No revision chunk is
needed.

The review confirmed that the branch:

- adds the closed control error-code envelope without changing status or prose
  compatibility;
- keeps event filtering per connection, reuses the bounded broadcast, retains
  authorization as the only access gate, and makes lag signaling opt-in;
- shares frame-kind validation and JSON formatting between transports and the
  CLI;
- wires `thegn events tail` through the existing discovered Unix/bearer TCP
  control path, with no polling, session-input enablement, DB migration, new
  config key, or new TUI wake source;
- pays the `events.subscribe` CLI catalog/ledger cell without changing either
  surface-gap or completion ratchet; and
- regenerates and validates the control schema snapshot.

## Small correction applied

Collapsed the nested `clippy::collapsible-if` in the gRPC lag-signal branch in
`crates/thegn-svc/src/control/grpc.rs`. The short-circuit form preserves the
legacy no-send path when `signal_lag` is false and the existing disconnect
behavior when sending the marker fails.

## Verification

Passed:

- `just quick thegn-core`
- core `control_wire`, `capability`, and `completion` nextest filters
- `just quick thegn-svc`
- service event-query, error-envelope, and old-server compatibility tests
- `cargo nextest run -p thegn-svc --test control_schema`
- `cargo check -p thegn-host`
- `cargo fmt --all -- --check`
- `git diff --check`

The host quick gate and host `events` test filter were attempted after the
correction but their test-profile build remained in the repository's known
multi-minute host build bottleneck and was interrupted without a failure
diagnostic. The pre-correction host quick attempt reported only the corrected
gRPC clippy warning. A linked executable smoke check was not completed because
the host binary build hit the same bottleneck.

Full-workspace gates, coverage, cross-platform checks, live daemon/state-DB
checks, and e2e were not run, consistent with the THE-34 chunk policy.

No revision chunk paths apply.
