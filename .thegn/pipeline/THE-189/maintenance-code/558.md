# THE-189 maintenance-code — native dispatch 558

verdict: implementation-ready

## Scope

Applied only the four compile corrections authorized by
`.thegn/maintenance/THE-189/primary-revision-2.md`. The live hydration
schedule design and F-001..F-005 behavior were not changed.

## Changes

- Handled a nested `RefreshKind::Scheduled` envelope explicitly in the event
  loop, preserving exhaustive dispatch without a wildcard arm.
- Loaded the replacement clock period from its `Arc<AtomicU64>` before
  constructing the worker-local period slot.
- Passed `None` from the untagged calendar failure regression fixture to the
  new generation parameter.
- Cloned the reminder generation into the acknowledgement before borrowing it
  for the notification fence, preserving stale-reminder suppression and cursor
  release.

## Regression coverage

The existing implementation tests remain in scope and were not weakened:

- effective projection and per-class diff coverage for clock, CI, PR, usage,
  weather, calendar, and LOC;
- fake-time rearming without immediate refresh, live replacement, rapid
  generation replacement, untagged-refresh precedence, and publication
  fencing;
- stale reminder acknowledgement cursor release and the reviewed shared
  ticker cadence fixtures.

## Validation

Completed without Cargo, builds, tests, nextest, Clippy, or full gates:

```text
rustfmt --edition 2024 --check crates/thegn-host/src/run.rs crates/thegn-host/src/hydrate_refresh_ticker.rs crates/thegn-host/src/hydrate_calendar.rs crates/thegn-host/src/hydrate_calendar_tests.rs
git diff --check
```

Both commands passed. No full gate is claimed. Rust compilation and focused
test outcomes remain unverified because the dispatch instructions prohibit
Cargo execution and reserve Rust validation for the primary.

## Commands requiring primary execution

```text
nix develop --command cargo check -p thegn-host --all-targets
nix develop --command cargo nextest run -p thegn-host hydrate_schedule
nix develop --command cargo nextest run -p thegn-host hydrate_refresh_ticker
nix develop --command cargo nextest run -p thegn-host hydrate_calendar
nix develop --command cargo nextest run -p thegn-host hydrate_weather
nix develop --command cargo nextest run -p thegn-host ci_refresh
nix develop --command cargo nextest run -p thegn-host loc
nix develop --command just quick thegn-host
```

No concrete design blocker was found within the primary-approved scope.
