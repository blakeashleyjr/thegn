# THE-59 code handoff — row 429

Verdict: done

Implemented the revision-1 external voice lifecycle correction in the exact
three-file scope:

- Added a request-specific atomic cancellation signal to `VoiceController`.
  Explicit cancel, live config changes, and controller drop signal an active
  transcription; stale request ids remain rejected, and completed requests
  release their handle.
- Added a cancellable provider entry point while preserving the synchronous
  `VoiceProvider` seam. WAV stdin writes now run off-thread and are monitored
  alongside bounded stdout/stderr readers, cancellation, and the hard deadline.
- Added Unix process-group termination and child reaping on setup, write,
  output-cap, wait, timeout, and cancellation paths; all helper threads are
  joined before returning.
- Added focused service tests for cancellation, excessive output, and a
  non-reading transcriber timeout.

Verification:

- `just quick thegn-svc` — passed.
- `just quick thegn-host` — passed.
- `cargo nextest run -p thegn-svc voice` — 6 passed.
- `cargo nextest run -p thegn-host voice` — 2 passed.
- `cargo fmt --all` and `git diff --check` — passed.

Not verified: full workspace/CI/coverage/e2e gates, live microphone or
external transcription integration, and cross-platform process semantics.

Required commit subject: `fix(the-59): bound and cancel voice subprocesses`.
