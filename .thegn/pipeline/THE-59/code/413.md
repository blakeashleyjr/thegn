# THE-59 code handoff — row 413

Verdict: done

Implemented the external command-backed voice mode chunk from `chunk-2.md`:

- Added the argv-only service voice provider, bounded WAV/transcript handling,
  timeout/error classification, and doctor seam registration.
- Added host capture/transcription workers, reducer/effect draining, cancellation
  and stale-timer protection, focused-pane transcript injection, and recording
  mode-chip decoration.
- Registered `Alt v` toggle and `Alt V` cancel actions, with Escape cancellation
  only while recording; added keymap specs, help content, and e2e freeze opt-out.
- Kept the feature opt-in and free of bundled audio, STT, model, capability, or
  protocol changes.

Verification:

- `cargo check -p thegn-svc -p thegn-host --bin thegn` — passed.
- `just quick thegn-svc` — passed.
- `just quick thegn-host` — passed.
- `cargo nextest run -p thegn-svc voice` — 3 passed.
- `cargo nextest run -p thegn-host voice keymap statusbar` — 103 passed.
- `cargo fmt --all` and `git diff --check` — passed.

Not verified: full workspace gates, end-to-end microphone/audio commands, or
real external transcription services; those are intentionally outside this
chunk’s minimal dev loop.

Commit subject: `feat(the-59): wire external voice command mode`
