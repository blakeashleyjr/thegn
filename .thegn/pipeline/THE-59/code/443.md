# THE-59 reconcile handoff — row 443

Verdict: done

Implemented the exact reconcile spec from `reconcile-main.md` by completing
the in-progress merge of `main` into `tg/the-59-voice-mode`.

Resolution summary:

- Preserved the main branch's four marked-enum entries and THE-59's
  `VoiceKind`, updating the reachability/count ratchet to 95.
- Preserved both voice and main host module registrations and event-loop
  initialization, including theme-builder/media setup.
- Preserved both platform process-group kill seams.
- Preserved main's localized mode-chip path alongside the voice controller.
- Carried THE-59's two action specs into main's `message_key` schema and added
  their stable keys to both shipped catalogs so the merged host builds cleanly.

Verification:

- `RUSTC_WRAPPER= just quick thegn-core` — passed.
- `RUSTC_WRAPPER= just quick thegn-host` — passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-core marked_definition` — 2/2
  passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host voice` — 2/2 passed.
- `git diff --check` — passed.

The merge commit is `b8f133b0` (`Merge branch 'main' into
tg/the-59-voice-mode`). This artifact is committed separately so the row
handoff remains tracked.

Not verified: full workspace `just test`/`just ci`, coverage, e2e, and
cross-platform runtime behavior; those are outside this reconcile chunk's
targeted dev loop.
