# THE-59 code handoff — row 407

Verdict: done

Chunk: `.thegn/pipeline/THE-59/code/chunk-1.md` (core voice contract, reducer,
configuration, feature gate, and config ratchets).

Commit subject: `feat(the-59): add voice provider contract and state machine`.
The final commit hash is supplied in the dispatch report (the artifact is part
of that commit, so embedding its hash here would be self-referential).

Implemented:

- Added substrate-free `VoiceProvider`, `VoiceCaps`, classified `VoiceError`,
  and the pure `VoiceState`/`VoiceEvent`/`VoiceEffect` reducer.
- Added command-backed `VoiceConfig`/`VoiceKind` with five documented keys,
  strict enum validation, and a bounded 1..=300 second capture limit.
- Registered the core modules and config field, added `Feature::Voice` as an
  experimental feature, and clamped it off in the stable channel.
- Updated the config schema count, example config, and shrink-only env ratchet;
  no environment aliases, capability row, host/service files, audio
  dependency, model store, migration, or external STT code was added.

Verified:

- `RUSTC_WRAPPER= just quick thegn-core` passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-core voice channel config` passed:
  582 tests passed, 3062 skipped by filter/profile selection.
- `cargo fmt --all` and `git diff --check` passed.

Unverified: full workspace gates (`just test`, `just ci`, coverage, e2e),
runtime host/service integration, and real external voice commands were not run
per the chunk's targeted dev-loop policy. The first test attempt was blocked
before compilation by the read-only `sccache` cache; rerunning with
`RUSTC_WRAPPER=` passed.

Next: chunk 2 may consume the public `VoiceConfig`, `VoiceKind`, provider
contract, reducer, and `Feature::Voice`; keep host/service/UI changes within
`.thegn/pipeline/THE-59/code/chunk-2.md`.
