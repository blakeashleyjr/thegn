# THE-10 architect review — dispatch row 436

Verdict: APPROVED

I reviewed `c19da601b8d5d8e5922a11e5d3c9b7dffa406e5f..HEAD` against
`.thegn/pipeline/THE-10/architect/design.md` and the repository contribution
standards.

Checked:

- Canonical project/program vocabulary at the CLI, UI/sidebar, palette,
  keymap, help, config, Home Manager, capability catalog, and snapshots.
- Three-release compatibility behavior: config/env aliases with canonical
  precedence and diagnostics, `thegn project`, `wt new --project`, legacy
  action IDs, and deprecated capability rows.
- Preservation of internal `Workspace*`/`Project*` storage and DB contracts,
  tracker-owned fields, CLI JSON shape, and control schema.
- Completion bindings, help/catalog/surface ratchets, stale-doc guard, and
  shell syntax.

Small review corrections are committed in `7b37edc0`:

- restored canonical `program` completion catalog bindings and updated the
  completion ratchet;
- corrected stale public project/workspace examples in
  `config/config.toml.example`;
- made the CLI capability projection advertise canonical `program.*` IDs
  while retaining alias-aware coverage.

Final verification:

- `cargo nextest run -p thegn-core env_overlay config_example capability` —
  28 passed;
- `cargo nextest run -p thegn-svc --test control_schema` — 1 passed;
- `cargo nextest run -p thegn-host complete help catalog_tests mq_assets
platform_ratchet` — 102 passed;
- `just quick thegn-core`, `just quick thegn-svc`, and `just quick
thegn-host` — all passed;
- `test/stale-docs-guard.sh`, `git diff --check`, and `bash -n test/smoke.sh`
  — all passed.

Not run, per the design boundary: full workspace CI/build/coverage, e2e or
live-state smoke tests, migrations against a live state directory, and
external integrations. No revision chunk is required.
