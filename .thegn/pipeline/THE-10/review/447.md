# THE-10 security/test/bug review — dispatch row 447

## Verdict

PASS — ready for the merge queue.

## Review coverage

- Audited the compatibility normalizer and layered configuration paths for precedence, malformed input handling, legacy-key diagnostics, and path/environment handling. Canonical values win over legacy duplicates, and legacy values are accepted with visible diagnostics.
- Audited CLI and completion aliases for `project`/`program` and `wt new --project`/`--program`. The compatibility warning is emitted on stderr, preserving structured stdout output; arguments are matched as exact command/option tokens rather than interpolated into shell or filesystem operations.
- Audited capability projections and keymap action aliases. Deprecated capability rows retain the canonical verb/surface scope, and legacy action IDs normalize to the canonical dispatch IDs.
- Checked the diff for whitespace errors and reviewed the associated snapshots, ratchets, and failure-path tests. No swallowed error, injection/path/permission issue, race, or missing regression test was found that blocks landing.

## Tests

Scoped suites passed:

- `thegn-core` config: 574 passed
- `thegn-core` capability: 20 passed
- `thegn-host` keymap: 62 passed
- `thegn-host` CLI help: 2 passed
- `thegn-host` palette: 45 passed
- `thegn-host` workspace: 131 passed
- `thegn-svc --test control_schema`: 1 passed
- `git diff --check main...HEAD`: passed

The required queue-boundary gate was run. The initial `THEGN_ALLOW_HEAVY=1 just test` attempt was stopped before tests by the sandbox's sccache read-only-filesystem error. Per the environmental exception, it was retried with `RUSTC_WRAPPER=` and passed: 7,093 passed, 22 skipped, 0 failed. No socket-denial failures occurred, so no tests are excluded or unverified.

No source fix was necessary; this committed file is the review handoff artifact.
