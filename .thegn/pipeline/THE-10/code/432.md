# THE-10 code handoff — row 432

Verdict: done

Implemented exactly Chunk 3 (`.thegn/pipeline/THE-10/code/chunk-3.md`) with
the required subject:

`docs(the-10): update project vocabulary, help ratchets, and snapshots`

Changes:

- Updated the README, changelog, and scoped help pages to use **project** for
  the one-repo product concept and **program** for multi-repo groups.
- Documented canonical `projects_dir`, `THEGN_PROJECTS_DIR`, `projectsDir`,
  `thegn program`, and `--program`, including the three-release compatibility
  window and legacy aliases.
- Retained the stable `workspaces-and-worktrees` page id and filename while
  switching its title, action claims, and prose to project vocabulary; legacy
  workspace action ids remain documented and covered by the help ratchet.
- Updated all 43 inventoried matching Linux muse snapshots to the reviewed
  `PROJECTS` presentation. The two nonmatching `40x12` snapshots were left
  unchanged.

Verification:

- `RUSTC_WRAPPER= just quick thegn-host` — passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host help keymap --no-fail-fast` —
  137 passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host action_docs_ratchet claimed_actions_are_mentioned_in_the_page_body page_action_claims_are_real_action_ids --no-fail-fast` — 3 passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-core --test config_example --no-fail-fast` — 3 passed.
- `bash test/stale-docs-guard.sh` and `git diff --check` — passed.
- Snapshot scope audit — 43 changed, no `40x12` changes.

Unverified: full workspace/CI/coverage/smoke/e2e suites, live state DB,
migrations, and external integrations were not run per the chunk policy. The
shorthand `cargo nextest run -p thegn-core config_example` selected zero tests
in this checkout; the explicit integration target above passed.

Next: supervisor may verify this tracked artifact and advance Chunk 3.
