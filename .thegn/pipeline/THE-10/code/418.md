# THE-10 code handoff — row 418

Verdict: done

Implemented exactly Chunk 2 (`.thegn/pipeline/THE-10/code/chunk-2.md`) with
the required subject:

`feat(the-10): rename workspace presentation and preserve CLI/keymap aliases`

Changes:

- Made `thegn program` and `wt new --program` canonical while retaining the
  multi-repo `project` command and `--project` flag as visible, warning aliases.
  Warnings are raw-argv based, stderr-only, and do not alter JSON stdout.
- Renamed host-edge project picker, sidebar, menu, modal, status, onboarding,
  panel, palette, and help presentation while preserving internal workspace,
  database, provider, and state identifiers.
- Changed canonical project action ids and palette entries, retained all legacy
  workspace action/keybind spellings and chords, and covered legacy summon
  overrides in hint resolution.
- Updated smoke coverage to use canonical program vocabulary and assert legacy
  CLI compatibility.

Verification:

- `RUSTC_WRAPPER= just quick thegn-host` — passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host keymap cli_help palette workspace --no-fail-fast` — 227 passed.
- `cargo fmt --all`, `git diff --check`, and `bash -n test/smoke.sh` — passed.
- Isolated CLI checks — canonical/legacy program JSON, help aliases, and both
  deprecation warnings passed.
- Isolated smoke subset through the program/`wt new` section — passed.

Unverified: full workspace/CI/coverage/e2e suites, live state DB, migrations,
and external integrations were not run per the chunk policy.

Next: supervisor may verify this tracked artifact and advance the stage.
