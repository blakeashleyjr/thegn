# THE-10 reconcile handoff — row 455

Verdict: done

Resolved the already-started merge of current `main` into the THE-10 rename
lane. Preserved main's event-feed documentation, drawer-runtime integration,
localized action metadata, and per-project hooks/editor examples while keeping
the lane's canonical project vocabulary, project action ids, and CLI help.

Commits:

- `1ac8c383 Merge branch 'main' into tg/the-10-rename-projects`
- This handoff artifact is committed separately after the merge commit.

Verification:

- `RUSTC_WRAPPER= just quick thegn-core` — passed.
- `RUSTC_WRAPPER= just quick thegn-host` — passed after retaining the localized
  new-project prompt path and applying the canonical project wording.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host complete help catalog_tests`
  — 91 passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-core --test config_example` —
  3 passed.
- `git diff --check` and scoped conflict-file whitespace checks — passed.
- Pre-commit shellcheck and treefmt hooks — passed.

The shorthand `cargo nextest run -p thegn-core config_example` selected zero
tests and exited 4 in this checkout; the explicit integration target above
passed. Full workspace/CI/coverage/smoke/e2e suites, migrations, live state DB
access, and external integrations were not run per the reconcile spec.

Next: supervisor may verify this tracked artifact and close row 455.
