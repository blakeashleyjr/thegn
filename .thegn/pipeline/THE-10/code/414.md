# THE-10 code handoff — row 414

Verdict: done

Implemented exactly Chunk 1 (`.thegn/pipeline/THE-10/code/chunk-1.md`) with
the required subject:

`fix(the-10): add project config compatibility and program capability aliases`

Changes:

- Added the pure `config_compat` normalizer with a three-stable-release window,
  canonical-wins duplicate diagnostics, legacy scalar/UI/table aliases, and
  canonical dotted-key handling.
- Kept internal `workspaces_dir`/`Workspace*` names stable while serde/schema,
  config writes, examples, and Home Manager output use `projects_*`/`project`.
- Added canonical `program.*` capability rows plus deprecated, parity-checked
  `project.*` aliases; `for_verb`, surface coverage, ledgers, and gap ratchets
  handle both identifiers without adding routes.
- Added focused compatibility, validator, environment, write-path, catalog,
  example-schema, and Home Manager drift coverage.

Verification:

- `RUSTC_WRAPPER= just quick thegn-core` — passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-core config --no-fail-fast` —
  572 passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-core capability --no-fail-fast` —
  20 passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-core --test hm_module_drift
--no-fail-fast` — 2 passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-core --test env_overlay_coverage
--no-fail-fast` — 2 passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-svc --test control_schema
--no-fail-fast` — 1 passed.
- Focused compatibility/write tests — 3 passed; `git diff --check` passed.

Unverified: full workspace/CI/coverage/smoke/e2e suites, live Home Manager
evaluation, migrations, live state DB, and external integrations were not run
per the chunk policy. The first quick/test attempts used the inherited sccache
wrapper and hit its read-only filesystem; wrapper-free reruns passed. The
literal `hm_module_drift` filter selected zero tests, so the explicit integration
target was run and passed.

Next: supervisor may verify this tracked artifact and advance Chunk 1 to review.
