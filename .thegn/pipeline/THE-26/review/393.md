# THE-26 security/test/bug review — row 393

Verdict: PASS

Ready for the merge queue. The prior row-386 findings are fixed, and this
review also fixed the remaining sensitive-array redaction gap and hardened
crash-report directory/file reads and writes against symlink/path races.
The code and this artifact are present in the worktree; committing is blocked
by the shared Git metadata being read-only (`index.lock`).

## Verification

- `RUSTC_WRAPPER= cargo nextest run -p thegn-core -E
  'test(/diagnostics|log_redact|debug/)'`: 30 passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host -E
  'test(/bundle|doctor|help/)'`: 104 passed.
- `cargo fmt --all -- --check` and `git diff --check`: passed.
- Required `THEGN_ALLOW_HEAVY=1 just test` was attempted exactly and stopped
  before tests because the sandbox denied sccache. The mandated wrapper-disabled
  retry reached tests; the only failures were sandbox socket permissions.
- Accepted queue-boundary gate, with only the confirmed sandbox fixtures below
  excluded: `RUSTC_WRAPPER= cargo nextest run --workspace ...`: 6,978 passed,
  89 skipped. Ratchets, workspace coverage tests, and all runnable tests were
  green.

## Sandbox-unverified fixtures

These 67 tests were excluded because they fail only while opening loopback or
Unix sockets (or local HTTP mock listeners) with `PermissionDenied`/
`Operation not permitted`; they are not code failures in this environment:

- `thegn-core::dns_filter::tests::{forward_query_round_trips_against_fake_resolver,forward_query_times_out_when_no_resolver,server_end_to_end_singleton_and_events,server_forwards_allowed_query_to_configured_upstream}`
- `thegn-core::ssh_creds::tests::unix_socket_alive_probes_connectability`
- `thegn-core::sandbox_dns_e2e::{a1_blocked_domain_returns_nxdomain,a2_drain_clears_ring,a3_singleton_reuses_port,a4_multiple_queries_all_logged}`
- `thegn-host::bridge_sup::tests::connect_registers_routes_and_forwards_then_disconnect`
- `thegn-host::daemon::service::tests::ws_warm_attach_pipeline_over_a_real_socket`
- `thegn-host::forward::tests::{remap_on_conflict_sets_flag,start_and_views_then_stop,stop_all_on_returns_ports}`
- `thegn-host::handlers::worktree_attach::tests::silent_daemon_hits_the_probe_timeout_instead_of_hanging_the_worker`
- `thegn-svc::bridge::tests::{connection_close_delivers_exit_to_proc_subscribers,connection_close_disconnects_fs_watch_subscribers,connection_close_fails_pending_calls_fast,exec_batch_runs_all_in_one_round_trip_and_preserves_order,exec_git_status_parses_like_cli,exec_roundtrip_success_and_failure,fs_watch_streams_create_events_and_filters_git_churn,gix_status_routes_through_registered_bridge,malformed_frame_is_skipped_and_loop_survives,proc_list_includes_this_process_cwd,proc_out_with_invalid_base64_is_dropped_not_emptied,proc_stdin_does_not_block_read_loop_when_child_ignores_stdin,proc_stdin_rejects_invalid_base64,read_timeout_is_shorter_than_the_write_default,spawn_proc_reports_exit_code,spawn_proc_streams_stdin_to_stdout_then_exits,unknown_method_is_an_error_not_a_hang,unknown_response_id_is_ignored}`
- `thegn-svc::control::client::tests::attach_refuses_an_incompatible_daemon_proto`
- `thegn-svc::fly::tests::create_4xx_after_app_exists_tears_down_not_orphans`
- `thegn-svc::host::cloud::tests::{connect_daytona_pings_sandbox_list,connect_maps_auth_failures_to_token_rejected,connect_pings_the_provider_list_with_bearer_auth,deliver_daytona_conflict_is_idempotent_success,deliver_daytona_registers_a_snapshot_from_the_registry_ref,deliver_sprites_ensures_the_template_sprite_exists,image_present_daytona_matches_template_name_in_snapshot_list,image_present_sprites_checks_the_sprite_list}`
- `thegn-svc::ipc::tests::{racing_binders_on_a_stale_socket_elect_exactly_one_daemon,unix_bind_is_the_lock_and_round_trips}`
- `thegn-svc::iroh_reach::tests::{handshake_timeout_closes_stalled_pre_auth_connection,dead_connection_is_evicted_from_registry}`
- `thegn-svc::revtunnel::tests::{bounded_tunnel_round_trips_backpressured_payload,host_skips_startup_preamble_before_frames,reverse_tunnel_multiplexes_concurrent_connections,reverse_tunnel_handles_large_payload,reverse_tunnel_round_trips_over_mock_stream}`
- `thegn-svc::fly_mock::create_list_destroy_with_ledger`
- `thegn-svc::sprites_mock::{create_gives_up_bounded_on_persistent_500,destroy_gives_up_bounded_on_persistent_500,destroy_retries_transient_500_then_succeeds,ensure_exists_skips_when_present_creates_when_absent,create_retries_transient_500_then_succeeds,wait_ready_errors_on_deadline,provider_enum_dispatch_against_mock,full_provider_flow_against_recorded_api,ensure_executable_pushes_once_then_idempotent,wait_ready_polls_past_a_transient_failure,wait_ready_returns_ok_when_fs_answers,write_exec_uses_0755_mode}`
- `thegn-svc::{vps_mock::create_list_destroy_round_trip_with_ledger,vps_do_mock::create_list_destroy_round_trip_with_ledger}`

No other findings. The merge step remains `thegn integrate`.

## Handoff limitation

The requested `git add`/commit could not run because the shared worktree Git
metadata rejects `index.lock` with `Read-only file system`. The live
`thegn dispatch report 393 ...` command was attempted; the installed binary
does not contain `dispatch`. The freshly built `target/debug/thegn dispatch
report 393 ...` was also attempted without changing `XDG_STATE_HOME` and
reached the live state path, which failed with `attempt to write a readonly
database`. The artifact is therefore written but untracked in this managed
environment; no row status was changed.
