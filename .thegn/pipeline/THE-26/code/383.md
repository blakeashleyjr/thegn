# THE-26 code handoff — row 383

Verdict: complete.

Implemented the debugging workflow documentation chunk:

- Added and registered `docs/help/debugging.md`, covering doctor/reporting,
  redacted bundles and their current-process limitation, tracing versus raw
  stderr capture, perf/Telemetry/flame-graph surfaces, and BugStalker-only
  debugger support and limits.
- Linked the page from the help index and CLI help.
- Corrected only the `[log]` diagnostics comments in
  `config/config.toml.example` to distinguish tracing sinks from raw stderr
  capture; no keys or runtime behavior changed.

Verification:

- `XDG_RUNTIME_DIR=/tmp RUSTC_WRAPPER= just quick thegn-host` — passed.
- `XDG_RUNTIME_DIR=/tmp RUSTC_WRAPPER= cargo nextest run -p thegn-host help` —
  passed, 75 tests (2,599 skipped).
- `git diff --check` — passed.

The literal commands without the environment workaround could not run in this
managed shell: `just` could not create `/run/user/1000/just` because it is
read-only, and the configured `sccache` wrapper returned `Operation not
permitted`. The equivalent scoped checks passed with runtime temp files under
`/tmp` and the wrapper disabled. No full workspace, e2e, or heavy gate was run.

Commit subject: `docs(the-26): document debugging surfaces`

Handoff limitation: the commit could not be created because the worktree's
Git metadata is read-only. The required live `thegn dispatch report 383
--text ...` was also attempted: the installed binary is stale and lacks the
`dispatch` subcommand, while the freshly built binary reached the command but
the live state database was read-only. The row status was not changed.
