# THE-26 code handoff — row 377

Verdict: complete.

Implemented the chunk-1 diagnostic hardening in the three allowed files:

- Added a span-preserving text-line redactor that reuses the canonical argv/key
  rules, including explicit coverage for the unsupported bare-positional-secret
  limitation.
- Redacted panic text and retained WARN-ring lines at `CrashReport::render`.
- Re-redacted historical crash report contents before bundle inclusion,
  replacing invalid UTF-8 rather than copying unvalidated bytes.
- Added `diagnostics/ring.log` to every bundle with a deterministic,
  clearly-labeled current-process WARN-ring header and `(none)` for an empty
  snapshot.
- Added focused unit coverage for text redaction, crash rendering, historical
  report sanitization, and empty/non-empty ring entries.

Commit subject: `fix(the-26): harden diagnostic bundle redaction`

Verification (sandbox-compatible environment overrides only: `RUSTC_WRAPPER=`
and, for `just`, `XDG_RUNTIME_DIR=/tmp TMPDIR=/tmp`):

- `just quick thegn-core` — passed
- `cargo nextest run -p thegn-core diagnostics` — 12 passed
- `cargo nextest run -p thegn-core log_redact` — 10 passed
- `just quick thegn-host` — passed
- `cargo nextest run -p thegn-host bundle` — 7 passed
- `cargo fmt --all -- --check` and `git diff --check` — passed

The default `just` runtime directory and inherited `sccache` wrapper were
blocked by this sandbox, so those overrides were required. No full workspace,
e2e, `just test`, or `just ci` gates were run per the chunk policy. Changed
paths are exactly the three specified source files plus this tracked handoff;
config, openspec, capability, CLI/debugger, and ratchet paths are unchanged.
