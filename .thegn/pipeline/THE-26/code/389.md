# THE-26 code handoff — row 389

Verdict: complete.

Implemented the debugger-audit fixes from review row 386:

- Extended text redaction to cover `Authorization:` and
  `Proxy-Authorization:` headers, cookie payloads, JSON fields, and other
  colon-delimited sensitive fields.
- Restricted retained crash-report enumeration and bundle reads to regular
  files, excluding symlinks and non-regular entries.
- Changed retained-report read failures from silent omission to an explicit
  `omitted` manifest entry while allowing the bundle to complete.
- Added regression tests for all supported redaction shapes, symlink/non-regular
  report filtering, and the bundle omission path.

Verification:

- `XDG_RUNTIME_DIR=/tmp RUSTC_WRAPPER= just quick thegn-core` — passed.
- `XDG_RUNTIME_DIR=/tmp RUSTC_WRAPPER= just quick thegn-host` — passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-core -E
'test(/diagnostics|log_redact/)'` — passed, 24 tests.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host -E 'test(/bundle/)'` —
  passed, 8 tests.
- `cargo fmt --all -- --check` and `git diff --check` — passed.

The default `sccache` wrapper could not run in this managed sandbox
(`Operation not permitted`), and `just` initially could not create its default
runtime temp directory because `/run/user/1000/just` is read-only. The scoped
checks passed with the wrapper disabled and `XDG_RUNTIME_DIR=/tmp`. No full
workspace, e2e, or heavy gate was run, per the dev-loop policy.

Commit subject: `fix(the-26): harden diagnostics redaction and crash bundles`

Handoff limitation: the implementation and artifact could not be committed
because the shared worktree Git metadata is read-only (`index.lock`). The live
`thegn dispatch report 389` attempt was also blocked by the read-only state
database; the installed binary is stale and lacks `dispatch`, while the fresh
binary reached the command and failed on that database write. Row status was
not changed.
