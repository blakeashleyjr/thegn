APPROVED

Reviewed the full `git diff main...HEAD` after merging `main` in commit
`e1e66583`. No revision chunk is required.

The implementation matches the THE-9 design: merge-queue state is rolled up
in thegn-core, the right-sidebar token uses the existing capability and palette
seams, hit geometry is shared with rendering, and activation routes through
the existing panel/tab/open-section flow. The ambient bottom-bar badge is
removed while the configured `mq` widget remains supported. Configuration,
help, changelog, and the in-flight OpenSpec change are synchronized.

During review I corrected a semantic edge case in
`crates/thegn-host/src/handlers/sidebar_mouse.rs`: a workspace row's leading
divider gap was included in the row hit rectangle, so clicking that unpainted
gap at the token's x-coordinate could open the merge queue. The token route now
requires the y-coordinate to be within the painted row, with a regression test
covering the gap. This is committed as `87891ed3`.

Verification completed:

- Core required nextest filter: 324 passed, 1 unrelated pre-existing failure,
  3085 skipped. The failure is
  `thegn-core::sandbox::tests::oci_local_secrets_go_to_env_file_not_argv` at
  `crates/thegn-core/src/sandbox_tests.rs:1361`; the test and implementation
  are unchanged from `main`.
- Host required nextest filter: 104 passed, 0 failed.
- `thegn-svc` control schema: 1 passed.
- `just quick`: passed.
- Required clippy checks for touched crates: passed, including the final
  post-correction host check.
- `treefmt`: passed with 0 files changed.
- `openspec validate --all --strict`: all 170 items passed.
- Rust documentation with `-D warnings`: passed for `thegn-core` and
  `thegn-host`.
- Focused THE-9/core, sidebar, status-bar, detail, and render-plan tests:
  passed; the post-correction sidebar mouse suite had 24 passed.
- `cargo fmt --all -- --check` and `git diff --check`: passed.

Unverified items are deliberate or environmental:

- Muse/e2e snapshots listed in `architect/design.md` remain unverified because
  the lead addendum prohibits running e2e/coverage/full `just test` or `just
ci` during this review. They include the focused-sidebar, panel-work,
  chrome-regions, responsive-breakpoints, glitch-hunt-bars,
  glitch-hunt-panel-accordion, and four themed snapshots listed there.
- `test/ratchet-check.sh` is absent from the repository; the required core and
  host ratchet test filters passed.
- The understand-anything knowledge graph was not present in the worktree, so
  no diff overlay could be generated by the requested review skill.

The only automated red is the unchanged sandbox baseline failure above; it is
not caused by THE-9.
