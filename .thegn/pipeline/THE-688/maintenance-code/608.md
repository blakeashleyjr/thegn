# THE-688 implementation handoff — native dispatch 608

verdict: source-review-clear

## Scope implemented

- Removed the obsolete `has_persisted_worktree_session` ownership-shaped API,
  implementation, cleanup veto, and dedicated test.
- Added `delete_tab_groups_for_worktree_all_sessions`, deleting dependent
  `group_tabs` rows before `tab_groups` rows by worktree path. Automatic merge
  bookkeeping invokes it inside the existing post-removal transaction next to
  `db.del_worktree(worktree)`.
- Preserved the session-scoped deletion used by interactive/headless `wt rm`.
- Added regression coverage for collection with layouts in two sessions,
  including child tabs, and for live-session, tenancy, and nonterminal-dispatch
  refusals in both ordinary and forced sweeps. Existing report vocabulary and
  THE-685/THE-686/THE-687 areas were left unchanged.

## Checks performed

- `rustfmt --edition 2024` on all changed Rust files — passed.
- `git diff --check` — passed.
- Static `rg` checks confirmed no production Rust reference to
  `has_persisted_worktree_session`; only the historical investigation artifact
  mentions it.
- Inspected the affected `test/smoke.sh` sweep assertions; no stale layout or
  ownership wording required a change.

## Unverified work

Cargo/build/test/Clippy/smoke validation was not run because this dispatch is
CPU-constrained and the primary centrally schedules Rust validation. Do not
interpret this handoff as a full gate result. The following exact commands
remain for primary validation:

```text
nix develop --command cargo check -p thegn-host --all-targets
nix develop --command cargo nextest run -p thegn-core delete_tab_groups_for_worktree_all_sessions
nix develop --command cargo nextest run -p thegn-host merge_sweep
nix develop --command cargo nextest run -p thegn-host merge_cleanup
just smoke
```

No full gate, compilation result, nextest result, Clippy result, or smoke
result is claimed here.

## Findings for the next stage

The live-session regression uses `session_start_once` and an RAII release so
the remaining process-local liveness guard, rather than persisted layout,
proves the refusal. All-session layout deletion is intentionally separate from
the existing session-scoped operation because persisted rows are not schema-
constrained to one session per worktree.

commits: 4a03eda2 fix(the-688): treat persisted layouts as cleanup bookkeeping
