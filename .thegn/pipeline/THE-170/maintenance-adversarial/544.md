# THE-170 adversarial source review — dispatch 544

Verdict: source-review-clear

## Scope

Reviewed the implementation commit `621419a3` against the captured issue,
`CLAUDE.md`, `docs/ARCHITECTURE.md`, and the primary instructions in
`.thegn/maintenance/primary-plan.md`. The review covered the plugin adapter,
host provider registry, issue seam/router lifetime changes, all
`ForgeError::NotConfigured` constructor/match sites, reload/provider-list call
paths, and the new regression tests. No production behavior was changed by
this dispatch.

## Findings

No exploitable bypass, race, permanent-allocation path, missing production call
path, or production-code defect was found.

- Dynamic plugin identities are allocated as `Arc<str>` in
  `crates/thegn-svc/src/plugin/provider.rs:191-212`, and the adapter returns a
  borrow from its owned identity at `:256-259`. Adapter and router drops can
  therefore reclaim each generation; no global registry or `'static`
  promotion remains.
- The public issue seam now returns `&str` at
  `crates/thegn-svc/src/issue/mod.rs:276-278`. Router/provider-list surfaces
  carry borrows tied to the router at `:490-500` and `:602-618`, while async
  operations borrow the adapter, so identity use cannot outlive its owner.
- The host registry still replaces its published snapshot at
  `crates/thegn-host/src/plugin_providers.rs:23-27`; adapter construction at
  `:29-47` shares only the live bridge and owns the identity per router.
- `ForgeError::NotConfigured` is now `Cow<'static, str>` at
  `crates/thegn-core/src/forge/mod.rs:43-48`; the dynamic runtime-build path
  uses `Cow::Owned` at `crates/thegn-svc/src/forge/native.rs:37-39` and is
  wired into `graphql` at `:592-601`. Literal diagnostics remain borrowed.
- A source scan found no production `Box::leak`, `into_boxed_str`,
  `String::leak`, `Box::into_raw`, or equivalent dynamic-name leak in the
  relevant crates. The sole remaining match is the documented test-only lane
  at `crates/thegn-core/src/util.rs:1585-1590`, which the primary plan says to
  leave unchanged.

## Test review and authorized addition

The existing 4,096-generation churn test at
`crates/thegn-svc/src/plugin/provider.rs:521-558` deterministically checks
identity ownership and release counts rather than relying on flaky RSS
thresholds. The existing round-trip test covers plugin async calls. I added a
test-only replacement regression at `:561-608` that drops one router, rebuilds
with a new dynamic identity, and verifies both `get_issue` calls reach the
adapter/plugin reply path rather than the router id-miss path.

## Lightweight verification performed

- `rustfmt --edition 2024 crates/thegn-svc/src/plugin/provider.rs`
- `git diff --check`
- repository source scan for permanent-allocation idioms listed above

## Explicitly unverified / commands for primary execution

No Cargo command, build, nextest, Clippy, `just quick`, or full test suite was
run. Full Rust validation remains pending under primary ownership. Execute the
following exact focused commands, followed by the centrally scheduled gate:

```text
cargo nextest run -p thegn-svc adapter_identity_churn_is_owned_and_router_replacements_route_by_generation
cargo nextest run -p thegn-svc router_replacement_keeps_dynamic_identity_on_async_route
cargo nextest run -p thegn-svc plugin_namespace_routes_by_complete_registered_prefix
cargo nextest run -p thegn-svc runtime_failure_keeps_dynamic_message_in_owned_not_configured_payload
cargo nextest run -p thegn-svc sdk_graphql_envelopes_fall_through_without_offline_evidence
cargo nextest run -p thegn-core error_classes_and_messages explicit_invalid_push_remote_is_not_replaced_by_origin
just quick
just ci
```

No full gate result is claimed.
