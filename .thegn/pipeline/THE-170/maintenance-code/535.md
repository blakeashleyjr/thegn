# THE-170 implementation handoff — dispatch 535

Verdict: implementation-ready

Implemented the primary-approved plan completely within scope. No concrete
design blocker was found. No new feature, generation registry, interning map,
child dispatch, merge, push, tracker closure, row-status change, or main-branch
edit was performed.

## Implemented

- Replaced the plugin issue adapter's leaked `plugin:<id>` identity with an
  `Arc<str>` owned by each adapter. Router/adapter drops now release old
  generation identities.
- Changed `IssueBackend::provider_id` and the issue router/provider-list
  surfaces from `&'static str` to borrows tied to the backend/router. Built-in
  providers retain borrowed static literals without allocation.
- Updated issue routing test doubles and added a 4,096-generation adapter and
  router churn regression. It asserts exact `plugin:<id>` identities,
  namespace input validation, replacement routing identity, and `Arc` release
  counts after each router drop.
- Replaced `ForgeError::NotConfigured(&'static str)` with
  `Cow<'static, str>`. Literal diagnostics remain borrowed; the native
  GraphQL runtime-build failure now stores its formatted message as owned data.
- Converted all approved forge constructor/match sites and added a regression
  proving the dynamic runtime message survives in a `Cow::Owned` payload.
- Updated the stale host registry comment and left the documented test-only
  `thegn-core/src/util.rs::test_lane` leak unchanged.

## Changed files

- `crates/thegn-core/src/forge/checkout.rs`
- `crates/thegn-core/src/forge/mod.rs`
- `crates/thegn-core/src/forge/model.rs`
- `crates/thegn-core/src/github.rs`
- `crates/thegn-core/src/github_authorship.rs`
- `crates/thegn-host/src/plugin_providers.rs`
- `crates/thegn-svc/src/conformance.rs`
- `crates/thegn-svc/src/forge/mod.rs`
- `crates/thegn-svc/src/forge/native.rs`
- `crates/thegn-svc/src/forge/native_global_connectivity_tests.rs`
- `crates/thegn-svc/src/forge/native_regression_tests.rs`
- `crates/thegn-svc/src/issue/github.rs`
- `crates/thegn-svc/src/issue/jira.rs`
- `crates/thegn-svc/src/issue/kaneo.rs`
- `crates/thegn-svc/src/issue/linear.rs`
- `crates/thegn-svc/src/issue/mod.rs`
- `crates/thegn-svc/src/plugin/provider.rs`

## Verification performed

- `rustfmt --edition 2024` on all changed Rust files.
- `rustfmt --edition 2024 --check` on all changed Rust files.
- `git diff --check`.
- Source audit confirmed every `IssueBackend` implementation uses `&str` and
  the only remaining `Box::leak`/equivalent in `crates` is the existing,
  documented test-only `util.rs::test_lane` site.

## Exact validation commands requiring execution

These were not run by this dispatch because Rust validation is centrally
scheduled and CPU-constrained:

```text
cargo nextest run -p thegn-svc adapter_identity_churn_is_owned_and_router_replacements_route_by_generation
cargo nextest run -p thegn-svc plugin_namespace_routes_by_complete_registered_prefix
cargo nextest run -p thegn-svc runtime_failure_keeps_dynamic_message_in_owned_not_configured_payload
cargo nextest run -p thegn-svc sdk_graphql_envelopes_fall_through_without_offline_evidence
cargo nextest run -p thegn-core error_classes_and_messages explicit_invalid_push_remote_is_not_replaced_by_origin
just quick
just ci
```

No Cargo command, build, nextest, Clippy, or full test suite was run. No full
gate result is claimed; compilation, focused tests, `just quick`, and the
central final gate remain unverified by this dispatch.
