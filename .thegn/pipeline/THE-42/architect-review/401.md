# THE-42 architect review — row 401

## Verdict

REVISE

## Review basis

Reviewed the complete branch diff from merge-base `c19da601` through the
implementation and review-side commits, against
`.thegn/pipeline/THE-42/architect/design.md`, both coder handoffs, and the
panel/media/provider repository seams.

The implementation correctly removes the centered media overlay, keeps media
provider work off the event loop, routes keyboard and mouse transport through
the existing operation path, preserves the action catalog/control schema,
adds the reserved Spotify kind, and passes the relevant ratchets.

## Corrections applied

- `841c0d51` — queue refreshes on a newly available or changed snapshot
  identity, and artwork fetches are single-flight rather than spawned once per
  loop turn.
- `bbdae4ae` — the existing `media.show_art` setting now controls both artwork
  fetching and rendering, including clearing cached artwork on a live toggle.

## Remaining blocking gap

The panel fabricates `MediaCaps` from `MediaState` and never consumes
`MediaBackend::players()`. Consequently provider-specific optional controls are
not reliably gated (for example, MPRIS/playerctl video snapshots can paint
chapter controls despite `chapters: false`), and the promised Half/Full source
list is not an enumeration of available players. Queue responses also lack a
request generation/tag. These require a cross-layer correction and are recorded
in the revision chunk:

[revision-1.md](/home/blake/.superzej/worktrees/thegn/tg-the-42-media/.thegn/pipeline/THE-42/architect-review/revision-1.md)

## Verification

- Required core filter `env_overlay config_example capability`: 26 passed.
- Required service control-schema test: 1 passed.
- Required host filter `complete help catalog_tests mq_assets platform_ratchet`:
  102 passed.
- Focused host media filter after the review corrections: 12 passed.
- `cargo fmt --all -- --check` and `git diff --check`: passed.

Not run: full workspace CI/coverage, e2e/snapshot re-recording, and live
external-provider integration. Those are not the reason for this verdict; the
provider-capability/source-list contract above is.
