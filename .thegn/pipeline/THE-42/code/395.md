# THE-42 code handoff — row 395

Verdict: done

Implemented chunk 1 exactly:

- Added substrate-free `MediaWidth`/`MediaRenderPolicy` projection decisions and
  tests in `thegn-core::media`, including width degradation, capability gating,
  art visibility, and playing-only animation.
- Kept `MediaBackend` boxed/object-safe and changed optional-operation defaults
  to the explicit local `MediaError::Unsupported` classification.
- Moved Linux MPRIS and `playerctl` implementations under
  `thegn-media/src/platform/linux/`, preserving public re-exports and behavior.
- Added reserved `spotify` config/leaf kinds, inert lowering/resolution, and
  reserved doctor coverage without credentials, network, or Spotify CLI use.
- Updated the media config example and pinned the new platform module in the
  platform-cfg ratchet. No env, completion, async-trait, or control-schema
  ratchet was changed.

Commit: `feat(the-42): harden media seam and reserve spotify` (the branch tip
contains the final commit hash).

Checks:

- `just quick thegn-core` — passed.
- `just quick thegn-media` — passed.
- `just quick thegn-svc` — passed.
- `cargo nextest run -p thegn-core media` — 7 passed.
- `cargo nextest run -p thegn-core config` — 569 passed.
- `cargo nextest run -p thegn-media model` — 7 passed.
- `cargo nextest run -p thegn-media platform_cfgs_are_pinned` — 1 passed.
- `cargo nextest run -p thegn-svc reserved_selections` — 2 passed.
- `cargo fmt --all -- --check` and `git diff --check` — passed.

Unverified: the requested `cargo nextest run -p thegn-svc media_probes`
filter has no matching test name in this checkout and therefore ran zero tests;
the actual reserved-selection registry/conformance tests were run instead.
Per chunk policy, no workspace/full CI, e2e, migration, or live-state `thegn`
invocation was run.

Next: chunk 2 can consume the public media policy and reserved Spotify seam.
