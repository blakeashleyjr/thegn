# THE-42 code handoff — row 421

Verdict: done.

Fixed the stale now-playing delivery race identified by review row 416:

- Added a monotonic media watcher/config/player generation shared by the event
  loop and off-loop media producers.
- Tagged watcher, transport, playlist, and disabled-state snapshot deliveries;
  restarting the watcher advances the generation, and the event-loop drain
  rejects obsolete deliveries before they can change the badge or panel.
- Centralized playlist activation in the media-control module so its late
  snapshot is tagged like every other operation result.
- Added a regression test proving an obsolete snapshot queued after a restart
  cannot replace the current-generation snapshot.

Commit: `fix(the-42): reject stale media snapshots` (this handoff commit).

Checks:

- `just quick thegn-host` — passed.
- `cargo nextest run -p thegn-host stale_snapshot_after_restart_cannot_replace_current_generation --no-fail-fast` — 1 passed.
- `cargo fmt --all` and `git diff --check` — passed.

Unverified: full workspace gates, e2e/snapshot re-recording, and live external
media-provider integration were not run, per the targeted dev-loop policy.

Next: reviewer can inspect generation capture at operation creation and the
restart/disable transition behavior.
