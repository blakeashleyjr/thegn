# THE-48 code handoff — row 415

Implemented Chunk 1 (core CI-log contract, cache, config, and catalog).

- Added substrate-free bounded UTF-8 tailing, CI secret redaction, cache-entry
  metadata, candidate identity/dedupe, and retention helpers.
- Added `[ci]` cache bounds plus default-off `[ci.autofix]` policy, with only a
  trusted `[workspace.<slug>.ci]` mode overlay and `Config::repo_ci` resolver.
- Added additive schema v62 CI log and autofix-dedupe tables, verification,
  cache/store operations, retention, and worktree cleanup.
- Added `ci.runs` and `ci.logs` read capabilities, control verbs/scope policy,
  and parameterized MCP state-tool declarations.
- Updated the config example, configuration help, and env-overlay ratchet.

Verification:

- `just quick thegn-core` — passed.
- Targeted `cargo nextest` CI-log, DB migration/cache, capability, MCP state,
  config-example, env-overlay, and completion tests — passed.
- `cargo fmt --all -- --check` and `git diff --check` — passed.
- `treefmt --ci` could not run because `shfmt` is not installed in PATH.

Commit: `feat(the-48): add CI log core contract and catalog`
