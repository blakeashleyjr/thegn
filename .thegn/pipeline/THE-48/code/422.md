# THE-48 code stage — row 422

Verdict: complete.

Implemented chunk 2, “provider bounds, control API, MCP/CLI projection, and
refresh reads”:

- Added bounded, concurrently drained provider log collection for `gh` and
  `glab`, with finite line/byte limits and stderr classification preserved.
- Added read-only `ci.runs` and `ci.logs` control methods, HTTP routes,
  client projections, API tables, and regenerated `docs/api/control-v1.json`.
- Added daemon cache-first CI run/log reads, confined worktree targets,
  blocking provider misses, bounded/redacted log entries, and source/timestamp
  metadata.
- Added MCP `ci.runs`/`ci.logs` state projections and the cache-first plural
  `thegn ci logs` command while retaining `ci log` as an alias, including JSON
  output handling.
- Updated completion catalog and slot ratchet for `ci logs`.

Verification:

- `cargo fmt --all -- --check`
- `cargo check -p thegn-svc -p thegn-host --lib`
- `just quick thegn-svc` and `just quick thegn-host`
- `cargo nextest run -p thegn-svc ci control_schema`
- `cargo nextest run -p thegn-host cmd::ci mcp`
- `cargo nextest run -p thegn-core completion`
- `git diff --check`

Not verified: full workspace/pre-push gates, live provider network calls, and
manual binary/e2e behavior, per the chunk dev-loop policy.

Commit subject: `feat(the-48): expose cached CI logs through control surfaces`
