# THE-48 reconcile handoff — row 459

Verdict: complete.

Merged current `main` into `tg/the-48-ci-logs` and resolved the expected
cross-lane conflicts while retaining both feature sets:

- Preserved main's schema v62–v66 ladder, autopilot/config-validation changes,
  typed control errors, event filtering helpers, and generated API/help content.
- Renumbered THE-48's CI cache migration from v62 to the next free version,
  v67, including `SCHEMA_VERSION`, `migrate_v67`, `verify_v67`, the dispatch
  ladder, and the pre-v67 migration regression test.
- Preserved trusted CI configuration and raised the marked-definition pin from
  main's 96 to the actual merged 97; `CiProviderKind` was already on main and
  only `CiAutofixMode` is new in this lane.
- Kept both CI and main control-client methods/helpers and both API snapshot
  definition families. Updated configuration help to document CI autofix,
  autopilot, trusted layers, and repo overlays together.
- Applied two narrowly required post-merge compatibility fixes: the typed
  `BadRequest` code in the CI HTTP helper and `credential_free: false` for the
  existing PR-queue CI agent invocation. Removed one unrelated trailing-space
  error in a merged main handoff so `git diff --check` stays clean.

Verification:

- `XDG_STATE_HOME=/home/blake/.superzej/pipeline-state RUSTC_WRAPPER= just quick thegn-core` — passed.
- `XDG_STATE_HOME=/home/blake/.superzej/pipeline-state RUSTC_WRAPPER= just quick thegn-host` — passed after the two compatibility fixes.
- `XDG_STATE_HOME=/home/blake/.superzej/pipeline-state RUSTC_WRAPPER= cargo nextest run -p thegn-core marked_definition db_migrate config_example` — 22 passed.
- `XDG_STATE_HOME=/home/blake/.superzej/pipeline-state RUSTC_WRAPPER= cargo nextest run -p thegn-svc --test control_schema` — 1 passed.
- `git diff --check HEAD` — passed.

Not verified: full workspace/pre-push/CI gates, live provider/network behavior,
manual daemon or binary behavior, cross-platform builds, and e2e. The whole
workspace `cargo fmt --all -- --check` remains noisy on unrelated main files;
the reconciled files and final diff are clean.
