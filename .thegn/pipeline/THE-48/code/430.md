# THE-48 code handoff — row 430

Verdict: complete.

Implemented Chunk 3, “off-loop ingestion, Work UI, PR prompt evidence, and
autofix”:

- Extended the CI refresh worker to retain bounded failed-job log tails for
  recent terminal runs, preserve provider truncation metadata, redact before
  SQLite, retain per-worktree rows, and pulse the terminal waker.
- Added a guarded `ci_autofix` coordinator using the existing
  `PrCiFailure`/PR-queue prompt and `agent_run` seams. Suggest mode emits a
  deduplicated notification; auto mode verifies queue/agent policy, PR
  ownership, current local/remote head, attempt budget, isolation floor, and
  claims the candidate before spawning.
- Changed CI detail and panel Enter/`v` to use an in-place modal drill. Detail
  reads cached logs first, fetches only missing/stale entries off-loop, and
  displays cached/source, tail/truncation, and redaction metadata.
- Fed bounded cached log evidence into existing PR CI-failure prompts with
  check-URL fallback preserved.
- Updated panel, CLI, and local-CI help for cached logs, `ci logs`, autofix
  policy, and optional local-runner recipes.

Verification:

- `just quick thegn-host` — passed.
- `cargo nextest run -p thegn-host ci_refresh ci_drill pr_driver` — 21 passed.
- `cargo nextest run -p thegn-host pr_driver` — 15 passed.
- `cargo fmt --all -- --check` and `git diff --check` — passed.

Not verified: full workspace/pre-push/CI gates, live forge/provider calls,
manual binary/e2e behavior, and actual agent dispatch against a configured
remote PR. No live state DB or built binary was invoked.

Commit subject: `feat(the-48): add off-loop CI autofix handoff and UI`
