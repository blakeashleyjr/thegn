# THE-22 row 366 — chunk 1 completion

## Verdict

COMPLETE. Merged `main`, confirmed THE-27's public `review::PrReviewSnapshot`
and `forge::model::ReviewThread` substrate, and implemented the per-thread core,
durable roster, notification audit, and forge resolve seam without copying or
renaming THE-27 types.

## Commits

- `374c42e0` — merged `main` into `tg/the-22-watched-pr-tasks`.
- This commit — `feat(the-22): add per-thread review task substrate`.

## Implemented

- Added the substrate-free `pr_review_tasks` reconciler with canonical hashed
  thread/decision source keys, deterministic bounded SHA-256 revisions,
  thread-vs-review-decision fallback identity, bounded/sanitized prompts using
  THE-27's formatter and `TaskKind::PrReview` validation, resolved transitions,
  and bounded `pr.thread_unresolved` events.
- Added typed review-task CRUD on `agent_dispatches`, atomic partial-index
  upsert/dedupe, status/resolve operations, and durable forge-attempt cooldown
  bookkeeping while leaving ordinary pipeline dispatch projections unchanged.
- Added schema v64 fresh columns, additive/idempotent migration, verification,
  migration-from-v61 preservation coverage, and the unique partial source index.
  The work order named v63, but merged `main` already uses v63 for THE-27's
  `pr_review_cache`; v64 is the required next version so existing v63 databases
  do not skip THE-22's migration.
- Added `pr_review_task_queued` / `pr_review_thread_resolved` notification wire
  kinds, bounded once-keyed DB helpers, and audit coverage.
- Added the object-safe `resolve_review_thread` forge operation/capability,
  unsupported-provider default, service-ladder cap/forwarding, and GitHub CLI's
  bounded combined reply+resolve GraphQL mutation.

## Verification

Passed the chunk's scoped checks (sandbox required `XDG_RUNTIME_DIR=/tmp`,
`TMPDIR=/tmp`, and `RUSTC_WRAPPER=` because `/run/user` and the shared sccache
server are unavailable):

- `just quick thegn-core`
- `cargo nextest run -p thegn-core pr_review_tasks` — 9 passed
- `cargo nextest run -p thegn-core db_migrate` — 18 passed
- `cargo nextest run -p thegn-core forge` — 32 passed
- `just quick thegn-svc`
- `cargo nextest run -p thegn-svc forge` — 13 passed
- `git diff --check`
- forge-leak ratchet — clean
- async-trait ratchet — clean

## Not verified

- Full workspace gates, coverage, e2e, built `thegn`, and live-state DB migration
  were intentionally not run under the chunk/dev-loop policy.
- The repository-wide ignored-result ratchet currently fails on six unrelated
  host files and three stale allowlist entries brought in by merged `main`.
  This chunk's new best-effort ALTERs are confined to already-pinned
  `db_migrate.rs`; the chunk forbids editing the unrelated host/ratchet files.

## Next hints

- Chunk 2 can call `reconcile_review_tasks`, `Db::upsert_review_task`,
  `Db::list_review_tasks`, `Db::resolve_review_task`,
  `Db::record_review_forge_attempt`, the two notification helpers, and
  `Forge::resolve_review_thread`.
- Emit each returned event only after its upsert succeeds durably. A revised
  running task retains `Running`, which prevents a concurrent launch while the
  durable prompt/revision records the next input.
- Chunk 3 must add the two new notification kind names to the example config's
  notification-priority prose, as required by the existing config prose test.
