# THE-22 code handoff — dispatch row 357

Verdict: BLOCKED

## Blocker

Chunk 1 has an explicit hard dependency on THE-27 and says to run only after
the Lead merges `tg/the-27-pr-comments-in-diff` into this branch. That merge is
not present at this branch's `HEAD` (`3268e842`):

- `git merge-base --is-ancestor b3aff883 HEAD` exits 1.
- `b3aff883 Merge branch 'tg/the-27-pr-comments-in-diff' (fold-actor)` is
  reachable from `main`, but not from `tg/the-22-watched-pr-tasks`.
- The required THE-27 `review` module and `PrReviewSnapshot` API are absent from
  this checkout. Only the older `forge::model::ReviewThread` is present.

Implementing the chunk in this state would require either taking over the
Lead-owned prerequisite merge or copying/renaming THE-27 types, which the chunk
spec explicitly forbids. No production or test files were changed, and no
tests were run because the required public substrate is unavailable.

## Resume hint

The Lead should merge current `main` (which contains `b3aff883`) or otherwise
merge THE-27 into this branch, resolve any branch conflicts, and then redispatch
chunk 1. The next coder should first confirm that `b3aff883` is an ancestor of
`HEAD` and that `crates/thegn-core/src/review.rs` is available before applying
the scoped THE-22 implementation.
