# THE-22 code handoff — dispatch row 376

## Verdict

BLOCKED at handoff. Implemented architect revision 1's race-safe watched-review
corrections in core persistence, the PR driver, handoff worker, hydration, and
the internal event bus, but this sandbox cannot create the required branch
commit: the shared Git directory is read-only.

## Implemented

- By-number queue rows use the repository root only for read-only forge
  queries. Their durable review task and cache identity retain an empty
  worktree, hydration avoids the empty cache key, and handle exits before any
  checkout/forge/sandbox/agent work.
- Added a durable pending snapshot lane on the existing unique roster row.
  Active spawning/running inputs and cooldowns remain frozen; newer revisions
  are retained once, then either safely accepted for the verified agent push or
  promoted as one queued latest task when feedback changed.
- Added head-independent content fingerprints so a refresh observing the
  agent's verified head is distinguished from a new comment or anchor change.
- Added the typed `pr.thread_unresolved` event-bus projection with its bounded
  `ReviewTaskEvent` payload, published separately from the once-keyed queued
  notification and only after durable upsert success.
- Added schema v65 fresh DDL, additive migration/verification, roster mapping,
  by-number driver coverage, pending persistence/promotion coverage, revision
  dedupe coverage, and event wire-name coverage.

## Verification

Passed with `XDG_RUNTIME_DIR=/tmp/thegn-runtime`,
`CARGO_TARGET_DIR=/tmp/thegn-target-the22`, and `RUSTC_WRAPPER=`:

- `just quick thegn-core`
- `just quick thegn-host`
- `cargo nextest run -p thegn-core pr_review_tasks db_review_tasks event_bus`
- `cargo nextest run -p thegn-core db_migrate`
- `cargo nextest run -p thegn-host pr_driver review_task_handoff pr_queue`
- `cargo fmt --all -- --check`
- `git diff --check`

## Not verified

Full-workspace gates, coverage, e2e, built-binary execution, and live-state DB
migration were intentionally not run per the revision's dev-loop policy.

## Next hints

The implementation must be staged and committed from an environment with write
access to `/home/blake/code/thegn/.git`. The internal event is represented as a
boxed `Event::PrThreadUnresolved` variant to preserve the event-bus enum's size
ratchet; its serialized wire name remains exactly `pr.thread_unresolved`.

The attempted `git add -A` failed before staging because Git could not create
`.git/worktrees/tg-the-22-watched-pr-tasks/index.lock`; a temporary-index retry
also failed while inserting blobs into the shared object database. The required
artifact therefore remains untracked in this session.
