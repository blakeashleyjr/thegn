# THE-22 row 372 — chunk 3 completion

## Verdict

COMPLETE. Aligned the config example, PR-queue help, and in-flight OpenSpec
change with the implemented explicit per-thread watched-review lifecycle. The
draft no longer requires the rejected PR-wide blocker/fingerprint,
`review_trigger`, per-entry override, or reply-never-resolve design.

## Commit

- This commit — `docs(the-22): align watched-review task contracts`.

## Implemented

- Documented that only explicit PR-queue rows whose `watch` includes `review`
  derive one durable task per unresolved provider thread on the existing
  off-loop cadence; there is no all-PR watcher, auto-watch default, second
  timer, or new config key.
- Documented current queue role/review-prompt capture, bounded prompts/events,
  stable thread-key dedupe, revision-in-place for new comments, the
  `pr.thread_unresolved` event, and queued/revised/resolved/needs-human
  notification audit.
- Added `pr-review-task-handle` to PR-queue help frontmatter and documented the
  shared palette/panel `h` action, explicit-only launch, verified-push and
  unchanged-revision checks, provider `resolve_review_thread` capability, and
  unresolved human fallback for unsupported/rate-limited/stale outcomes.
- Replaced the OpenSpec proposal, design, tasks, PR-queue delta, and state-DB
  delta with the final THE-27-dependent pure derivation, shared roster v64,
  off-loop reconciliation, explicit handle, forge resolution, notification,
  and no-external-control/catalog contract.
- Added the two implemented notification wire kinds to the config example's
  exhaustive priority prose. No Rust, env-overlay, completion, control schema,
  capability catalog, or other public snapshot was changed.
- Kept all three empty help debt ratchets unchanged: the new action is both
  claimed and written, and the targeted ratchet tests confirm no debt entry is
  required.

## Verification

Passed the chunk's scoped checks using `XDG_RUNTIME_DIR=/tmp`, `TMPDIR=/tmp`,
and `RUSTC_WRAPPER=` where applicable:

- `just quick thegn-host`
- `cargo nextest run -p thegn-host help` — 77 passed
- `cargo nextest run -p thegn-core config` — 584 passed
- `nix develop --command just openspec validate add-watched-pr-comment-tasks
--strict` — valid
- `git diff --check`
- scope audit — only the seven required config/help/OpenSpec source files plus
  this handoff artifact changed; protected snapshots and Rust remained untouched

The first sandboxed core-config run selected an unrelated DNS-forwarding test
whose UDP socket bind was denied. The identical command was rerun outside the
network-restricted sandbox and all 584 selected tests passed.

## Not verified

- Full-workspace tests, lint, coverage, CI, e2e, built-binary execution, and
  live-state DB migration were intentionally not run under the chunk/dev-loop
  policy.

## Next hints

- The final action id is `pr-review-task-handle`; it is intentionally TUI-only.
- Help/config prose now names both notification kinds:
  `pr_review_task_queued` (notice) and `pr_review_thread_resolved` (info).
- The empty help ratchet allowlists are accurate and should not be regenerated
  into debt; protected env/completion/control/catalog snapshots remain unchanged.
