# THE-22 security/test/bug review — row 385

## Verdict

FAIL — the code review is ready, but the required commit handoff is blocked
because this linked worktree’s common `.git` directory is read-only.

## Findings addressed

- Fixed a stale watched-review completion race. Resolution is now conditional on
  the active source revision and rejects completion when newer feedback is
  pending; the handoff re-reads and requeues the latest revision instead of
  claiming success.
- Fixed command-palette review-task selection when PR rows and task rows are
  interleaved. Added a regression test for uneven multi-PR layouts.
- Added the missing `action-pr-review-task-handle` key to both shipped locale
  catalogs; this restores the i18n coverage ratchet.

## Verification

- `cargo fmt --all -- --check` — passed.
- Core review-task tests: 6/6 passed, including the active-resolution race
  test.
- Host PR-queue tests: 8/8 passed, including uneven interleaving selection.
- `TMPDIR=/tmp XDG_RUNTIME_DIR=/tmp RUSTC_WRAPPER= just quick thegn-core` —
  passed.
- `TMPDIR=/tmp XDG_RUNTIME_DIR=/tmp RUSTC_WRAPPER= just quick thegn-host` —
  passed.
- The required `THEGN_ALLOW_HEAVY=1 just test` was attempted. The initial run
  hit the documented sccache sandbox denial; the wrapper-disabled retry ran
  into only sandbox-denied socket fixtures. The accepted wrapper-disabled
  workspace gate then passed 7,266/7,266 tests (92 intentional/filtered skips).

## Sandbox-unverified tests

The following were individually observed as sandbox permission failures or use
the same confirmed denied socket fixture and were excluded from the accepted
gate. They are environmental, not code failures:

- DNS: `dns_filter::tests::forward_query_round_trips_against_fake_resolver`,
  `dns_filter::tests::forward_query_times_out_when_no_resolver`,
  `dns_filter::tests::server_end_to_end_singleton_and_events`,
  `dns_filter::tests::server_forwards_allowed_query_to_configured_upstream`,
  and integration `sandbox_dns_e2e` tests `a1`–`a4`.
- Local secret-file tests: `sandbox::tests::oci_local_secrets_go_to_env_file_not_argv`,
  `sandbox::tests::systemd_local_secrets_go_to_environment_file_not_argv`.
- Unix/TCP socket fixtures: `ssh_creds::tests::unix_socket_alive_probes_connectability`,
  `bridge_sup::tests::connect_registers_routes_and_forwards_then_disconnect`,
  `daemon::service::tests::ws_warm_attach_pipeline_over_a_real_socket`,
  `forward::tests::{remap_on_conflict_sets_flag,start_and_views_then_stop,stop_all_on_returns_ports}`,
  the `thegn-svc::bridge` socket tests (`connection_close_delivers_exit_to_proc_subscribers`,
  `connection_close_fails_pending_calls_fast`, `connection_close_disconnects_fs_watch_subscribers`,
  `exec_git_status_parses_like_cli`, `exec_roundtrip_success_and_failure`,
  `exec_batch_runs_all_in_one_round_trip_and_preserves_order`,
  `fs_watch_streams_create_events_and_filters_git_churn`,
  `gix_status_routes_through_registered_bridge`, `proc_list_includes_this_process_cwd`,
  `malformed_frame_is_skipped_and_loop_survives`,
  `proc_out_with_invalid_base64_is_dropped_not_emptied`,
  `proc_stdin_does_not_block_read_loop_when_child_ignores_stdin`,
  `spawn_proc_reports_exit_code`, `read_timeout_is_shorter_than_the_write_default`,
  `proc_stdin_rejects_invalid_base64`, `spawn_proc_streams_stdin_to_stdout_then_exits`,
  `unknown_method_is_an_error_not_a_hang`, `unknown_response_id_is_ignored`),
  `control::client::tests::attach_refuses_an_incompatible_daemon_proto`,
  `fly::tests::create_4xx_after_app_exists_tears_down_not_orphans`, cloud mock
  tests (`connect_pings_the_provider_list_with_bearer_auth`,
  `connect_daytona_pings_sandbox_list`, `connect_maps_auth_failures_to_token_rejected`,
  `image_present_daytona_matches_template_name_in_snapshot_list`,
  `image_present_sprites_checks_the_sprite_list`,
  `deliver_daytona_registers_a_snapshot_from_the_registry_ref`,
  `deliver_daytona_conflict_is_idempotent_success`, `deliver_sprites_ensures_the_template_sprite_exists`),
  `ipc` bind tests (`racing_binders_on_a_stale_socket_elect_exactly_one_daemon`,
  `unix_bind_is_the_lock_and_round_trips`), `iroh_reach` endpoint tests
  (`dead_connection_is_evicted_from_registry`,
  `handshake_timeout_closes_stalled_pre_auth_connection`),
  revtunnel loopback tests (`reverse_tunnel_round_trips_over_mock_stream`,
  `reverse_tunnel_multiplexes_concurrent_connections`,
  `reverse_tunnel_handles_large_payload`, `host_skips_startup_preamble_before_frames`,
  `bounded_tunnel_round_trips_backpressured_payload`), and the
  `fly_mock::create_list_destroy_with_ledger`, all 12 `sprites_mock` tests,
  and `vps_do_mock::create_list_destroy_round_trip_with_ledger` integration fixtures.

The lifecycle test
`destroy_waits_for_detached_session_end_before_removing_worktree` failed once
under full-suite contention but passed in isolation; it was retained in the
final gate and passed there. No live forge/provider or external-network E2E was
run.

## Handoff blocker

`git commit` could not create
`/home/blake/code/thegn/.git/worktrees/tg-the-22-watched-pr-tasks/index.lock`
(`Read-only file system`). The fixes and this report remain in the worktree;
they must be committed by a runner with writable git metadata before merge.

The installed `thegn` is an older binary without `dispatch`; the source-built
CLI reached the live roster but could not write it because the live database is
outside this sandbox’s writable roots (`attempt to write a readonly database`).
The live dispatch report therefore could not be filed from this sandbox.
