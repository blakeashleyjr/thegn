# THE-22 architect review — row 381

## Verdict

APPROVED

The implementation matches the THE-22 design and the row-374 revision after
the row-376 handoff corrections. No new revision chunk is required.

## What was checked

- Reviewed the complete THE-22 delta from `374c42e0` through `6c5b8dab`, with
  special attention to the row-376 race-safety changes.
- Confirmed pure per-thread derivation, bounded deterministic revisions and
  prompts, durable partial-index roster identity, additive schema v65, pending
  active-task snapshots, and transient-fetch preservation.
- Confirmed by-number rows use the repository root only for read-only forge
  queries, retain an empty execution worktree, skip the empty cache identity,
  and cannot launch a sandbox or agent.
- Confirmed the typed `pr.thread_unresolved` event is serialized with the
  required wire name and is published separately from the once-keyed queued
  notification after durable upsert.
- Confirmed explicit off-loop TUI/palette handling, exact role/command
  resolution, baseline/head/local-HEAD verification, unresolved-thread
  recheck, capability-gated provider resolution, durable backoff, and human
  fallback.
- Confirmed no new timer, public CLI/MCP/gRPC/control route, capability
  catalog entry, or unrelated configuration surface was added.

## Small corrections applied

- Added a conditional durable review-task claim keyed by the selected source
  revision, preventing a refresh between hydration and `h` from launching a
  stale prompt/baseline.
- Fixed PR-queue cursor hit indices and selection to follow the renderer's
  interleaved PR/task order.
- Promoted pending newer feedback when an active agent exits without a push,
  so the latest revision is queued instead of stranded in a pending lane.

## Verification

- `cargo nextest run -p thegn-core pr_review_tasks db_review_tasks db_migrate event_bus`
  — 51 passed.
- `cargo check -p thegn-host --locked` — passed.
- `cargo fmt --all -- --check` and `git diff --check` — passed.

## Not verified

The targeted host nextest/test-code invocations were started but the sandbox
detached during the lengthy host test-profile codegen; no diagnostics were
reported before termination. Full-workspace gates, coverage, e2e, built-binary
execution, live-state migration, and a real GitHub mutation were not run, in
line with the design's scoped dev-loop policy.

## Handoff note

The shared Git directory is read-only in this execution environment, so
`git add`/`git commit` cannot create its index lock and the branch ref cannot
be advanced. The verdict file is present in the worktree, but the required
Git-tracked commit could not be produced here.
