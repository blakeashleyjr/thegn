# THE-7 row 318 completion

## Verdict

PASS — fixed the fold-gate platform-cfg ratchet without pinning new debt.

## Changes

- Moved the Unix FIFO test setup behind `test_fifo_supported` / `test_fifo` in
  `crates/thegn-host/src/platform/mod.rs`; all Unix and non-Unix `#[cfg]` arms
  now live inside the platform seam.
- Made the `theme_store.rs` FIFO regression test platform-free. It retains the
  Unix assertion and exits without exercising FIFO behavior on unsupported
  non-Unix targets, equivalent to the test's prior non-Unix exclusion.
- Did not change production read or write paths. Exclusive temporary-file
  creation, final-component no-follow opens, bounded reads, permission
  preservation, and atomic rename behavior remain exactly as implemented in
  row 307.
- Did not add `theme_store.rs` to `test/platform-cfg-host-ratchet.txt`.

## Verification

- `cargo fmt --all -- --check` — passed.
- `git diff --check` — passed.
- `just quick thegn-host` — passed.
- `cargo nextest run -p thegn-host -E 'test(platform_cfgs_live_in_platform_modules)'`
  — 1 passed, 2,724 skipped.
- `cargo nextest run -p thegn-host -E 'test(bounded_reader_rejects_a_fifo_without_blocking)'`
  — 1 passed, 2,724 skipped.
- `THEGN_ALLOW_HEAVY=1 just test` — passed: 7,137 tests passed, 22 skipped.

The direct nextest runs and the successful full gate used `RUSTC_WRAPPER=`
because this managed execution environment rejects the configured sccache
daemon with `Operation not permitted`. The same full `just test` recipe and
workspace suite ran unchanged.

## Unverified

- The non-Unix helper arm was not cross-compiled or run on a non-Unix host.
- Coverage, cross-compilation, and e2e/visual suites were outside this gate-fix
  work order and were not run.

## Behavior changes

None. This is test-only platform code motion; production security and I/O
behavior are unchanged.

## Commit

- `fix(the-7): restore platform cfg ratchet` (implementation and this handoff).
