# THE-482 adversarial source review — native dispatch 529

verdict: source-review-clear

## Scope and outcome

Reviewed the implementation commit `4df0b47d` against `.thegn/maintenance/issue.json`,
`.thegn/maintenance/primary-plan.md`, the primary coordination brief, `CLAUDE.md`,
and `docs/ARCHITECTURE.md`. The source review found no remaining production-code
finding. No production code was changed in this review.

The core-to-UI call path is complete: `CalendarDocs::from_config` resolves the
effective explicit/auto `week_start`, `render::month_section` requests
`MonthGrid::week_numbers()` only when enabled, and the row renderer emits that
vector as the visible gutter. The configured-start definition is documented in
`crates/thegn-core/src/calendar/grid.rs:113-123`,
`crates/thegn-core/src/config_calendar.rs:37-41`, and
`config/config.toml.example:347-349`. Monday remains tied to each row's existing
ISO value by the compatibility assertions in
`crates/thegn-core/src/calendar/tests.rs:97-107`; non-Monday crossover behavior is
asserted at lines 109-153.

## Findings

None remaining after the authorized test-only additions below.

The adversarial review specifically checked:

- year rollover and week-53 vectors for Monday, Sunday, and Saturday starts;
- the Sunday/Saturday rows that previously mixed ISO weeks;
- explicit and auto-resolved week starts through the existing locale resolver;
- preservation of `DayCell.iso_week` semantics and the downstream render call path;
- checked date/year arithmetic and the min/max representable-date behavior;
- absence of a new config key, locale-numbering table, resource growth, async/race
  path, or alternate gutter computation;
- unconditional ISO wording in the changed documentation surfaces and the calendar
  help/snapshot search results.

## Authorized regression tests added

Only `crates/thegn-core/src/calendar/tests.rs` was changed:

- `auto_week_start_feeds_the_truthful_row_numbering` (lines 155-170) binds the
  existing `en_US`, `ar_EG`, and `en_GB` auto-resolution cases to actual row labels.
- `week_numbering_stays_total_at_chrono_date_boundaries` (lines 173-201) exercises
  the minimum-date special case and the final representable-year upper-bound case
  without changing the production `MonthGrid::build` contract.

## Validation status

Local non-Rust checks run:

- `rustfmt --edition 2024 --check crates/thegn-core/src/calendar/grid.rs crates/thegn-core/src/calendar/tests.rs crates/thegn-core/src/calendar/mod.rs crates/thegn-core/src/config_calendar.rs crates/thegn-host/src/detail/calendar/layout.rs`
- `git diff --check`
- read-only source/call-path/documentation review

Not run by instruction: Cargo, builds, nextest, Clippy, coverage, full test suites,
UI snapshots, and the primary batch gate. The new tests are therefore compile/test
unverified here. The primary must execute the focused command:

```text
cargo test -p thegn-core calendar::tests:: -- --nocapture
```

The primary must also run the centrally scheduled Rust validation and the generated
config/schema/help/snapshot checks affected by the changed `show_week_numbers`
description. This review does not claim any full gate passed.

## Commit handoff

This artifact and the test-only additions are committed on the dispatch branch.
