# THE-56 code handoff — row 399

Verdict: complete for `chunk-2.md` (off-loop issue pickup, implementation-to-PR bridge, merge completion hook, and bounded status projection).

Implemented:

- Added the host-side autopilot driver: durable claims, worktree/dispatch linkage, existing issue-agent execution, validation, push, PR creation, and optional PR-queue enqueueing.
- Hooked successful tracker refreshes to pickup with authenticated assignee-filter provenance.
- Hooked the PR queue's observed `merged` transition to optional tracker Done sync and autopilot completion.
- Added `autopilot status [--repo PATH] [--json]`, capability/control/completion entries, and workflow/CLI documentation.

Verification:

- `just quick thegn-host` — passed.
- `cargo nextest run -p thegn-host autopilot hydrate_tracker pr_queue` — 9 selected tests passed.
- `cargo nextest run -p thegn-core capability` — 18 selected tests passed.
- `cargo nextest run -p thegn-host completion` — 15 selected tests passed.
- `cargo fmt --all` and `git diff --check` — passed.

Could not verify: no live tracker, forge, agent-process, PR-queue, or end-to-end integration was run; the full workspace suite and CI were intentionally not run per the chunk's dev-loop policy.

Next hint: review the blocking integration seams, bounded status output, and merged-only completion callback with mocks or an integration fixture.
