# THE-56 code handoff — row 398

Verdict: complete for `chunk-1.md` (core autopilot policy, config, and durable claim journal).

Implemented:

- Added substrate-free autopilot states, provider/account-qualified issue keys,
  exact label/status/assignee-filter matching, claim/attempt policy, legal
  transitions, and bounded diagnostics.
- Added default-off `[autopilot]` config with trusted
  `[workspace.<slug>.autopilot]` overlays, validation, example documentation,
  operator help, and env-overlay ratchet entries.
- Added the `AutopilotStore` seam and SQLite `autopilot_runs` journal with an
  atomic immediate transaction claim, unique provider/account/issue identity,
  active concurrency budget, expected-state transitions, dispatch/PR
  correlation, bounded listing, and PR lookup.
- Added the additive schema v63 migration and verifier. Schema v62 remains
  reserved for THE-27/THE-48 reconciliation as required by the chunk spec.

Verification:

- `RUSTC_WRAPPER= just quick thegn-core` — passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-core autopilot db_migrate config` —
  571/572 selected-pattern matches passed; the one failure was the example
  documentation ratchet and passed on rerun after adding all workspace overlay
  keys.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-core --test config_example example_config_documents_every_section_and_key` — passed.
- Initial wrapper-enabled quick check could not start because sccache hit a
  read-only filesystem; rerun with the wrapper disabled passed.

Commit: `feat(the-56): add autopilot claim policy and journal`.
Next hint: chunk 2 is serial and should consume the public config/policy/store
APIs here; it must not reuse schema v62.
