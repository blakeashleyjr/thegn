# THE-56 code handoff — row 451

Verdict: complete for the security/test review fixes in `review/442.md`.

Implemented:

- Autopilot now claims matching issues on the tracker refresh worker and
  schedules each claimed run through the bounded background scheduler, so a
  slow or hung issue agent cannot block later refreshes.
- Issue workers require a fail-closed sealed sandbox. The effective sandbox is
  hardened to worktree-only access with no configured credentials, home/cache/
  volume mounts, network, devshell, or setup hooks; no usable backend leaves
  the run at `needs_human`.
- Added an explicit credential-free agent-run mode that clears inherited
  environment state, keeps only the infrastructure allowlist, and filters
  credential-shaped variables including tokens, keys, secrets, auth, sockets,
  and agent handles. Existing PR/merge agents retain their explicit historical
  credentialed mode.
- Required autopilot journal and dispatch writes now gate progression and
  surface failures as bounded `needs_human` outcomes, including worktree and
  issue linkage, dispatch attachment/status, state transitions, PR
  correlation, queue handoff, and merge completion.
- Added focused credential-environment filtering coverage.

Verification:

- `RUSTC_WRAPPER= cargo check -p thegn-host --bin thegn` — passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host agent_run autopilot` — 6
  selected tests passed.
- `git diff --check` and pre-commit hooks — passed.
- `RUSTC_WRAPPER= just quick thegn-host` — blocked by the two pre-existing
  main-side clippy violations in `automation_executor.rs:104` and
  `daemon/service.rs:566`, as documented by review row 442.

Not verified: full workspace/CI/coverage/e2e gates, live tracker/forge/agent/
PR-queue integration, real sandbox backend execution, and injected database
failure fixtures. Those remain landing/review validation work.

Implementation commit: `6c5e6f89 fix(the-56): harden autopilot worker handoff`.
