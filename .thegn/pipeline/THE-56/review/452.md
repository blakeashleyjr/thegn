PASS

Review scope: security, failure paths, races, path/permission handling, and
tests for THE-56.

Review fixes committed with this handoff:

- Reserved a background permit before scheduling and moved the durable claim
  into the reserved worker after its database open. A full lane or transient
  worker-side database-open failure can no longer strand a claimed run.
- Added sandbox `ensure` and `preflight_exec` before launching the agent, so a
  fresh OCI sandbox cannot be treated as ready without setup/exec validation.
- Made autopilot reject systemd and the currently host-shell/native Windows
  backends for sealed work, rather than mislabeling partial/no filesystem
  isolation as safe for untrusted issue text.

Validation:

- `THEGN_ALLOW_HEAVY=1 just test`: 7,553 passed, 22 skipped.
- Scoped host regression tests: 4 passed, 2,896 skipped.
- Earlier scoped host autopilot/agent/scheduler tests: 5 passed; core
  autopilot/config/capability/db-migration tests: 650 passed.
- Changed files pass rustfmt checks and `git diff --check`.

No socket-denied tests occurred, so no tests were excluded from the gate. The
workspace-level formatter check has pre-existing failures in unrelated files;
the changed files are formatted. Ready for the merge queue. Do not run
`thegn integrate` from this row.
