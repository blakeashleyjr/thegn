# THE-163 adversarial source review — dispatch 599

## Verdict

`source-review-clear`

This is a source-only review. No full Rust gate is claimed; Cargo validation
remains explicitly pending with the primary.

## Review scope

Read and applied:

- `CLAUDE.md`
- `docs/ARCHITECTURE.md`
- `.thegn/maintenance/issue.json`
- `.thegn/maintenance/THE-163/primary-plan.md`

Reviewed the implementation from `c6cb3fab^..HEAD`, including:

- `crates/thegn-host/src/plugin_damage.rs`
- `crates/thegn-host/src/handlers/plugins.rs`
- `crates/thegn-host/src/render_plan.rs`
- `crates/thegn-host/src/run.rs`
- `crates/thegn-host/src/chrome.rs`
- related plugin scheduling and render tests/call sites

## Findings

No confirmed source-level correctness, bypass, race, or resource-growth finding
was found in the THE-163 change.

The reviewed anchors are consistent with the acceptance criteria:

- `plugin_damage.rs:35-64` compares final ordered statusbar identities and
  terminal-cell widths, returning `None`, `StatusbarContent`, or `Structural`.
  Equal-width content changes remain bounded; width, set, label, and order
  changes escalate.
- `handlers/plugins.rs:394-482` coalesces the whole drained batch, preserves
  structural lifecycle/load/respawn damage, and avoids a frame when a batch
  returns to the prior visible statusbar view.
- `run.rs:11730-11748` maps content-only damage to `statusbar_dirty` and
  structural damage to the existing broad chrome path.
- `render_plan.rs:143-160` preserves full/overlay precedence and idle `Skip`.
- `run.rs:13224-13323` recomposes and bounded-diffs only the statusbar rect for
  the new channel, while ordinary bars still include the masthead.
- `chrome.rs:2092-2112` uses the same display-cell width for placement and
  drawing, skipping segments that do not fit rather than clipping their slot.

The change does not add generation tagging or output bounding, matching the
primary instruction that THE-165/THE-161 work remains out of scope.

## Regression coverage added

Added a test-only regression in `crates/thegn-host/src/plugin_damage.rs:165-181`
covering same-width label changes and segment reordering. Both remain
structural even when the rendered widths are unchanged, preventing a stale
placement from being misclassified as content-only.

Existing focused tests also cover identical views, coalesced return-to-original
updates, both width-change directions, wide-glyph cell widths, render-plan
precedence, and bounded statusbar-vs-structural rows/bytes.

## Checks performed

These non-Cargo checks were run:

```text
rustfmt --edition 2024 --check crates/thegn-host/src/plugin_damage.rs crates/thegn-host/src/handlers/plugins.rs crates/thegn-host/src/main.rs crates/thegn-host/src/nav.rs crates/thegn-host/src/render_plan.rs crates/thegn-host/src/run.rs crates/thegn-host/src/chrome.rs
git diff --check
```

## Unverified work — primary-owned Rust validation

CPU constraints for this dispatch prohibited Cargo, builds, Clippy, nextest,
and test suites. The primary must execute and record the results of:

```text
nix develop --command cargo check -p thegn-host --all-targets
nix develop --command cargo test -p thegn-host --bin thegn render_plan::tests
nix develop --command cargo test -p thegn-host --bin thegn plugin_damage::tests
nix develop --command cargo test -p thegn-host --bin thegn handlers::plugins::tests::update_verb_caches_a_view_and_requests_repaint
nix develop --command cargo test -p thegn-host --bin thegn handlers::plugins::tests::one_shot_messages_apply_like_session_messages
nix develop --command cargo test -p thegn-host --bin thegn handlers::plugins::tests::statusbar_views_are_stable_by_plugin_id_then_contribution_order
nix develop --command cargo test -p thegn-host --bin thegn chrome_tests::statusbar_renders_plugin_segments_in_the_cluster_gap
nix develop --command cargo test -p thegn-host --bin thegn chrome_tests::statusbar_plugin_segments_degrade_by_skipping_not_clipping
```

No full gate result is claimed until those commands are run by the primary.
