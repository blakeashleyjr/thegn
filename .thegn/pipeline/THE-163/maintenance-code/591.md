# THE-163 implementation — native dispatch row 591

## Verdict

**Implementation-ready.** The primary-approved plan is implemented within
scope. No full validation result is claimed.

## Changes

- Added `plugin_damage.rs` with a pure ordered classification:
  `None`, `StatusbarContent`, and `Structural`.
- Coalesced plugin drains compare the visible statusbar view set before and
  after the batch. Identical final state emits no damage; equal rendered-cell
  widths and stable ordered labels use the statusbar-only channel; additions,
  removals, lifecycle changes, and width/placement uncertainty remain
  structural.
- Extended `render_plan::Damage` and `RenderPlan::Incremental` with a separate
  `statusbar` channel. The incremental renderer recomposes and diffs only
  `chrome.statusbar` for content-only plugin updates; ordinary `bars` still
  repaints masthead plus statusbar.
- Kept plugin lifecycle/contribution changes on the broad chrome path and
  preserved overlay, geometry, switch, and idle precedence.
- Aligned plugin placement and span advancement with terminal display-cell
  width, including wide/combining glyph behavior.
- Added classifier, coalescing, render-plan, and bounded-diff regression tests.

## Source checks performed

- `rustfmt --edition 2024 crates/thegn-host/src/plugin_damage.rs crates/thegn-host/src/handlers/plugins.rs crates/thegn-host/src/main.rs crates/thegn-host/src/nav.rs crates/thegn-host/src/render_plan.rs crates/thegn-host/src/run.rs crates/thegn-host/src/chrome.rs`
- `rustfmt --edition 2024 --check crates/thegn-host/src/plugin_damage.rs crates/thegn-host/src/handlers/plugins.rs crates/thegn-host/src/main.rs crates/thegn-host/src/nav.rs crates/thegn-host/src/render_plan.rs crates/thegn-host/src/run.rs crates/thegn-host/src/chrome.rs`
- `git diff --check`

## Unverified work / exact commands for the primary

Per dispatch CPU constraints, Cargo, builds, Clippy, nextest, and test suites
were not run. The primary must execute:

```text
nix develop --command cargo check -p thegn-host --all-targets
nix develop --command cargo test -p thegn-host --bin thegn render_plan::tests
nix develop --command cargo test -p thegn-host --bin thegn plugin_damage::tests
nix develop --command cargo test -p thegn-host --bin thegn handlers::plugins::tests::update_verb_caches_a_view_and_requests_repaint
nix develop --command cargo test -p thegn-host --bin thegn handlers::plugins::tests::one_shot_messages_apply_like_session_messages
```

If `nix develop` fails because the sandbox cannot write its `/nix/store`
temporary path, report that concrete environment blocker; do not substitute a
full gate or claim readiness from source checks alone.

## Findings for the next stage

- No concrete design blocker found.
- The implementation does not touch THE-165 generation tagging or THE-161
  output bounding.
- No row status was changed, and no merge, push, tracker closure, or child
  dispatch was performed.
