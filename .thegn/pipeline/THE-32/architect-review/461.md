# THE-32 architect review — row 461

APPROVED

Reviewed `tg/the-32-submodules` against merge-base `caef2f0e`, the recorded
design in [design.md](../architect/design.md), the prior review and revision
chunk, the completed coder handoff, and repository layering/async/safety
standards.

The revision requirements are satisfied:

- Gitlink conflict enrichment reads mode-160000 entries from the explicit base,
  ours, and theirs trees with NUL-safe plumbing. Unavailable metadata fails
  closed, and typed pointer conflicts are deferred before regeneration,
  custom-driver, rerere, throwaway-merge, or blanket-staging paths.
- PR and local diff views render a gitlink as one atomic pointer row. Synthetic
  `Subproject commit` lines are not navigation rows, line-stageable content, or
  inline-comment targets; ordinary file behavior remains intact.
- Lifecycle/config, trust gating, glyph/cache compatibility, measurement
  boundaries, raw gitlink patch handling, and help/ratchet updates remain
  aligned with the design.

Small corrections applied and committed in `402df17f`:

- bridged submodule-status or ordinary-status failures now preserve the
  last-known submodule glyph instead of becoming `Ok(false)`;
- recorded-gitlink lookup failures now fail the submodule read instead of
  misclassifying initialized pointers as moved;
- pointer-conflict rows retain the explicit `conflict` label even when a
  summary is present.

Verification:

- `cargo fmt --all -- --check` and `git diff --check` passed;
- `just quick thegn-core`, `just quick thegn-svc`, and `just quick thegn-host`
  passed;
- focused core submodule/patch/fold/config tests passed (16 tests after the
  correction), service submodule tests passed (4), and the control-schema
  snapshot passed;
- focused host integration and PR/local atomic-view tests passed (24 tests),
  and the broader host filtered ratchet run passed (409 tests).

Notes: full-workspace tests, e2e, migration, and feature validation against a
built release binary were not run. No revision chunk is required.
