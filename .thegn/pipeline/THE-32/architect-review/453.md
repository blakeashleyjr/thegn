# THE-32 architect review — row 453

REVISE

Reviewed branch `tg/the-32-submodules` against its base merge-base
`caef2f0e`, the THE-32 design, completed chunk handoffs, and repository
standards.

Small corrections applied and committed in `897cd374`:

- parse Git’s actual `git diff --raw -z` header/path records, with renames
  disabled at the production call site;
- reject backslash and Windows-drive submodule paths at the cross-platform
  boundary;
- honor `SubmoduleMode::Off` in sidebar glyph reads so it does not issue the
  recursive submodule-state query.

The implementation still has two blocking gaps. The revision requirements are
in [revision-1.md](revision-1.md):

1. Merge-tree gitlink conflict enrichment reads `git ls-files -u`, but the
   object-database `merge-tree` operation does not populate the current index.
   Divergent mode-160000 pointers can therefore be missed, and lookup errors
   are collapsed to an empty list before custom-driver/rerere/regeneration
   handling. Resolve ours/theirs pointer IDs from the merge input trees, fail
   closed on unavailable metadata, and prove that gitlink conflicts are typed,
   deferred, and excluded from all auto-resolution paths.
2. Forge parsing records `DiffFile::is_submodule`, but `pr_view.rs` and
   `diff_view.rs` do not consume it. Gitlink hunk text is still rendered as
   ordinary selectable/commentable lines. Render an atomic pointer row and
   make it non-selectable/non-commentable in both views, with focused tests.

Verification completed: formatting and diff checks; focused core and service
submodule tests; the host quick check; and the broader scoped tests recorded by
the completed chunk handoffs. The existing tests did not exercise real
NUL-paired raw-diff output, config-off glyph queries, synthetic merge-tree
gitlink conflicts, or forge/local gitlink selection, which is why those gaps
were found during this review. Full workspace/e2e coverage was not rerun.

Revision chunk: `.thegn/pipeline/THE-32/architect-review/revision-1.md`
