# THE-32 code-stage handoff (row 458)

Verdict: done

## Delivered

- Replaced merge-conflict gitlink enrichment based on the current index with
  NUL-safe `git ls-tree` reads from the explicit base, ours, and theirs merge
  inputs. Missing entries remain non-gitlinks; malformed or failed metadata
  reads are errors.
- Threaded the merge inputs through the service/plumbing seams and made host
  conflict handling fail closed: unavailable metadata stays deferred and
  cannot reach regeneration, custom-driver, rerere, or blanket staging paths.
- Made PR Files and local diff views render a mode-160000 change as one atomic
  pointer row with abbreviated SHAs and a caps-safe submodule marker. Synthetic
  `Subproject commit` hunk lines are not navigation rows, and PR inline
  comments are rejected with `submodule pointers are atomic`.
- Added a real-git divergent-gitlink tree-enrichment fixture and focused PR/
  local atomic-row regressions.

## Verification

- `cargo fmt --all -- --check` — passed.
- `just quick thegn-svc` — passed.
- `just quick thegn-host` — passed.
- `cargo nextest run -p thegn-svc submodule` — 4 passed.
- `cargo nextest run -p thegn-host integrate` — 22 passed.
- `cargo nextest run -p thegn-host submodule_file` — 2 passed.
- `git diff --check` — passed.

## Unverified

Full-workspace gates, e2e, migration, and built-binary validation were not run,
per the chunk dev-loop policy.

## Commits

- `8ab30f89 fix(the-32): harden submodule conflicts and atomic diff views`
- This handoff artifact is committed separately after the implementation.
