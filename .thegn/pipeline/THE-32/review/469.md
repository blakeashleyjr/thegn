# THE-32 security/test/bug review — row 469

PASS (ready for the merge queue)

Review fixes committed in `2106261e`:

- Kept submodule entries atomic in the PR-review diff modal, including row
  counts, rendering, and navigation. Added a regression test so synthetic
  `Subproject commit` lines cannot leak into the review UI.
- Applied trusted per-repository `git.submodules` overlays when collecting
  sidebar status and scheduling/carrying submodule scans.
- Fixed LOC exclusion patterns for absolute worktree paths. The initial full
  gate caught this as
  `thegn-host::loc_scan::tests::scan_excludes_submodule_source_but_keeps_superproject_source`;
  the corrected `**/<path>` pattern passes.

Verification:

- Full queue-boundary gate: `7,676 passed, 24 skipped` using
  `XDG_STATE_HOME=/home/blake/.superzej/pipeline-state`, `RUSTC_WRAPPER=`, and
  excluding the sandbox-denied socket tests below.
- Scoped core/service/host submodule and integration suites passed.
- `cargo fmt --all -- --check` and `git diff --check` passed.

Unverified due to the sandbox's socket restriction:

- `thegn-core::dns_filter::tests::forward_query_round_trips_against_fake_resolver`
- `thegn-core::dns_filter::tests::forward_query_times_out_when_no_resolver`

The merge step remains for `thegn integrate`.
