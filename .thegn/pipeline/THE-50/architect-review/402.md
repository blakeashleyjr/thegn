APPROVED

## What I checked

- Reviewed `main...tg/the-50-tracker-audit` against the authoritative THE-50
  design and both coder chunks.
- Verified the live `IssueBackend` seam remains object-safe and account-shaped;
  no tracker tier, config key, catalog/control action, DB migration, event-loop
  path, or generic command escape hatch was introduced.
- Verified `IssueCaps` covers the current optional comment/label operations,
  native declarations are honest, default optional methods return typed
  `Unsupported`, and `IssueError` implements shared seam classification.
- Verified plugin contribution caps are parsed least-privilege, carried to the
  live provider registry, enforced locally for false caps, and that upstream
  `unsupported` replies map to typed `Unsupported`.
- Verified configured native issue probe rows expose caps without network
  probing or credential serialization, and the OpenSpec draft was narrowed to
  the audit scope with the deferred tracker/SDD work recorded as follow-ups.
- Added two small architect corrections: the offline conformance factory
  fixture now avoids Kaneo's stored-token DB fallback, and bridge tests now
  observe true-cap forwarding versus local all-false refusal. These are in
  commits `0c07014f` and `286fb34b`.

## Verification

- `RUSTC_WRAPPER= just quick thegn-svc` — passed.
- `RUSTC_WRAPPER= just quick thegn-host` — passed.
- Focused `cargo test` filters for `conformance`, `issue`,
  `plugin::provider`, and host `plugin_providers` — passed.
- `cargo fmt --all`, `git diff --check`, and commit hooks — passed.

## Notes / limits

- `just openspec-validate` could not run because `openspec` is not installed
  in this environment (`command not found`).
- Full workspace CI, coverage, e2e, and live tracker/plugin calls were not run;
  they are outside the scoped architect review and the chunk dev-loop policy.
- Kaneo's vendor-shaped board/project/move surface and standalone plugin doctor
  inventory remain explicitly documented follow-ups, as required by the
  design. No revision chunk is needed.
