# THE-50 / code row 400

## Verdict

done

## Implemented

- Added `docs/extending/tracker-provider.md`, a self-contained recipe for the
  live account-shaped `IssueBackend` seam, exact object-safe `BoxFuture` shape,
  `IssueCaps`, typed `Unsupported`/`SeamError` behavior, offline probes,
  conformance, secret refs, and off-loop provider boundaries.
- Documented plugin `IssueProvider` registration, contribution caps,
  `provider.call` request/response behavior, local false-cap refusal,
  unsupported degradation, account labels, timeout behavior, and the scope of
  standalone doctor.
- Documented the concrete CLI-backed-provider recipe and why `jira-cli` is not
  implemented here; vendor argv remains implementation-local and no generic
  command setting is introduced.
- Linked the recipe from the extending index, native provider recipe, and
  plugin recipe; updated plugin help with caps, wire, timeout, and doctor
  behavior.

## Validation

- `RUSTC_WRAPPER= just quick thegn-svc` — passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-svc conformance` — 10 passed.
- `RUSTC_WRAPPER= just quick thegn-host` — passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host plugin_providers` — 1 passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host help` — 75 passed.
- `git diff --check` — passed.

## Unverified / next hints

Full workspace gates, coverage, e2e, and live tracker/plugin calls were not
run per the Chunk 2 dev-loop policy. No config, API wire, completion, or
control-surface changes were made.
