# THE-50 / code row 397

## Verdict

done

## Implemented

- Added serde/schema-visible `IssueCaps` with comments and labels bits,
  least-privilege null/omitted parsing, and unknown-field rejection.
- Added typed `IssueError::Unsupported` and `SeamError` classification,
  including transient connectivity and unambiguous missing-binary handling.
- Added the required `IssueBackend::caps()` contract and honest native
  declarations (Kaneo comments/labels only).
- Added offline issue capability/conformance coverage, including deliberate
  overclaim and underclaim test doubles and typed default-operation checks.
- Carried parsed plugin contribution caps through the host registry, refused
  false-cap operations locally, and mapped RPC `unsupported` replies to the
  typed error while preserving existing provider-call behavior.
- Added native issue capability data to configured issue probe reports.

## Validation

- `RUSTC_WRAPPER= just quick thegn-svc` — passed.
- `RUSTC_WRAPPER= just quick thegn-host` — passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-svc conformance` — 10 passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-svc issue` — 67 passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-svc plugin` — 30 passed.
- `RUSTC_WRAPPER= cargo nextest run -p thegn-host plugin_providers` — 1 passed.
- `cargo fmt --all` and `git diff --check` — passed.

## Unverified / next hints

Full workspace gates, coverage, e2e, and live tracker/provider calls were not
run per the Chunk 1 dev-loop policy. The initial wrapper-configured quick check
could not write its sccache cache; the same scoped checks passed with
`RUSTC_WRAPPER=`.
