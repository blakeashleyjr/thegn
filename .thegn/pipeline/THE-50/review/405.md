# THE-50 / security-test-bug review row 405

## Verdict

PASS — ready for the merge queue.

## Review

- Audited the THE-50 delta across `IssueCaps`, `IssueError`/`SeamError`, native
  provider declarations, plugin contribution parsing and local capability
  refusal, bridge error handling, provider-registry lifecycle, and issue probe
  output.
- No actionable swallowed-error, injection/path, permission, race, or ratchet
  defect was found in the changed code.
- Malformed or unknown plugin issue caps take the least-privilege all-false
  path; optional calls behind false caps do not allocate an RPC request.
- No resolved credentials are added to probe reports or logs by this change.

## Verification

- Scoped issue/conformance tests: passed (67 issue-related tests; 10
  conformance tests).
- Host plugin-provider registry test: passed.
- `RUSTC_WRAPPER= THEGN_ALLOW_HEAVY=1 just test`: passed — 7,039 tests passed,
  22 skipped.
- The first `THEGN_ALLOW_HEAVY=1 just test` attempt stopped before tests because
  sccache hit the sandbox read-only filesystem. The required wrapper-disabled
  retry passed. No socket-denial exclusions were needed.
- `git diff --check`: passed.

No code fix was necessary. The merge step remains the integrator's job.
