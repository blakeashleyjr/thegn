# Primary coordination for future THE-440 investigation

THE-418 approved removal of host direnv/devShell implicit evaluation and degraded fallback; it will touch agent.rs/envplan.rs/shell_snippet.rs. Do not edit its worktree. Review final shared launch changes before implementation and plan a controlled stack if needed. THE-505 is adding strict generation admission, not yet wired/approved. Do not invent a second config digest or claim current global Config is an admitted token.

Current agent_permissions::seed writes .claude/settings.local.json with following read/write and overwrites permissions.allow. agent.rs calls before sandbox resolution and swallows failure; other harnesses warn/no-op. Preferred smallest fix: investigate documented per-process harness CLI/config overrides first, since command-scoped permissions avoid repository artifact mutation and crash cleanup entirely. Verify actual supported harness behavior using local installed help or authoritative docs; do not assume new flags exist or concatenate raw shell permission strings. Preserve existing deny/hooks/unknown values and never use skip-permissions as substitution. If safe harness behavior cannot be attested, refuse configured headless permission grant before launch with clear infrastructure-hold evidence. Entire acceptance still needs meaningful real launch seams, concurrent runs and untouched repo tests.
