# Primary review of 974c4a1d: revisions required

The implementation addresses framing and many projection paths, but the following concrete correctness and acceptance gaps remain. Review primary-inflight-review.md, then implement these as one bounded correction dispatch. No Rust execution/builds. Do not claim remaining risk is exclusively compilation.

1. Byte conservation: publish rejects a never-admitted stale pd by SUBTRACTING its bytes; try_recv REMOVES an admitted stale pd WITHOUT subtracting bytes. Correct both. Add direct invariant tests summing retained queue bytes after accepted, replaced, refused, stale, evicted and cleared publications, including repeated stale floods while near cap. Checks precede allocation/retention. Include root/server/path clones in queue order, sequence and incomplete metadata budgets; explicit finite metadata cap is acceptable, don't call only message bytes the whole footprint.
2. Lifecycle: LspInner::close is never called in production. run.rs only prunes the host diagnostic store; clients and active registrations for closed worktrees remain forever and new roots stop working after32 lifetime opens. Wire registration retirement/client cleanup to actual host worktree lifecycle using off-loop ownership for subprocess teardown. Same generation identity must flow through queued and retained data; close/reopen evicts only the retired stream, never another server. No blocking child drop on render/input thread. Test >32 sequential open/close and restart-generation late notifications.
3. Active registry input bounds: set_active clones unconstrained root/identity before limits. Validate raw platform path/identity size before retaining/duplicating. The registry should not admit u64::MAX as a real generation or silently wrap AtomicU64. Bound config registry/custom entries and negative cache as part of lifecycle; retired entries must release capacity without allowing unknown generations. Avoid treating externally supplied sequence as trusted monotonic ownership.
4. Store admission: LspDiagnostics::apply clears incomplete BEFORE checking store capacity. A complete publication refused due to capacity then leaves old data looking current/complete. Preserve/mark active loss on every refusal and clear only when a complete current-generation replacement/clear is actually committed. Count all retained item struct overhead, server identity and per-item duplicated file bytes BEFORE building Vec<DiagnosticItem>; current code clones/transforms everything then checks16MiB. remove_retired_streams must not accept stale publication authority merely because generation differs; use registration evidence from supervisor/queue.
5. Fair wake regression: wake_pending now avoids the original nonempty-queue busy-spin and handles health; prove races deterministically, idle wake count0, one bounded obligation, rearm after count/byte/time/input-preempted slice. Add actual fake-server flood + multiroot progress + complete clear + close/reopen generation tests. Current real integration test only tests deep JSON and cannot substantiate flood/fairness/ownership claims. Ensure no pending work stalls after stale dequeue returns Empty. Include real host drain budget function tests (8pub/256KiB/2ms onepubovershoot) rather than only literals in run.rs.
6. Compile/source hygiene: host is a bin; proposed cargo test -p thegn-host --lib is wrong. Do not run builds; primary will use correct central nextest filters. Run rustfmt and just ratchets and report precise actual results. Existing unbounded std Sender DiagnosticSink compatibility route should be removed or strictly test-only if no production caller; audit all public start/start_argv routes so no production LSP notification stream bypasses bounded delivery. No blanket allows or ratchet inflation.

No merge, push, Linear closure or spawn. Complete the issue's approved pipeline scope; THE657/658 additional LSP work remains out of scope. Parent507 remains implementation phase only, not completion.
