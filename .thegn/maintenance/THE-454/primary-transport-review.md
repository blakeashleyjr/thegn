Review the complete THE-454 change from 0be575ce, including all implementation/primary revisions and prior adversarial reports. No production edits. Optional tests allowed; no Cargo/builds. Primary will run serialized central checks.

Primary repaired the latest worker test claims: removed a test-only artificial timeout from CalendarHttpClient::send (it tested itself, not the connector) and uses an injected indefinitely pending DNS resolver through the actual production reqwest builder/connect_timeout instead. Changed the idle pool regression to a simultaneous second wave; sequential requests reused one connection and could not prove the configured cap. Replaced the still-present axum bogus Content-Length fixture with raw HTTP headers for both real backends, no payload, bounded outer test timeout. Resolver answer-cap policy failure is typed as DestinationRefused; actual resolver failures stay Network. Prior central compile errors repaired but updated head has not yet been compiled.

Independently inspect destination/DNS policy, redirects/auth secrecy, media/encoding/status paths, pooling, operation/connect/read deadlines, capacity before allocation, XML/request caps and query preservation. Review whether all fixtures would fail the corresponding broken behavior. No expansion into THE-465 event admission or THE-458 date expansion. Report source-review-clear/revisions-needed with precise evidence and pending validation; no full issue completion or merge claim.
