Primary source review and independent-review request: THE-454

Review bfb39b45 plus the primary regression corrections committed with this file, against base 0be575cecab318db9049352b0c7ae5cb45b18581. Prior reviews and the primary-plan.md, primary-revision-2.md, primary-review-addendum.md explain the approved transport scope. Production fixes now address the known URL, redirect, proxy, address policy, encoding, bounded response, and deadline issues. This is source review only; no Rust compile/test result exists yet.

Primary corrections: the geometric reserve test expected 9 additional slots for len=7, cap=7, append=2, although the bounded doubling target is 14 and additional is 7. Corrected that assertion. The CalDAV shared-deadline regression previously delayed only the fallback response, so it would also pass if the deadline were incorrectly reset. Both responses now spend 150 ms against a single 250 ms operation budget; the fallback is valid XML, so a reset-deadline implementation would succeed and fail the Timeout assertion.

Independent adversarial reviewer: inspect all changed production paths and regression assertions. In particular check exact-cap/declared-length fixtures really exercise application limits; same-client DNS denial is distinguished from ordinary network failure; pool reuse/account-local auth, query, ETag and sync-token isolation; IPv6 mapped/compatible/native loopback distinctions; no URL/token leakage; 409/507 recovery shares one deadline. Do not expand into THE-465 event admission or THE-458 expansion. Inspect tracker bounded-reader reuse for behavioral regressions.

Do not run Cargo/builds/broad tests. Primary will validate a separate frozen checkout, so never edit that checkout. Commit your precise findings and optional regression test additions only in this native issue worktree; no production edits, merges, issue closure, or extra agents. Use source-review-clear or revisions-needed, explicitly keeping actual Rust validation pending. Prefer inspectable proof over a test name or claimed coverage.
