# Primary review of a7e5c6df: revisions required

1. IPv6 URL literal policy bypass in http.rs new(): host_str includes brackets, IpAddr parse fails, and literals bypass DNS. Use typed Host::Ipv4/Ipv6. Prove real backend requests cannot reach loopback/mapped loopback without opt-in.
2. URL webcal normalization is broken: url::Url::set_scheme explicitly rejects non-special -> special transitions. Normalize validated webcal prefix then parse as HTTPS, preserving authority/query and rejecting userinfo/fragments; tests already added should expose this.
3. Address classification: 192.0/16, 198.51/16, 203.0/16 are overblocked; IPv6 translation, transition, reserved/site-local classes remain allowed. Precise documented public-unicast policy with boundary tests; LAN opt-in must still reject unspecified/multicast/broadcast and reserved not-local-unicast ranges.
4. New policy test is guaranteed to fail on 224.0.0.1 and ff02::1 because it expects private opt-in to permit multicast. Correct the test expectations, not the safe refusal.
5. Body allocation guard constrains length but extend_from_slice can double capacity beyond cap (chunked non-power-of-two first chunks). Explicit bounded growth with fallible reserve and capacity tests. DNS resolver also collects without a count cap.
6. Request must send Accept-Encoding: identity. Validate all Content-Encoding values, not only get(first). MIME must match case-insensitively and avoid ambiguous repeated values.
7. CalDAV XML escaping does multi-pass large allocations before checking expanded request length. Calculate checked exact expanded size including envelope before allocation, then one bounded write. Bound response sync-token/etag before retained state publication too.
8. Complete promised tests: DNS/actual connection policy and rebinding; proxy environment in isolated child; at-cap/cap+1 chunked and declared lengths; slow headers/body, idle and absolute deadline shared by recovery; per-process pool reuse across reconstructed backend accounts and no auth/validator/cookie crossover; redacted failures. Cover both backends, and fallback, not helper-only asserts.
9. Keep tracker adapter behavior under the shared bounded reader. Calendar parser/event budgets are sibling465; transport regression tests must not silently replace those tasks.

No tests run yet; these findings are source review plus local dependency implementation inspection. Independent native adversary479 is ongoing.

Policy reference checked by primary: IANA special-purpose registries (2025-10-09 revisions), https://www.iana.org/assignments/iana-ipv4-special-registry/ and https://www.iana.org/assignments/iana-ipv6-special-registry/. Use explicit reviewed CIDRs, not Python ipaddress global/private classifications as normative HTTP security policy. Document conservative transition/translation refusal and positive public-unicast examples next to denied boundaries.

## Implementation greenlight: resolve this review and independent review479

Read .thegn/pipeline/THE-454/maintenance-adversarial/479.md and fix all confirmed findings and missing acceptance tests. Reviewer committed one failing-by-source IPv6 test in3335014f; retain and make correct. Primary added the literal bypass and broken webcal normalization not noticed independently; these are highest priority. Complete the source/test repair as one coherent revision handoff; do not just list missing fixtures as unverified. Tests should exist; only execution is deferred.

Implementation clarifications: no new runtime knobs besides approved private-network opt-in. Internal injected resolver/deadline/test-cap seams are allowed and must exercise the same production connection path. Use bounded count DNS answers and fail closed if overflow; do not assert OS getaddrinfo internal allocation is controlled by our count cap. Public IPv6 conservatively restrict to global-unicast2000::/3 minus precise documented special-purpose/transition ranges; handle mapped forms via embeddedIPv4policy. Local opt-in may add RFC1918/CGNAT/loopback/linklocal/ULA for intentional LAN use, still deny multicast/unspecified/reserved/documentation/translation/transition forms. Do not broadly blacklist normal public /16s.

Use one bounded accumulator with capacity growth covered by a pure test, shared tracker reader semantics unchanged. Retained application buffers must be bounded; document that HTTP framing/header allocations are governed by reqwest/hyper rather than claiming allocator-widecap. Request-token preflight includes XML envelope+exact expansion length BEFORE allocate. Header tests cover repeated Content-Encoding and media tokens with params/mixed case. Missing Content-Type refusal must remain documented or justifiedcompatibility.

Deadline tests must be deterministic where possible; injected internal durations can keep tests quick. Test actual backend uses same absolute deadline across409/507recovery, and no retry storm. Proxy env test isolated child (never process-global envmutation in paralleltest); test literal/DNSrefusal before listenerseesrequest. Cross-originredirecttarget fixtures canbe loopbackoptin;HTTPSdowngrade Location is notfollowed evenwithoutconnectingtotheHTTPSsource iftestseamstatusonly, but documentwhatproved.

No Cargo/build/nextest/clippy inworker. Primary centrally executes focused tests then finalgate. Record implementation-ready and exact tests added; no passinggateclaim. Preserve originalscope; don't implementcalendar-eventbudget465 orspan458here. Commit code+tests+artifact; no mainmerge/push/issueclosure.
