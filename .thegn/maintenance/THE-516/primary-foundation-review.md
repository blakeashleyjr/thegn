# Independent foundation review required

Review complete chunks1-2 and especially 99a16fdb plus current primary corrections. No Rust execution or new feature work. Read primary-revision-3.md and report509. Whole516 remains open; this is foundation acceptance only.

Primary rejected 509's claim that try_wait(Some) leaves a Unix child unreaped. It reaps! Primary now waits for bounded stdout completion first, signals only while the direct leader remains owned and never previously reaped, then polls exit. Late stdout retains its finite slot (including detached descendant); no numeric group signal after reaping or uncertain ownership. Test spawn failure no longer assumes one job fills TWO slots or that reaping cannot race return. Primary fixes explicit reaper channel type, missing Hasher trait and explicit u32 big-endian hashing.

Review bounded ownership under timeout/output overflow/pipe inheritance/leader early exit/reader setup failure/try_wait error and Windows weaker direct-child cleanup; same-file held handle plus actual birth-time generation stable across checkout and rename, distinct after recreation, failclosed if unsupported; exact canonical ledger DDL and bounded borrowed row preflight plus indexed enumeration/promotion. Check migrations on fresh and old68/oldpartial69 definitions, no silent authorities from ambiguous claims. Tests must exercise real guarantees, not comments. Add deterministic regressions if needed and give clear versus concrete revisions. Do not mutate production without reporting proposal. No merge/push/trackerclosure/spawn. Primary owns all runtime gates.
