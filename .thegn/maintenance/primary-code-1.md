# Primary approval: THE-516 foundation

The revised plan in maintenance-plan-revision/477.md is approved with the corrections below. Implement chunks 1-2 as coherent commits, then hand off for primary review before migrating allocation and routing. This is partial implementation, not issue completion. No Cargo/builds; add meaningful regression tests and record unverified commands. Use verdict implementation-ready.

- Preserve Git as source of truth. Exact identity bytes belong in platform adapters (Unix raw bytes, Windows lossless native representation); do not introduce platform cfg outside the existing platform seam. A representation that cannot be preserved must refuse before side effects. Use existing Git command hardening helpers.
- RepositoryId is the full domain-separated digest of the canonical Git common directory, not origin or basename. Expose a pure constructor from validated exact bytes plus an I/O adapter; THE-515 must consume this API. Document that moving the common directory changes this identity and requires explicit migration of trusted overlays; do not silently reclaim trust by basename/origin.
- ExactPath and BranchRef private fields and bounded constructors. Avoid a public mutable struct whose display fields can become authority. Bound Git capture/output and identity inputs; use existing command bounds rather than unbounded Command::output.
- WorktreeGeneration persists per Git admin instance, changes on replacement, remains on rename; distinguish stable instance generation from monotonically changing operation/reconciliation revision so two renames of the SAME generation cannot publish stale completion. Full later CAS must validate expected operation revision + Git state. Randomness is used for allocation nonce, not an excuse to create duplicate retry attempts.
- No Git/filesystem I/O in DB schema migration or UI input/render. Schema migration is additive SQL; exact legacy verification runs on existing background admission lanes and promotes only unique claimants. Document explicit ownership of reconciliation.
- Do not reject existing duplicate legacy paths via a new unique constraint before quarantining both competing rows. Store claims separately if needed; uniqueness belongs to verified admitted instances. No first-row winner.
- Keep old behavior APIs available until callers migrate in later chunks; do not claim the issue fixed by adding unwired identity types or a ledger. No mass moves.
- Preserve tests for supported platform semantics; Linux execution here will not count as macOS/Windows validation.

Deferred later chunk correction: prefer keeping a checkout physical path stable during rename unless existing product contract requires movement. If movement remains necessary, exact journal/recovery must cover every failed publication and report split state. Provider updates cannot be falsely described as atomic with SQLite. Later consumer migration must make failure and stale revision behavior explicit.
