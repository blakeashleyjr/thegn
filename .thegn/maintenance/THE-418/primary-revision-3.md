# Primary review of adversarial505 — narrow routing correction

F01 is a real regression introduced by the new attach-only skip path. F03 identifies a meaningful gap; fix both. F02 describes the pre-existing generic provider/prewarm worker lifecycle, not any retained direnv/host evaluation (those operations and their detached helpers were removed). Do not turn this removal fix into the whole generic subprocess/task supervision redesign. Record that broader pre-existing bounded provider-worker lifecycle remains THE-251/related scheduling scope; do not claim it fixed. Any new work introduced here must not worsen it. No new Linear issues/features.

1. Carry captured stable target leaf identities (or an existing real tab generation) on every SpecBatch, including all Err/PrewarmSkipped cases. Compare before applying attach, loading, pool/dirty/relayout state. Never use a stale numeric index as authority. Ensure a late batch does not erase a newer tab's inflight reservation. Account for tab-close bookkeeping shifting indices: settle only the captured request that still owns its reservation, not a replacement. Update both actual producers and regression fixtures, with stale skipped results both with/without attach; keep valid attach behavior.
2. Extract the actual automatic-prewarm resolution/relaunch/rejection route from run.rs into a small callable production helper outside the god-file. The real worker and executable regression MUST call that helper, including both rejection points and remembered-agent fold. The test must not manufacture its own skip using separate calls to reject_host_prewarm. Use narrow injected external probe/provider effect seams if needed, no live user daemon/provider. Keep the existing real drain/Panes attach effect seam, and positive-control harmless fake shell/direnv/nix executable evidence. Add an explicit focused-host positive control separately; requested target failures still cannot execute a host fallback.
3. Primary19a871fc fixed test compile requirements: SpecError has no Debug, so result.as_ref().unwrap was invalid. Preserve the corrected assertions or equivalent. Reviewer505 exact suggested --lib commands are invalid for the host binary; report actual cargo-nextest package/filter commands.

No Cargo/builds/nextest/Clippy/full suites. Format/diff checks allowed. Implement and commit, return implementation-ready with exact unverified checks. Do not merge, close, push or spawn. Keep scope bounded to this regression and production-route coverage.
