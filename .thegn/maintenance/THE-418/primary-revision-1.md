Primary review of 28f6579b: revisions required before approval

The removal of host direnv warming, automatic approval, closure push, and warm-derived Nix socket mounting is the right direction. The approved plan remains incomplete at the actual launch boundaries and regression evidence. Implement the following fixes, then commit a new implementation-ready handoff. No Cargo/builds; primary centrally validates. Do not modify another worktree or the live user configuration/hooks.

1. Remote fallback bypass in prepare_sandbox_env: the new terminal halt guard is `!degrade_allowed && sb.enabled && sb.backend != None`. host_fallback_allowed correctly returns false for remote/provider placement with nested backend=None, but this extra condition then skips the halt. If remote resolution yields no spec, the later local-only check also skips and the function can return host execution. Base the final refusal on the authorization result itself; valid remote native execution should return a real remote spec before this boundary. Exercise remote/provider nested None and disabled cases, local requested Auto exhaustion, and explicit local host selection through the actual resolver outcome seam, not only the bool helper. Avoid live providers by injecting availability/resolve outcomes.

2. Automatic sibling/tab prewarm still synthesizes bare host login shells. run.rs around the prewarm_requests loop calls launch_spec_synced_with, then applies remembered-agent relaunch and publishes SpecOrigin::Prewarm. With explicit host backend this remains a local `shell -lc ...` before the user focuses the tab. Add an explicit no-host-prewarm policy at the actual prewarm boundary, preserving focused user host launch and attaching existing sessions. Guard after remembered-agent replacement as well as initial spec selection so a relaunch cannot reintroduce host execution. Cover drawer/layout/spare paths that can create such a process; no broad feature redesign.

3. compose_launch's failed devcontainer session branch calls blocked_devcontainer_argv, which itself runs the user shell with -lc on the host. Its static error command does not prevent host login rc/.envrc hooks from running first. Return a typed launch refusal (or a demonstrably inert trusted error process without user/repository startup hooks); do not turn rejected target authority into a host login shell. This is within the approved degraded-provider boundary. Regression must execute the failure seam with a fake shell/sentinel and observe zero host shell invocations.

4. The handoff only lists bool/string tests. Add the approved real lifecycle/launch-seam zero-execution regressions using fake direnv/nix/shell and hostile .envrc/flake sentinels. Default and legacy modes must make zero host evaluator calls. Symlink/hardlink/FIFO/directory cache leaves and external bytes+mtime must remain untouched. Cover startup/prewarm plus representative shared materialize/split/daemon paths, actual refusal and generated target scripts. Tests should fail against the old code; source grep and matching helper implementation alone are insufficient. No real cloud or destructive long-running children.

5. Refresh stale docs/comments: direnv_warm.rs says all wrappers have no host process/filesystem probe, but launch_spec_full does probe and can provision; state only that it does not perform removed warming. Verify doctor/help and fallback wording match actual final behavior. Preserve explicit user host execution semantics and target-side builds.

The issue remains open. After this revision: primary review, independent adversarial review, central focused/final tests, then native landing.
