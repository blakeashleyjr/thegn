Primary review of bb06f0a7: foundation revisions required

Remain within approved chunks 1–2. Do not migrate allocation/routing yet or claim the full issue fixed. No Cargo/builds; source, formatting, and focused noncompiling fixtures only. Primary will validate the corrected foundation. Address these before independent review:

1. Fallible exact identity bounds. encode_parts asserts on caller-controlled aggregate length and uses an unchecked iterator sum; ExactPath/BranchRef allow MAX_IDENTITY_BYTES but adding domain/framing already exceeds that same limit and panics. Make encoding/digest/constructors return typed bounded errors, check sums before allocation, distinguish per-component from aggregate limits, and test exactly supported size and cap+1 without panic. native_path_bytes currently clones before checking size; preflight borrowed platform representation before allocation. display_label currently renders the entire raw input and only then truncates; stop rendering at the output budget. Preserve full lossless authority bytes separately.

2. Git capture must be bounded in time as well as bytes. git_stdout_bounded can block forever in read_to_end or wait. Use the existing bounded process mechanism where possible, with process cleanup and no idle polling; do not invent an unbounded reader thread that survives timeout. Test over-limit output, never-closing stdout, child holding inherited stdout, nonzero exit and cleanup under an injected harmless child. Preserve hardened git_cmd environment behavior and keep work off the UI loop.

3. Exact path/registration inspection. Trimming a CR before LF discards a legitimate final CR in Unix path bytes. Remove only the actual Git terminator, or use a verified NUL protocol. --porcelain -z must not silently fall back to newline parsing if NUL framing is absent/truncated. Validate record structure and duplicate/conflicting branch/detached fields instead of attaching a stale field to a later record. Compare the entry common directory against the originally inspected repository and detect changed registration/admin identity across capture; current multi-probe result can mix root A's registration with a replacement repository B at the path. Keep inspection fields immutable/private with projections. Tests must really use same-basename roots, symlink aliases, newline/CR/non-UTF8 paths, detached worktrees, mismatched/replaced registration and admin-instance replacement; existing repository test names same-basename/symlink but creates neither.

4. Generation and operation revision. Admin path bytes alone do not identify an admin directory instance after delete/recreate. Add an exact captured instance stamp or explicit verified allocation token contract sufficient to distinguish replacement while retaining generation on rename. Include a separate operation/reconciliation revision in the ledger foundation so later same-generation rename CAS has an actual stored value. Do not claim a random public byte constructor establishes verified Git generation; document host capture/proof ownership and refuse unsupported platform representations before effects.

5. Ledger uniqueness must apply to verified claims. Current unconditional UNIQUE(path) and UNIQUE(repo_id, admin_id) reject competing legacy/quarantine rows, contrary to the approved plan. Preserve all conflicting claims and require explicit quarantine, with partial unique indices for verified admission. Promotion must atomically refuse conflicts, preserve all claims, and never choose a first-row winner. Handle detached branch identity explicitly (current required nonempty branch_ref cannot represent inspect_registered's None). Bound row fields/reasons/counts before decoding or allocating from SQLite, not only after row.get<Vec<u8>>. Enforce consistent quarantine reason/state on insert and update; use typed/expected generation+revision transition inputs appropriate to this foundation.

6. Verify the complete v69 schema shape and required indices/constraints before stamping, not only table existence and compatibility columns. Add real migration tests from populated v68 including duplicate legacy rows and a malformed pre-existing worktree_instances table. Do not touch the live DB. Remove new lint violations such as unnecessary mut, unannotated ignored results, and == false assertions. Preserve prior migration contracts and fail closed on incomplete schema.

Keep canonical RepositoryId policy shared with THE-515, and keep old public behavior APIs intact until later reviewed consumer migration. Review artifact must state exact remaining scope and honest unverified tests. No merge/push/issue close/child agents.
