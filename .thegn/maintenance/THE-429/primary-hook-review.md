Independent review of complete THE-429 source from0be575ce, including494revision and primary test corrections. No production edits, optional tests, no Cargo/builds. Read prior primary-revision-1 and reports.

Primary corrected a false-negative sentinel: historical/current checkout probes now observe the SAME actual payload marker, reset after positive control. Tests now execute the actual flake hookExtras shell body with only immutable Nix Python/script references substituted, rather than merely invoking the detector (which would miss an installer reintroduced beside it). Fixture base carries old hook sources; hostile branch mutates both payload paths. Added optional THEGN_TEST_BINARY native wt new invocation with isolated config/state/runtime/HOME, real compiled /home/blake/code/thegn/target/release/thegn, malicious base and zero payload evidence. Primary ran this exact fixture successfully: native worktree fixture:ok; focused checkout-hook/detector fixtures:ok. No Rust source changed. Nix evaluation remains pending primary; full Rust batch gate already passed on base and will be rerun for Rust maintenance changes.

Review flake removes all automatic candidate-code checkout execution and config writes, keeps existing explicit precommit/prepush gates; detector is immutable/read-only/bounded/no-follow and refuses ambiguous/custom hook targets; fixture payloads cannot reach sharedliveconfig; tests prove actual new setup and would fail old installation. Known live legacy hook is still present and must be preserved/retired by PRIMARY only after reviewed native landing; do not touch it. This pending operational cleanup must not be mistaken for source failure. Report source-review-clear/revisions-needed with evidence, no merge/issueclosure. Keep review bounded to concrete safety/correctness issues, no unrelated feature work.
