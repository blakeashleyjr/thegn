Primary review of 1b8f3edd: revisions required

Removing the installer and tracked executable is correct. Preserve all generated validation gates and the explicit read-only/manual migration policy. Before independent approval, address these concrete gaps. No Cargo/builds; primary owns Nix evaluation and central validation. Do not mutate live hooks or dotfiles.

1. Detector can block on a replacement FIFO: lstat proves a regular file, then os.open(O_RDONLY|O_NOFOLLOW) blocks if the entry is replaced by a FIFO before open. Add O_NONBLOCK (and require available flags explicitly) before fstat. Verify descriptor identity against the lstat candidate and reject replacement. The descriptor walk is otherwise good. Add deterministic injected replacement-before-open tests for FIFO, symlink, and different regular file; use a bounded child timeout so a regression cannot hang the test runner. No automatic deletion is authorized.

2. The detector calls subprocess.run(capture_output=True, text=True) for Git with no timeout or output cap, so its advertised bounded/read-only startup diagnostic can hang or accumulate arbitrary config output. Use one bounded, timed Git query helper with typed refusal, no inherited pager/prompts, and bytes/lossless path handling or explicit unsupported refusal. Test oversized output, hung fake Git, invalid UTF-8/path output, and inherited command-scope hooksPath. Refuse ambiguous effective hooks settings before inspection; do not rely only on local/global/system values if environment command config overrides them.

3. The current malicious-checkout fixture never invokes the changed automatic setup, so it mainly proves Git does not execute an uninstalled hook. Exercise the actual setup seam (an extracted immutable helper invoked by shellHook, or primary-supplied evaluated shellHook under a hermetic temporary repo) before checkout/worktree add. Include an existing compiled native thegn worktree creation path under isolated XDG/Git config if feasible without builds; never use the live repo DB. The regression should fail against the old installation behavior because the malicious branch payload is actually reached there, not only because a source-file existence assertion fails. Keep explicit local gate behavior intact.

4. Remove the fixture dependency on git show 0be575ce: shallow/source-archive checkouts lack that commit. Store the exact legacy payload as a non-executable test fixture (never installer input), assert its SHA256, and keep product detection fixed to the known digest. Register the Python regression in the existing appropriate lightweight test/CI gate so it runs routinely; do not only list a manual command. Verify it runs from a copied source tree without Git history.

5. Record actual focused command outputs and any remaining Nix/native validation pending. Keep migration docs explicit that existing live copies remain active until primary cleanup after landing. No live cleanup from the worker. No full gate claim, merge, push, or issue close.
