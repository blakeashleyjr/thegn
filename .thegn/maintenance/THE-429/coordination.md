# Primary coordination: THE-429

Native batch base is0be575ce. Primary read flake.nix hookExtras and test/git-hooks/post-checkout.sh: shell entry installs checkout-controlled hook into effective hooksPath; hook executes current checkout heal-worktree.sh and can ln -sf a config target. Include both install and invocation boundaries. Investigate the smallest complete repair; removing unsafe automatic installation/execution is acceptable if it preserves explicit developer gates and documents compatibility. Do not invent a generic hook trust framework if removing the implicit hook is sufficient. Existing installed old hook migration must be considered: never overwrite/remove an unknown or foreign hook, global hooksPath, or directory/symlink/FIFO target on a marker alone. Exact known legacy byte digest plus no-follow ownership can establish narrow cleanup eligibility; report when explicit external cleanup remains required. Do not weaken pre-commit/pre-push checks or treat optional development hooks as security authority.

THE-418 plan removes generated direnv auto-approval and implicit host repository evaluation; no auto-allow workaround. No code before primary approves. Inspect existing test fixtures and native worktree creation, not just flake text.
