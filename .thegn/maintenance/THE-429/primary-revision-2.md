Primary accepts adversarial498 findings1–3; implement these focused revisions.

1. Detector bounded Git capture must kill the isolated process GROUP on POSIX timeout/overflow/error (not merely direct child). Cleanup must also cover a child exiting while descendants retain pipes. Bound direct-child reaping, close all pipes, and avoid normal successful Git being killed simply because EOF precedes process status by a small race. No Windows process-tree claim without job objects; this Nix helper only supports POSIX no-follow primitives, so explicitly refuse unsupported platforms before creating subprocesses. Add repeated fakeGitfork pipe-holder regressions, bounded deadline and child/process/fd cleanup. Scope exact owned group only, never broadkill.
2. Before first Git query refuse/scrub full repository directory/index/object/namespace redirection environment, consistent with hardened git_cmd. Refusal is simplest and visible. Include GIT_DIR, GIT_WORK_TREE, GIT_COMMON_DIR, GIT_INDEX_FILE, object/alternate dirs, namespace, ceiling/discovery/config local redirects. Preserve intentional normal HOME/XDG/global config inspection; keep existing command-scope hooksPath refusal. Add two private repos A cwd/B env and prove onlyA inspected or explicitrefusal, Bunchanged.
3. Extend the former .pre-commit-config.yaml target preservation matrix: regular file, directory, valid/dangling symlink, FIFO, hardlink/externalbytes+mtime and deterministic component/leaf replacement around actual hookExtras setup. The source removeswrites; do not reintroduceinstaller/uninstaller. Snapshot path types/bytes/metadata before and after setup+checkout/nativewt. Avoid readingFIFO. Existing sharedsentinelpositivecontrol and actualshell_entrybodyfixture must remain.
4. Clean the pre-existing extra EOFblank in .thegn/pipeline/THE-429/maintenance-investigate/484.md so git diff --check0be575ce..HEADpasses. Do not interpret optionalnativefixture as absentbinary: primarycompiledbinaryEXISTS at /home/blake/code/thegn/target/release/thegn. You may run THEGN_TEST_BINARY=/home/blake/code/thegn/target/release/thegn python3 -B test/the429_checkout_hooks.py in isolated fixtures. Primaryalreadyran nativefixture and Nixevalsuccessfullyat3459232a; pendingupdatedheadmustretainhonestevidence. No newbuild needed orallowed. Wire defaultrecipe to choose existingbinaryifavailable and explicitlyreportmissing, while finalgatewillrequireenvprovidednativecheck.

No Cargo/builds/fulltest/lint, no livehooks/config/DB modifications. Small Pythonfixtures, shellsyntax, formatting areallowed. Commit implementation plusactualevidence then implementation-ready. Fullissue notdoneuntilfreshreview,nativegate,landing,andprimaryarchives/removes exactknownlivelegacyhook. Do notperformcleanupyourself.
