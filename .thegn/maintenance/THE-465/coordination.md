Primary coordination for this batch

Investigate only until the primary approves a concrete plan. THE-454 is actively hardening calendar HTTP transport in its native worktree; read its latest handoff and source without editing it. Plan to stack the reviewed transport commit before implementing shared files. Do not duplicate URL/address/body-limit policy. THE-458 owns date-window intersection and output expansion budgets; define compatible admission/output error semantics together, without claiming THE-457 recurrence, THE-455 local traversal, or THE-456 concurrency are closed.

A required typed nonzero budget must reach every production backend constructor, including local ICS and command adapters. Trace parser allocation BEFORE full CalEvent construction and the cache/cursor publication boundary; truncation after collecting events is insufficient. The global budget must account for simultaneous router instances and response/payload ownership lifetimes rather than merely setting a per-request constant. Use bounded exact resource accounting with counters for tests, not fragile RSS thresholds alone. Overflow must preserve the previous authoritative cache and cursor and surface truthful health.

All workers are Luna high. No Cargo builds/tests or broad checks; central validation is primary-owned. No merge, push, issue close, or extra agents.

Primary integration notes (2026-09-17): THE458 investigation513 approved in a53a52d6, native code516 active. It owns one expansion budget (sourcevisits65536, recurrencework262144, occurrences8192, bucketrefs32768, bytes32MiB, payload1MiB, childentries16384, window3660days), typed failures and Arc/shared materialized payload; no transport changes. Coordinate input caps so source payload bounds do not exceed accepted expansion shapes. THE454 +418 are now in native final gate; wait for454landing before rebasing465 implementation.

Inspect ICS parser itself: core/calendar/ics.rs is hand-written and unfold currently allocates the WHOLE input Vec<String>. Account admission needs incremental unfolding/component/property/event accounting before owned event allocation, not a post-parse truncation. CalDAV parse_multistatus similarly materializes all response/calendar-data/deletions first; command proc protocol may build full serde Value before callback, so inspect that boundary and cap before CalEvent conversion. Don't solve only router forwarding.

For global concurrent byte reservations, examine ownership through returned EventPage and router collection. A permit dropped at function return doesn't account for retained page data; Clone or moving public Vec fields out of a lease can defeat accounting. Conversely awaiting capacity while router holds previous results can deadlock or permanently starve later accounts. Prefer bounded nonblocking typed refusal with correct lease lifetime and deterministic progress, or prove a fixed bound from finite account count plus finite concurrency/input/output budgets. Explain the actual peak including transport bodies, decoded/parsing data, output and metadata; do not call a counter on final events peak-memory proof. No live commands/network or Rust builds during investigation.
