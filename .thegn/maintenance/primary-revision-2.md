Primary review of49e9a4cf: revisions required, remain foundation chunks1–2

Actual narrow reproductions (no Cargo):

- Temporary real Git repo, empty commit then checkout -b renamed: admin .git has SAME dev/inode but ctime_ns changes. Receipt /home/blake/code/thegn/target/maintenance-review-20260917/next-native-batch/identity-ctime-probe.txt. Current generation hashes ctime, so an ordinary Git checkout/index.lock rename changes supposedly stable instance generation. Use a real stable instance identity (e.g. captured creation identity where actually supported, or an explicitly allocated persistent token contract), never mutable ctime. Capture/recheck the stamp across the entire inspection and make branch/registration snapshot consistency real, not just equal path strings. Same instance rename/checkout must remain stable; delete/recreate must change. Add actual Git tests, not manual bytes.
- Executed exact fresh worktree*instances DDL using Python sqlite3, then PRAGMA table_info: instance_id notnull=0; generation/repo_id defaults=NULL. verify_v69_schema expects notnull=1 and defaults16/32, so it rejects its own fresh schema. Receipt identity-schema-probe.json. Fix DDL and verifier consistently (explicit NOT NULL on instance PK), and actually run cheap extracted SQL/schema probes if useful; no Cargo/build. Verify column TYPES, CHECKs, actual unique/partial index flags and predicates, not merely names/substring (current nonunique index named uq*... would pass). Assert fresh/v68/populated/malformed/wrong-type/nonunique/wrong-CHECK schemas with Rust tests for central run.

Remaining concrete blockers:

1. util::git_stdout_bounded replaced a pipe with a predictable shared-temp pathname, default permissions, and later path reopening. This introduces path replacement/read leakage; polling file size every10ms is not a hard spool bound, descendants can continue filling disk after parent exits/gets killed, and child.wait cleanup has no bound. Replace with genuinely bounded deadline-aware capture with explicit process/pipe ownership and cleanup; reuse or factor the existing hardened bounded_git_probe pattern at an appropriate shared layer without core->host dependency. No unbounded temp spool or orphan reader/process. Platform-specific handling belongs in supported platform seam; bounded typed unsupported is explicit where needed. Add actual injected children for idle/no-output, oversized output, child holding stdout, and prompt cleanup. Do not just report tests were added: current495 has none for this helper.
2. worktree_instances_for_path still grows an unlimited Vec. Bound row count and aggregate before retaining rows; add LIMIT/ordered index as appropriate and typed overflow so no caller sees a plausible partial list. Test many colliding legacy rows, huge borrowed DB fields, and exact-limit/one-over.
3. verify_worktree_instance still authorizes first match: legacy_claims_can_share_paths_until_verified explicitly verifies first of two competing legacy rows and only refuses second. This is forbidden first-wins migration. Atomically refuse/quarantine unresolved competing claims for same path OR same(repo,admin), preserve all claims; no verified winner until explicit reconciliation evidence resolves ambiguity. Partial unique index alone does not establish absence of legacy/quarantined competing ownership. Add reverse-order/concurrent promotion tests. Checked operation_revision increment must reject i64::MAX before UPDATE, not let SQLite promote overflow to REAL.
4. ExactPath::from_git_bytes clones/converts before checking raw length; cap before first conversion. Windows native_path_bytes_bounded uses OsStr.len()\*2 (WTF-8 byte length), not encode_wide code-unit count; this falsely rejects valid Unicode paths near cap. Count bounded UTF16 units before allocation. display_label UTF8 validation currently scans arbitrary full input before bounded projection; take a bounded prefix safely if public unbounded input is supported, or explicitly require/check bounded input. Add boundary tests with Unicode/native bytes.
5. Required real inspection regressions remain absent: same-basename repos, symlink alias, CR/newline/nonUTF8 paths, detached, replacement during capture, rename vs replacement generations. The existing one ASCII worktree smoke is insufficient for claimed exactness. Implement them with private fixtures and injected probe/capture seams. No live DB/config/hooks/cleanup.

Do not extend allocation/routing yet. No Cargo/build/nextest/Clippy/fullgate. Formatting, git diff checks and small Python SQL/OS reproductions are allowed. Commit corrections and honest implementation-ready handoff; full issue remains open, all later chunks still required.
