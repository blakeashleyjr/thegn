# Primary approval: THE-458 bounded span expansion

Investigation513 is accepted, with the dependency correction below. Implement end-to-end core expansion + existing month/reminder consumers, source tests, docs, committed artifact. No Cargo/build/nextest/Clippy/full suites; primary owns serialized gates. No merge/push/closure/spawn.

Dependency decision: THE-457 is NOT a blocker and is not newly opened in this batch. Implement only the recurrence work/checked-shift/lookback guard required to make THIS expansion boundary bounded; leave unrelated recurrence validation/reminder policy under457. THE-465 will separately enforce source admission. Do not wait for those tickets or claim them fixed. Remote transport454 remains separate and is currently native-gating with418; no transport changes here.

Approved architecture:

1. Replace allocating whole-lifetime dates_in with a window-aware bounded iterator/result; never allocate dates just to test overlap. Resolve date interval once, check malformed/inverted spans, preserve all-day/timed-midnight EXCLUSIVE endings and zero-duration documentedpoint behavior. Use checked arithmetic at Chrono extrema. Return a typed InvalidWindow/InvalidSpan/Arithmetic/Budget error for unsupported computation, never plausible partial Ok. Rework old public helpers/callers so no production path retains the unbounded wrapper; tests can use short explicitwindows.
2. One shared ExpansionBudget for the WHOLE call, not renewed per event/account/recurrence: max window3660days; source visits65536; recurrence work262144steps; materialized occurrences8192; bucket/shared-reference entries32768; retained bytes32MiB. These are safe hard ceilings, not newconfigfeatures. Allow smaller validated budgets in tests; zero budget never means unlimited. Count eventpayload strings/map entries/reminder/recurrence containers and result/map/vector overhead using checked arithmetic BEFORE clone/reserve. A single event payload max1MiB and max16384 aggregate child entries prevents preflight itself becoming unbounded. Errors redacted, constant-sized. Ceiling changes require concretevalid-caseevidence andprimaryreview.
3. Shared concrete occurrence representation (Arc<CalEvent> or an arena with indices) stores one nonrecurring materialized event payload per occurrence; date buckets clone only handles. Have a unique occurrence view for reminders, never flatten buckets into duplicate CalEvents. Existing CalendarPayload/Docs/CalState consumers must preserve sharing across hydration/cache/view, not re-clone payloads at adapters.
4. Recurring duration overlap: derive checked nominal span; consider starts before from which overlap window. Include long-duration first/RDATE/COUNT=1 cases without walking the duration. Fast-forward uncounted simple rules where semantics can be proven; for complex COUNT/EXDATE/BYSETPOS etc preserve semantics under one finite workbudget or return typed Budget. Do not silently drop earlier overlaps or pretend exhausting a walk is an empty complete calendar. Every innerperiod/candidate loop must consume shared work BEFORE growth, including huge BY\*crossproducts. No attempt to fully redesign457.
5. Month path receives Result. On error preserve last valid snapshot and mark the existing view status as incomplete/unavailable; first-load failure must not render a successful 'no events'. Reuse existing status/error presentation, no newfeature or newcontrols. Clear error after complete successfulrefresh. All expansion remains off-loop.
6. Reminder path consumes unique complete occurrences; on failure emit no reminders from partial data. Advance the successful evaluation cursor only after success acknowledgment from worker, not at dispatch. Bound retry interval/lookback/work without a hot loop: one inflight request plus next existing ticker retry; stale completion cannot regress a newer cursor. Visible bounded/coalesced failure status usingexistingstatus/log path; do not logeventcontents. Don't swallow malformed spans into defaults.

Required tests: century/allChrono-boundary nonrecurring spans one-day query and outsidequery do O(window) iteration; midnight/all-dayexclusive/zero-duration/inverted; oldrecurrence longduration startsbeforewindow inclCOUNT1/RDATE; validDST existingfixtures; complex workexhaustion refuses; each budget exact/oneover and multirow aggregate; largepayload multi-day assert Arc pointer equality/sharedarena; host completeempty vs firstloaderror vs lastgoodretain/recovery; reminders uniqueoccurrence/no duplicate, failurecursorretained/retryable/staleack. Add deterministic work/allocation accounting assertions; wallclock-only timing not sufficient. Test normalrenderprojection unchanged and failurevisible with focusedrenderfixtures. Run rustfmt/diffcheck/source-onlyratchets; exactruntimecommands pendingreport (host isbin, no --lib).
