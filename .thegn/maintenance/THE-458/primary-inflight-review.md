# Primary review during THE-458 code row 516

Recheck these against your final code; the worker was still editing when observed. Do not run Rust builds/tests. These are within the approved plan, not new feature work.

1. Duration lookback must not be silently clamped with lookback.min(max_window_days): a COUNT=1 or RDATE event starting 20 years ago and ending tomorrow must be present in today's narrow query. Intersect span directly or use the full checked lookback with bounded recurrence work/refusal. Simple single-occurrence/explicit RDATE overlap must succeed cheaply. Budget exhaustion may return typed error, never a plausible complete empty calendar that missed earlier overlap. Do not underflow from near NaiveDate::MIN unnecessarily for events whose bounded overlap is decidable.
2. CalEvent materialized={..self.clone()} currently happens BEFORE budget.occurrence/bytes and even for nonoverlap. Determine overlap from borrowed start/end fields first, reserve all retained allocation before cloning. Per-event byte preflight must include EventTime/TzRef strings in start/end/RDATE/EXDATE, all recurrence payload/collections and BTreeMap overhead conservatively, not child_count \* sizeof(usize) for arbitrarily larger children. Account bucket/occurrence pointer storage too or explicitly bound conservative aggregate peak. Apply source payload/children bounds even if a nonrecurring event is offwindow; bound traversal of all skipped RDATE/EXDATE and rules, not only accepted values.
3. Nonrecurring inverted start/end on the same date currently passes occupied_dates_in because only date order is checked; enforce checked full span validation consistently. Preserve a documented zero-duration point, all-day exclusive end, timed midnight exclusivity including subseconds (00:00:00.500 is NOT midnight). Checked yearly interval\*12 and subdaily/date stepping must not overflow or return plausible partial success on arithmetic failure.
4. Reminder worker receives checked_ms but due_reminders_result uses a newer Utc::now as its end. That can raise reminders past the acknowledged cursor and duplicate on next tick. Pass the exact supplied [last_checked_ms,checked_ms] through query/evaluation, and acknowledge that same window only after full success. Fence acknowledgments by the actual pending window/generation, not a bare bool+max timestamp; stale ack must not clear a newer pending job. Add deterministic cursor success/failure/stale/inflight tests.
5. load_cached currently hides DB errors as an empty Vec and silently discards malformed rows. New month/reminder paths must propagate those failures as unavailable/incomplete, preserve last-good month/cursor, and surface a truthful diagnostic. Db::open mapped to Arithmetic is misleading; use a host-side error that distinguishes cache failure from expansion. Ensure any retained Vec-returning compatibility helper cannot be used by production to swallow budget failure into no events; remove/cfg(test) dead wrappers as appropriate.
6. Core tests need actual long COUNT1/RDATE overlaps, budget-before-clone refusal, earliest/latest representable dates, fractional-midnight and same-day inversion, excessive BY crossproducts, many EXDATE/RDATE skips, multiday Arc pointer identity and unique reminders. Host tests must prove last-good retention plus visible first-load failure and recovery, exact worker window/cursor and no duplicate notification on a multiday event.

Use source ratchets and format checks before final report. Independent review/central runtime tests still follow.
