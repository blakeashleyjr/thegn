# Primary review of 86b7a4a2 — foundation remains blocked

The fresh v69 column defaults/NOTNULL and partial uniqueness improvements are useful, but do not start later chunks until these exact defects are corrected. No Cargo/builds; source checks and lightweight owned OS/SQLite probes permitted.

1. Compile defects apparent in source: bounded_stdout moves child into the map_err closure and uses child afterward; test Command bindings are immutable, .args returns &mut Command while helper accepts owned Command, and tests never configure stdout piped; WorktreeIdentityRecord lacks Clone although registered_entry returns entry.clone() as an owned record. Correct these before requesting central compilation.
2. Per-probe detached reader and per-timeout detached reaper threads are not a bounded lifecycle. The thread handles are dropped, Windows only kills direct child, and a pipe-holder/setsid child can retain an arbitrary succession of readers. Calling an unbounded child.wait in a newly spawned thread is not bounded reaping. Do not repeat this pattern. Reuse/factor the ownership/budget approach of host bounded_git_probe (no core->host dependency): finite shared capacity held until all reader/reaper/process ownership is settled, bounded shared reaper queue established before child spawn, safe platform process-tree ownership and no numeric PID signaling after reaping/reuse. Success/error cleanup must preserve ownership, including reader setup failure and early leader exit. Platform-specific implementations belong behind an appropriate core platform seam. State actual guarantees and supported-platform limitations; no pretend Windows parity. Add tests that repeatedly saturate a held pipe and assert bounded active readers/children, overflow, timeout, setup/spawn failure, EOF/leader race, and real completion reclamation. Prefer reuse over inventing a second general subprocess engine.
3. same-file dev+inode alone is object identity while held, not persistent creation generation; the handle is dropped before returning the stamp. Include a supported OS birth/creation identity or an explicitly owned persisted generation contract, held/revalidated through inspection. Stable std Metadata::created via the SAME opened same_file::Handle::as_file metadata is available to inspect; fail closed if unavailable instead of using ctime or silently only inode. Verify platform precision and support. Keep checkout/branch rename stable and replacement distinct. Do not claim our 2048-iteration tmpdir inode probe observed reuse (it did not); this is a contract gap, not an observed repro. Avoid unstable Windows MetadataExt.
4. verify_worktree_instance reintroduced unbounded row.get::<Vec<u8>>/String for path/repo/admin/generation/state/reason before checks. Use borrowed ValueRef preflight and bounded conversion just like the ledger decoder. worktree_instances_for_path checks aggregate after decoding/allocating a full row; preflight row and aggregate before cloning into retained result. Add actual indexes for non-verified path and repo/admin lookups; LIMIT with ORDER BY without a matching index still scans/sorts unbounded legacy claims. Use EXISTS rather than COUNT for competing claim existence. Preserve atomic ambiguity refusal, revision overflow and all rows.
5. Schema CHECK verification is still table_sql.contains(check). A comment/string literal can spoof it without an actual constraint. Compare an exact canonical known DDL shape (shared constants are simplest), or inspect/enforce semantics robustly; add wrongtype, nonunique, wrongpredicate and CHECK-comment-spoof fixtures, not one misleadingly named test that exercises only wrongpredicate. Private Python SQLite probes are allowed to prove fresh DDL matches verifier before central Rust build.

Remaining issue chunks3–8 remain required after this foundation. Keep reports implementation-ready with unverified work; no merge/push/closure/child agents. Primary will review and centrally validate before moving on.
