Primary dependency coordination

Investigate only until plan approval. THE-516 has an approved foundation plan in its sibling worktree, .thegn/maintenance/primary-code-1.md, with detailed investigation .thegn/pipeline/THE-516/maintenance-plan-revision/477.md. RepositoryId is one versioned domain-separated lossless canonical Git-common-dir digest, shared by linked worktrees. Do not create another basename/origin-slug identity. Origin is mutable, so it should be a separately verified authority revision bound to the admitted repository/config snapshot; changing an origin must invalidate existing overlay/launch authority without pretending two clones with the same origin are the same repository. Moving the common dir requires explicit trust remapping. Explain any mismatch between full issue wording and this policy in the plan before implementing.

THE-505 is building one immutable AdmittedConfig/ConfigRevision boundary; reuse that generation rather than introducing another ad hoc config hash or ambient reread. Queue and launch attestations must preserve exact verified repository identity, origin authority revision, and config revision through the first side effect and final publication. Core stays substrate-free; filesystem/Git capture is a host/service job. Handle unsupported platform identity explicitly before effects.

For legacy aliases, never infer trust from the current directory or first matching database row. Preserve source blocks and refuse ambiguous/unmapped authority visibly. Prefer extending existing config explain/edit/repo surfaces for explicit mapping rather than creating unrelated new features. Enumerate all consumers, including review_handoff.rs, lifecycle, queue gates, accounts/bundle, MCP, and sandbox mounts. Safe disabling alone is not complete if it silently breaks unambiguous configured repositories without a supported migration.

Workers are Luna high. No Cargo/builds/full tests, merges, pushes, issue closure, child agents, or cross-worktree edits. Primary coordinates reviewed stacking and centrally verifies Rust changes.
